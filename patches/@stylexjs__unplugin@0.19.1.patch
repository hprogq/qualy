diff --git a/lib/core.js b/lib/core.js
index 23e8c47ad8f19139b70ad89df42bf9d899fa21cd..4a237ec36f8f94424111dd76aaec965cc268f496 100644
--- a/lib/core.js
+++ b/lib/core.js
@@ -34,7 +34,29 @@ function pickCssAssetFromRollupBundle(bundle, choose) {
 }
 function processCollectedRulesToCSS(rules, options) {
   if (!rules || rules.length === 0) return '';
-  const collectedCSS = _babelPlugin.default.processStylexRules(rules, {
+  // A rule that refers to a constant whose defining module has not been
+  // collected cannot be written. Where the stylesheet is regenerated after
+  // every transform (dev), a consumer of defineConsts is routinely
+  // transformed before the module that defines them, so such a rule is
+  // left out until the definitions arrive. Where the stylesheet is final
+  // (a build), the same situation is a lost @media rule, and it is refused.
+  const known = new Set();
+  for (const rule of rules) {
+    const meta = rule && rule[1];
+    if (meta && meta.constKey != null) known.add(meta.constKey);
+  }
+  const unresolved = [];
+  const writable = rules.filter(rule => {
+    const ltr = rule && rule[1] && rule[1].ltr;
+    const ref = typeof ltr === 'string' ? /^var\(--([^)]+)\)/.exec(ltr) : null;
+    if (ref === null || known.has(ref[1])) return true;
+    unresolved.push(ref[1]);
+    return false;
+  });
+  if (unresolved.length > 0 && options.unresolvedConstants !== 'skip') {
+    throw new Error(`stylex: rules refer to constants no collected module defines: --${[...new Set(unresolved)].join(', --')}; import the .stylex file that defines them`);
+  }
+  const collectedCSS = _babelPlugin.default.processStylexRules(writable, {
     useLayers: options.useCSSLayers ?? false,
     enableLTRRTLComments: options?.enableLTRRTLComments
   });
@@ -289,7 +311,7 @@ const unpluginFactory = (userOptions = {}, metaOptions) => {
       } catch {}
     }
   }
-  function collectCss() {
+  function collectCss(mode) {
     const merged = new Map();
     if (devPersistToDisk) {
       try {
@@ -308,7 +330,8 @@ const unpluginFactory = (userOptions = {}, metaOptions) => {
     return processCollectedRulesToCSS(allRules, {
       useCSSLayers,
       lightningcssOptions,
-      enableLTRRTLComments: stylexOptions?.enableLTRRTLComments
+      enableLTRRTLComments: stylexOptions?.enableLTRRTLComments,
+      unresolvedConstants: mode?.unresolvedConstants
     });
   }
   async function persistRulesToDisk(id, rules) {
diff --git a/lib/es/core.mjs b/lib/es/core.mjs
index 583431190aab3aad75c831ff982d84a3bcc48341..3678117f066a6d6993545cc1383689982ca4ec59 100644
--- a/lib/es/core.mjs
+++ b/lib/es/core.mjs
@@ -25,7 +25,29 @@ export function pickCssAssetFromRollupBundle(bundle, choose) {
 }
 function processCollectedRulesToCSS(rules, options) {
   if (!rules || rules.length === 0) return '';
-  const collectedCSS = stylexBabelPlugin.processStylexRules(rules, {
+  // A rule that refers to a constant whose defining module has not been
+  // collected cannot be written. Where the stylesheet is regenerated after
+  // every transform (dev), a consumer of defineConsts is routinely
+  // transformed before the module that defines them, so such a rule is
+  // left out until the definitions arrive. Where the stylesheet is final
+  // (a build), the same situation is a lost @media rule, and it is refused.
+  const known = new Set();
+  for (const rule of rules) {
+    const meta = rule && rule[1];
+    if (meta && meta.constKey != null) known.add(meta.constKey);
+  }
+  const unresolved = [];
+  const writable = rules.filter(rule => {
+    const ltr = rule && rule[1] && rule[1].ltr;
+    const ref = typeof ltr === 'string' ? /^var\(--([^)]+)\)/.exec(ltr) : null;
+    if (ref === null || known.has(ref[1])) return true;
+    unresolved.push(ref[1]);
+    return false;
+  });
+  if (unresolved.length > 0 && options.unresolvedConstants !== 'skip') {
+    throw new Error(`stylex: rules refer to constants no collected module defines: --${[...new Set(unresolved)].join(', --')}; import the .stylex file that defines them`);
+  }
+  const collectedCSS = stylexBabelPlugin.processStylexRules(writable, {
     useLayers: options.useCSSLayers ?? false,
     enableLTRRTLComments: options?.enableLTRRTLComments
   });
@@ -280,7 +302,7 @@ export const unpluginFactory = (userOptions = {}, metaOptions) => {
       } catch {}
     }
   }
-  function collectCss() {
+  function collectCss(mode) {
     const merged = new Map();
     if (devPersistToDisk) {
       try {
@@ -299,7 +321,8 @@ export const unpluginFactory = (userOptions = {}, metaOptions) => {
     return processCollectedRulesToCSS(allRules, {
       useCSSLayers,
       lightningcssOptions,
-      enableLTRRTLComments: stylexOptions?.enableLTRRTLComments
+      enableLTRRTLComments: stylexOptions?.enableLTRRTLComments,
+      unresolvedConstants: mode?.unresolvedConstants
     });
   }
   async function persistRulesToDisk(id, rules) {
diff --git a/lib/es/vite.mjs b/lib/es/vite.mjs
index 12cea8cda04db57ce92759979e2c072e6c261a9b..a3c4f8c96455e44c37dc78f2928642f41e4974b4 100644
--- a/lib/es/vite.mjs
+++ b/lib/es/vite.mjs
@@ -48,7 +48,9 @@ function attachViteHooks(plugin) {
           res.statusCode = 200;
           res.setHeader('Content-Type', 'text/css');
           res.setHeader('Cache-Control', 'no-store');
-          const css = plugin.__stylexCollectCss?.();
+          const css = plugin.__stylexCollectCss?.({
+            unresolvedConstants: 'skip'
+          });
           res.end(css || '');
           return;
         }
@@ -68,6 +70,7 @@ function attachViteHooks(plugin) {
             } catch {}
           }
         }, 150);
+        interval.unref?.();
         server.httpServer?.once('close', () => clearInterval(interval));
       }
     },
diff --git a/lib/vite.js b/lib/vite.js
index 1c81eb81c9cc4648d3b623454b912adf5592af9c..f9a0cfdb84a9543a021b7b834f1a8f49aa38ea90 100644
--- a/lib/vite.js
+++ b/lib/vite.js
@@ -55,7 +55,9 @@ function attachViteHooks(plugin) {
           res.statusCode = 200;
           res.setHeader('Content-Type', 'text/css');
           res.setHeader('Cache-Control', 'no-store');
-          const css = plugin.__stylexCollectCss?.();
+          const css = plugin.__stylexCollectCss?.({
+            unresolvedConstants: 'skip'
+          });
           res.end(css || '');
           return;
         }
@@ -75,6 +77,7 @@ function attachViteHooks(plugin) {
             } catch {}
           }
         }, 150);
+        interval.unref?.();
         server.httpServer?.once('close', () => clearInterval(interval));
       }
     },
