pnpm-lock.yaml
db/migrations/

# generated artifacts: the generator owns their bytes, and the assembly
# start-up check compares them against what the manifest produces
*.gen.ts
cordis.gen.yml

# vendored upstream sources: read-only reference, never formatted or linted
repos/
