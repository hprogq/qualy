// What the door is willing to say a cited file is (§18, §19).
//
// One answer, and it is storage's: every route a stored file leaves by goes
// through the same rule, so the streamed door here and the signed url a
// backend mints cannot disagree about what a file claims to be. A second
// list here would be a second thing to keep in step, and the one that drifts
// is the one nobody is looking at.
export { BYTES, servedTypeOf } from '@qualy/plugin-storage/server'
