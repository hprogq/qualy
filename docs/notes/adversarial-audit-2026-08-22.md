# 对抗审查(2026-08-22,第三、四轮)

> 两个工作流、113 个 agent、34 条主张。每条主张交由**三名独立反驳者**(机制 / 可达性 / 先例与时效)交叉验证,默认立场是「反驳」,两票以上不反驳才记为确证。确证 **33** 条,反驳 1 条。**全部未修复**,本文件即修复工作清单。

与前两轮(同日,已全部修复并推送)的关系:那两轮打的是 assessment 的审核/申报/授权/并发/数据层,以及存储、API 契约、实时通道、Effect 纪律与测试门禁。本轮**刻意避开**那些靶区,并把当天落地的 12 笔修复本身当作嫌疑对象重审。已修复的 12 条与两条已知待办写进了 agent 的禁报清单,因此本文件中没有重复项。

**修复前请逐条重验**:代码可能已漂移,且部分主张的「后果」被反驳者部分推翻(见每条的「反驳者校正」)。

- 第三轮(63 agent):前两轮从未打开过的域:计分与成绩、阶段引擎、认证与身份、rbac 内部、装配层与 CLI、组织树——确证 18 条
- 第四轮(50 agent):当天 12 笔修复自身,加四条横切扫描:租户隔离、无界工作、UI 投影与 i18n 边界、构建与生产一致性——确证 15 条

## 亲自复核过的两条(不只依赖 agent)

**`Ui.browser` 模块在生产构建里被 tree-shake 掉——实测成立,严重度应上调为 critical。**
生成的聚合模块对每个 browser 模块写的是求副作用的裸 import(`packages/build/web/src/collect.ts:206`),
而 `@qualy/plugin-storage-local` 与 `@qualy/plugin-storage-cos` 的 package.json 都声明了
`"sideEffects": false`。在当前已 staged 的生产产物里搜该驱动自己的三个字符串——
`upload refused with status`、`upload cancelled`、`lengthComputable`——**一个都不存在**;
产物里唯一的 `XMLHttpRequest` 出自无关的第三方库(HAR 代码片段生成器)。
也就是说生产环境下 `registerUploadDriver` 从未执行,drivers 表为空,**所有附件上传必然抛
`UploadUnsupported`**;dev 下 Vite 不做 tree-shaking,所以本地一切正常。
生产 smoke(tools/quality/smoke-production.ts)只断言探针、壳、manifest、哈希资源与 SIGTERM,
看不见这一类「产物里少了一段副作用代码」的缺失,因此修复时应同时给它一条能看见的断言。

**巡检人员缓存键漏了批次——实测成立。**
`packages/plugins/assessment/core/src/server/index.ts:3591` 的键是
`${round.currentNodeId}:${roleIds}`,而它缓存的 `reviewersAt` 调用是带 `batchId` 的,
且该查询按批次判定授权是否被该批次接受(batch_access_sources)。同租户两个批次共用同一个
(节点, 角色)组合时共享一条答案:先被扫到的批次若尚未接受该授权,算出的 0 会被套到另一个
满编批次的轮次上,把它们全部扣成 `no-assignee`,而且每一轮扫描都会重现同样的错误。

## 严重(critical)

### [critical · phase-engine] replacePlan lets an unscheduled phase be reordered ahead of an entered one, corrupting the plan permanently

- 位置:packages/plugins/assessment/core/src/server/index.ts:2929
- 判定:CONFIRMED,3/3 upheld(第三轮)
- 主张:On an active batch the plan write only checks that the _committed_ phases keep their relative order; it never checks that they stay at the front. Moving an unscheduled phase above an entered/scheduled one is accepted and written, producing a row order that normalizePlan declares corrupt, which makes every later read of that batch (and of the batch list page containing it) die.
- 失败场景:Batch B is active with plan [P0 entered, P1 unscheduled]. An administrator (or any client) calls PUT .../phases with specs=[{id:P1,...},{id:P0,...}]. At index.ts:2919 committed=[P0]; at 2929 submittedIds.filter(id=>committedIds.includes(id))=[P0] which equals committedIds, so 'reorder-not-allowed' is NOT raised. No other rule inspects the position of an existing phase (the per-spec loop at 2943 only reviews fieldEditsOf; reviewInsertion at 2957 only runs for specs without an id). writePlanOrder then parks and rewrites ordinals, leaving P1 at ordinal 0 and P0 (actualEntryAt != null) at ordinal 1. From that moment normalizePlan (phase/engine/queue.ts:22-26) throws 'phase plan is corrupt: phase P0 entered while an earlier phase has not' on every call to toSnapshots — i.e. Assessment.timeline (3178), gate/authorizeEntryAction (1358, 1873), readDetail (1009), schedulePhase (3044), advancePhase (3117), setBatchStatus (2371) and, because listBatches builds every row's plan the same way (2076), the entire batch list page for anyone who can see B. Students cannot file or submit, reviewers cannot decide, and the plan cannot be repaired through the API: replacePlan itself dies at 2936 before it can accept a corrective order, and deleteBatch refuses because a phase has been entered (2795-2803). Only direct SQL restores the batch.
- 证据:index.ts:2919-2933 — committed rows are filtered out of the submitted list and compared only against each other, so an unscheduled row may be interleaved anywhere. index.ts:2943-2954 — existing specs get only fieldEditsOf reviews, no positional review. index.ts:1157-1173 — writePlanOrder assigns ordinal=index from the submitted order unconditionally. phase/engine/queue.ts:16-33 — normalizePlan throws on an entered phase preceded by an unentered one and on a scheduled phase preceded by an unscheduled one. index.ts:392-405 — toSnapshots is normalizePlan, used by every phase read. index.ts:2076 — listBatches calls it per row. The DB has no guard: batch_phases only has unique (tenant,batch,ordinal) (db/entities.ts:209-213). The client happens to prevent it (PhaseTimelineEditor.tsx:265-273 refuses a move target below shape.scheduled, PhaseRow.tsx:216-224 renders the arrows only for structural rows), which is why the server hole is unnoticed — the 'reorder-not-allowed' refusal exists precisely to hold this line and there is no server test for it (no hit for 'reorder' anywhere under packages/plugins/assessment/core/tests).
- 最小修法:Compare the whole submitted order against the retained order for the committed prefix, not just the committed subsequence: require that every committed row keeps its absolute index (submittedIds.slice(0, committedIds.length) deep-equals committedIds) before any write, and add a node test asserting 'reorder-not-allowed' for [unscheduled, entered].
- **反驳者校正**:The claim is correct on mechanism; three refinements.

(a) Reach, not "any client": `replacePlan` calls `requireRosterReach` (index.ts:2822, defined 1564-1579), so the caller needs `assessment.batch.manage` authority over the batch's management/roster anchors (or tenant-wide MANAGE for an anchorless batch). It is an authorized batch manager sending a hand-rolled payload, not an anonymous client.

(b) Two corrupt shapes, not one. `committed` at 2919-2921 includes merely _planned_ rows, so moving an unscheduled row above a planned-but-not-entered row passes the same subsequence test and trips the third `normalizePlan` invariant instead — `phase ... is scheduled while an earlier phase is not` (queue.ts:28-31). A fix must hold the whole scheduled prefix, not just the entered one.

(c) The corruption is silent and the scheduler is also hit. A pure reorder yields empty `editedIds`/`insertedKeys` (index.ts:2996-3009), so no config event is written (3010-3019) and `readPlan` (1030-1043) does not normalize — the request returns 200 with the reordered plan and no audit trace. Beyond the read sites listed, the background scheduler sweep also calls `toSnapshots` per candidate batch (index.ts:3790), so the defect escapes the corrupt batch's own requests.

The proposed fix is sound as written because insertions are separately guarded (edits.ts:213-218 refuses `position <= scheduledIndex(plan)`); it should compare against the full committed set including planned rows, which `committedIds` already is.

- **反驳者校正**:The defect is real and reachable, but the claim is mis-stated in four ways. (a) It does not require an entered phase: normalizePlan's second rule (src/phase/engine/queue.ts:26-30) also refuses a scheduled phase behind an unscheduled one, so [P0 scheduled, P1 unscheduled] — the ordinary shape of a batch just activated by schedulePhase (index.ts:3063-3067) — is enough, and the corruption window opens at activation rather than at first entry. (b) deleteBatch refuses at index.ts:2788 (`from !== 'draft'`), not at the entered check on 2795-2803; the active-status check fires first. Same conclusion, different line. (c) getPlan (index.ts:2811-2816) does NOT die, because readPlan (1030-1043) skips normalizePlan, so the plan editor still loads the corrupt batch; the dying reads are exactly the toSnapshots sites (1009, 1358/1873, 2076, 2371, 2936/2942, 3044/3049, 3117, 3178). (d) The blast radius is larger than claimed: sweepDueBoundaries (3775-3796) calls toSnapshots at 3790 inside a per-batch transaction whose only recovery is Effect.catchTag('QueryFailed') at 3794, and a corrupt batch with an unentered, due plannedEntryAt is selected by batchesWithDueBoundaries (server/db.ts:1784-1803); the thrown Error is a defect, not QueryFailed, so it escapes the loop and stops boundary ratification for every tenant, not just the affected batch. The proposed fix is the right shape (require submittedIds.slice(0, committedIds.length) to deep-equal committedIds before any write, plus a node test for [unscheduled, entered]), and it should also cover the scheduled-but-not-entered case.
- **反驳者校正**:The claim stands as written; two details in its prose need adjusting, neither of which weakens it.

1. setBatchStatus does not die at index.ts:2371 in general - that toSnapshots call is inside the `input.status === 'archived'` branch only (2367-2385). The archived->active reopen branch (2397 onward) reaches the corrupt plan by a different route: it returns `readDetail` at index.ts:2470, and readDetail calls toSnapshots at index.ts:1009. So every setBatchStatus call on the corrupted batch still dies, just via readDetail rather than line 2371.

2. The proposed fix is sufficient as stated, and worth recording why: a newly inserted spec (no id) placed at index 0 would slip past a prefix comparison over id-lists alone, but that case is already refused independently by reviewInsertion at edits.ts:215-221 (`position <= view.effectiveIndex` -> insert-not-after-current, `position <= scheduledIndex(plan)` -> scheduled-phase-immutable), called from index.ts:2957. So requiring `submittedIds.slice(0, committedIds.length)` to deep-equal `committedIds` closes the remaining hole, which is repositioning of _existing_ rows only. Two node tests are warranted, not one: [unscheduled, entered] and [unscheduled, scheduled], since normalizePlan (queue.ts:22-31) has a separate throw for each.

## 重要(major)

### [major · build-parity] Browser modules declared with Ui.browser are tree-shaken out of the production bundle, so no upload driver is registered and every attachment upload fails in production while working in dev

- 位置:packages/build/web/src/collect.ts:206
- 判定:CONFIRMED,3/3 upheld(第四轮)
- 主张:The generated aggregate reaches Ui.browser contributions with a bare side-effect import (`import "…/upload.ts"`), but both plugins that use it declare `"sideEffects": false` in their package.json. Vite/rolldown therefore reports moduleSideEffects=false for those modules and drops them from the release bundle. `vite dev` and the browser test runner do not tree-shake, so the drivers register there and the defect is invisible to every gate the repo has.
- 失败场景:A student on a production deployment (`pnpm build` + `pnpm start`, storage backend `local`, the committed qualy.yml default) attaches an evidence file in EvidenceForm. `uploadFile` (packages/plugins/assessment/core/src/client/entry/upload.ts:37) succeeds in calling `prepare`, so the server mints a reservation and an attachment row. It then calls `upload(ticket, …)`, which looks the grant's driver name up in the registry map (packages/plugins/infra/storage/src/client/index.ts:66). The map is empty because `packages/plugins/infra/storage-local/src/client/upload.ts:39` never executed, so it throws `UploadUnsupported: no upload driver named "local" is loaded`. The bytes are never sent, `complete` is never called, and the reservation is orphaned. Every attachment upload on every production build fails this way, on every backend. The exact same click works under `pnpm dev`.
- 证据:packages/build/web/src/collect.ts:204-207 emits `import "<relative path>"` for each browserModule; apps/web/.qualy/plugins.ts:14 and plugins.all.ts show the emitted lines for storage-local (and storage-cos in the superset). packages/plugins/infra/storage-local/package.json:7 and packages/plugins/infra/storage-cos/package.json:7 both set `"sideEffects": false`. node_modules/vite/dist/node/chunks/node.js:32943 resolves modules with `moduleSideEffects: pkg.hasSideEffects(resolved)` and node.js:1948-1955 turns a boolean `false` into `() => false`. Empirically, in the committed build output apps/web/dist (built 2026-08-22 00:41, i.e. current): grep over apps/web/dist/assets finds NONE of the driver's distinctive strings — "upload refused", "upload cancelled", "lengthComputable", "request.upload", "registerUploadDriver" — while the registry side IS present ("no upload driver named" and "UploadUnsupported" in EvidenceForm-bmdB84BJ.js). The rest of the aggregate did bundle correctly (all seven i18n namespaces and every component chunk are present), so this is not a stale artifact. No gate covers it: apps/web/scripts/check-chunks.ts:12-14 only enumerates `entry.components` and never looks at `browserModules`; tools/quality/smoke-production.ts:100-111 only fetches the shell, the manifest and one hashed asset; the browser suite runs `vite serve` (vitest.browser.config.ts:17-18), which doe…
- 最小修法:Make the aggregate's browserModule import non-elidable rather than relying on package metadata — e.g. emit `import * as m0 from "…"` and export the namespaces from the virtual module (an unused re-export still keeps the module), or have Ui.browser modules export their driver and have the aggregate call `registerUploadDriver` explicitly. Then extend apps/web/scripts/check-chunks.ts to assert each declared browserModule survived the release bundle, since that is the gate that was supposed to see this.
- **反驳者校正**:The finding stands, with the mechanism restated. The generated aggregate (apps/web/.qualy/plugins.all.ts:14-15, emitted by packages/build/web/src/collect.ts:206) reaches every Ui.browser contribution with a bare, relative side-effect import. Both contributing packages declare "sideEffects": false (packages/plugins/infra/storage-local/package.json:7, packages/plugins/infra/storage-cos/package.json:7). The bundler that acts on that is ROLLDOWN, not vite's node-resolve branch: vite 8.2.0 ships rolldown ~1.2.0, and rolldown applies the package.json sideEffects field natively at precedence level 5 (node_modules/.pnpm/rolldown@1.2.1/node_modules/rolldown/dist/shared/define-config-DSMNXceb.d.mts:2754-2761). The auditor's cited line, node_modules/vite/dist/node/chunks/node.js:32943, is inside tryNodeResolve's bare-specifier branch (guarded at node.js:32907-32911) and is never reached for a relative import, so that specific evidence is misattributed even though the outcome is identical.

Consequence, verified in the committed build output apps/web/dist (built 2026-08-22 00:41): both driver modules are gone — no "lengthComputable", "upload refused", "upload cancelled", "localUploadDriver", "cosUploadDriver", and no cos-js-sdk-v5 — and even registerUploadDriver's own body ("two upload drivers registered", packages/plugins/infra/storage/src/client/index.ts:40) was shaken as an unused export, proving nothing in the bundle calls it. The registry's failure path did ship ("no upload driver named", "UploadUnsupported" in EvidenceForm-bmdB84BJ.js). So uploadFile (packages/plugins/assessment/…

- **反驳者校正**:The claim stands as a real, reachable, production-only defect. Two small refinements to how it is stated:

1. Which generated file matters. A release build reads apps/web/.qualy/plugins.all.ts, not plugins.ts — packages/build/web/src/vite.ts sets `all = environment.command === 'build'` and `at()` selects the `.all.ts` variant. The relevant emitted lines are plugins.all.ts:14-15 (storage-cos and storage-local). plugins.ts:14 is the dev/browser-runner set, which is precisely why dev is unaffected. This strengthens rather than weakens the claim: both backends are dropped, so switching defaultBackend to `cos` does not avoid it.

2. A sharper proof than the driver-string absence. Beyond the missing driver strings, the string `two upload drivers registered` — the duplicate guard inside `registerUploadDriver` at packages/plugins/infra/storage/src/client/index.ts:41 — is absent from the entire apps/web/dist, while `no upload driver named` and `UploadUnsupported` (index.ts:47-49) are present in assets/EvidenceForm-bmdB84BJ.js. The registration entry point was itself tree-shaken as an uncalled export, which proves no module in the shipped bundle registers a driver. This also explains why the registry got inlined into the EvidenceForm chunk rather than hoisted into a shared one: EvidenceForm is its only remaining consumer.

- **反驳者校正**:The defect and the fix direction are right; three evidence details are mis-stated, none of which rescue the code.

1. Production-path proof was understated, not overstated. apps/web/dist is gitignored (.gitignore:5 matches `dist/`), so it is not "committed build output" - it is the current local build. The artifact production actually serves is the staged copy at packages/plugins/infra/web/client-dist (staged by packages/build/web/src/stage.ts:10 as the second half of the root `build` script, package.json:45), and a grep there for "registerUploadDriver"/"lengthComputable" also returns nothing. That is the stronger citation.

2. Two of the five "distinctive strings" are not distinctive. "XMLHttpRequest" and "octet-stream" DO occur in apps/web/dist/assets/src-KYsIm6LB.js, but from the unrelated httpsnippet/API-docs vendor bundle (the surrounding text is an ofetch/xhr code-snippet generator), and "octet-stream" also occurs in EvidenceForm-bmdB84BJ.js because assessment core's uploadFile writes `file.type || 'application/octet-stream'` (packages/plugins/assessment/core/src/client/entry/upload.ts:38). "registerUploadDriver" is likewise weak evidence - it is an imported binding name a minifier is free to mangle. The load-bearing absences are the three unmanglable ones: the literals "upload refused" and "upload cancelled" and the DOM property "lengthComputable" (storage-local/src/client/upload.ts:19, 26, 29), none of which appear anywhere under apps/web/dist.

3. The blast radius is wider than the two storage plugins. `"sideEffects": false` is set identically in 22 of the repo's p…

### [major · fresh-fixes] requestSupplement still authorizes and gates on a pre-lock snapshot — the exact hole a124973b closed in decideReview

- 位置:packages/plugins/assessment/core/src/review/service.ts:1705
- 判定:CONFIRMED,3/3 upheld(第四轮)
- 主张:a124973b moved decideReview's round read to after the batch lock so a word is judged against the step the round stands on, but left requestSupplement (and its two siblings) reading the round first and authorizing from that snapshot, and the only concurrency guard it has (setInstanceSupplementState) compares state, not route/stageId.
- 失败场景:Item chain: stage `class` (S1, reviewers A and B) then stage `senior`/college (S2, reviewer C). Round R is active at S1. B POSTs supplement-requests while A POSTs a decideReview approve.

1. B's transaction runs `instanceOf` (line 1703) and gets R@S1/active; `requireJudge` (1705) calls `mayAct`, which judges from `row.currentNodeId`/`row.currentRoleIds` of that snapshot (lines 745-758) → B passes.
2. B blocks on `lockBatch` (1706) because A holds it.
3. A's transaction advances R to S2, still `active`, and commits.
4. B resumes. `locked.status` fine, `reviewGate` fine, `row.state !== 'active'` is evaluated against the stale snapshot (1710) and passes. `setInstanceSupplementState(from:'active' → 'awaiting_supplement')` (1739-1745) succeeds, because R really is active — just at S2 now.
   Outcome: R is paused in `awaiting_supplement` by B, who holds nothing at S2. Per §32.70 `mayRead` (lines 774-783) admits only the open ask's requester while a round waits, so C — the actual judge at S2 — gets `ASSESSMENT_REVIEW_NOT_FOUND` and the claim vanishes from the college's queue. The trail event written at 1756-1764 carries `route/stageId` of S1, a step the round has left. B cannot undo it either: `cancelSupplement` runs `requireJudge` on the round as it now stands (line 1798), `mayAct` is false at S2, and B is neither subject nor roster admin → `ReviewNotFound`. The round only unsticks if the student answers a supplement request nobody with standing asked for.

- 证据:review/service.ts:1703 reads the round, 1705 authorizes from it, 1706 takes the batch lock only afterwards — the inverse of the order a124973b installed in decideReview (review/service.ts:952-969, where `located` is used for nothing but `batchId` and the row is re-read under the lock before `mayAct`). The state flip at 1739-1745 calls `setInstanceSupplementState` (review/db.ts:1555-1572), whose WHERE clause is `tenantId`, `id`, `state` only — note that 773b2e83 added exactly the missing `at: {route, stageId}` predicate to the neighbouring `setInstanceState` (review/db.ts:929-958) for the patrol, and did not give it to this one. cancelSupplement (1790) and answerSupplement (1934) read before their locks too, but both are protected by compare-and-set on `closeSupplementRequest`/state; requestSupplement is the one where a stale read carries an authorization decision.
- 最小修法:Re-read the instance after `lockBatch` and run `requireJudge` plus the state test against that row, as decideReview now does; or add the `at: {route, stageId}` predicate to `setInstanceSupplementState` so the pause fails when the round has moved.
- **反驳者校正**:The finding stands; two details need correcting. (1) "C gets ASSESSMENT_REVIEW_NOT_FOUND" is true only for a C without administrative reach: getReviewInstance falls through to `deps.rosterReach` (service.ts:912-915), so a roster admin can still read the round — but cannot decide (decideReview refuses with 'awaiting-supplement' at service.ts:985-987) and cannot cancel someone else's ask (the requester gate at service.ts:1795-1797 precedes everything), so the wedge is unaffected. (2) "The round only unsticks if the student answers" is one exit short: the entry's subject can also withdraw, which cancels the round — `cancelReviewInstance` deliberately accepts awaiting_supplement (entry/db.ts:685-712, called at entry/service.ts:1486). No staff path exists. Also worth stating precisely: the stale `row.state !== 'active'` test at :1710 is not itself the defect — a fresh read would also say 'active' at S2 — the defect is the stale authorization at :1705 combined with `setInstanceSupplementState` lacking the `at: {route, stageId}` predicate, exactly as the proposed fix says.
- **反驳者校正**:The finding stands, with three scope corrections.

(1) The guard is one predicate short, not absent. The existing compare-and-set at review/service.ts:1739-1745 already catches the case where the concurrent decision COMPLETED the round (completeInstance, service.ts:1319, leaves state 'completed', so `from: 'active'` fails). It also catches the case where the next stage arrives unstaffed: service.ts:1367 passes `arrived.state`, and a 'blocked' arrival likewise makes `from: 'active'` fail. The hole is exactly and only the stage-move-while-still-active case, which is the ordinary approve path at service.ts:1338-1382.

(2) The stale read carries both an authorization decision (requireJudge at service.ts:1705, whose SQL binds the snapshot's currentNodeId/currentRoleIds/currentRoute as literals, review/db.ts:300-310) and the recorded step (the trail event's route/stageId at service.ts:1760-1761 names a step the round has left). Both are wrong, and both are fixed by the same re-read.

(3) "The round only unsticks if the student answers" is overstated. Plain withdrawal is indeed refused (`review-under-way`, entry/service.ts:1203), but the subject can abandon/void the entry (entry/service.ts:1176-1189), and an administrator editing the item's review chain reroutes open rounds including awaiting_supplement ones (item/db.ts:643 selects state in ('active','blocked','awaiting_supplement'); item/service.ts:561-619 supersedes them with a fresh round). Both destroy or replace the round rather than repair it, and neither is discoverable from the symptom, so the severity is unaffected.

Eith…

- **反驳者校正**:The claim stands as a real, unhandled defect; one sentence needs narrowing.

Accurate as written: `requestSupplement` reads the round at packages/plugins/assessment/core/src/review/service.ts:1703 and authorizes from that snapshot at :1705 before taking `lockBatch` at :1706 (server/db.ts:45-54, a genuine `forUpdate()`); `instanceOf` is an unlocked plain SELECT (review/db.ts:406); `mayAct`/`userMayReview` decide solely from the snapshot's `currentNodeId`/`currentRoleIds`/`currentRoute` (service.ts:745-758, review/db.ts:282-312); `advanceReviewInstance` leaves the round `state='active'` at the next stage (entry/db.ts:645-678), so both the stale `row.state !== 'active'` test (:1710) and `setInstanceSupplementState`'s CAS (:1739-1745; review/db.ts:1550-1572, WHERE = tenantId/id/state only) pass; the trail event at :1756-1764 records the abandoned stage's route/stageId; `mayRead` (:774-783) then hides the round from everyone but the requester; and `cancelSupplement`'s `requireJudge` (:1798) refuses B with `ReviewNotFound` (requireJudge:1679). This is the inverse of the order a124973b installed in `decideReview` (service.ts:952-969), and 773b2e83 gave the `at: {route, stageId}` predicate to `setInstanceState` (review/db.ts:929-958) but not to `setInstanceSupplementState`.

Correction to "The round only unsticks if the student answers": two other exits exist, neither of them a way to undo the wrong ask. (a) The participant can abandon the claim — `cancelReviewInstance` deliberately covers `awaiting_supplement` (entry/db.ts:680-700 and its doc comment). (b) An administrator editing…

### [major · org-tree] "Whose batch is this" is answered by frozen paths on the read side and live node ids on the write side; one org move flips administration

- 位置:packages/plugins/assessment/core/src/server/index.ts:1582
- 判定:CONFIRMED,3/3 upheld(第三轮)
- 主张:batchVisibleTo/withinReach decides batch ownership by matching the participant's FROZEN batch_participants.anchor_path against the LIVE path of the grant's anchor node, while requireRosterReach decides the same question by calling rbac.requireAt on the anchor NODE ID, which rbac resolves entirely against live paths. Moving any org node rewrites the live paths and makes the two disagree, so the read guard and the write guard name different administrators for the same running batch.
- 失败场景:Tree: 校 R → 学院 A → 班 C. A batch is created by importing class C: batch_management_anchors = {C}, and participant P is frozen with assessment_anchor_node_id = C, anchor_path = 'R.A.C'. Administrator M holds assessment.batch.manage through an org role granted at A with coverage='subtree'; administrator N holds the same at college B. The registrar then transfers class C to college B via PUT /api/org/nodes/{C}/placement. org's moveSubtree (packages/plugins/base/org/src/server/db.ts:404-424) rewrites C's live path to 'R.B.C'; batch_participants.anchor_path stays 'R.A.C' (it is only rewritten in the re-admission branch, server/db.ts:1491 guarded by status <> 'active' at 1495, and the auto-sync column was deliberately dropped in db/migrations/20260811200227_drop-anchor-auto-sync.sql). Now: (a) M still SEES the batch, because withinReach evaluates 'R.A.C'::ltree <@ (live path of A) = 'R.A' → true, but every write is refused, because requireRosterReach calls rbac.requireAt(M, assessment.batch.manage, C) and canAt compares live 'R.B.C' <@ live 'R.A' → false. M is locked out of phase advance, roster edits, item configuration and the access list of their own running batch, mid-semester, with no error that names the cause. (b) N, who administers a college that has nothing to do with this round, now passes requireRosterReach for every anchor (live 'R.B.C' <@ live 'R.B' → true) and can advance its phases, add and exclude participants, edit its questions and accept staff assignments into it — while withinReach still says the batch is not theirs. Recovery is impossible through the product:…
- 证据:packages/plugins/assessment/core/src/server/db.ts:153-194 — withinReach joins live org_nodes for management anchors (line 160: `join org_nodes mn ... mn.id = ma.org_node_id`) but feeds scopeCoverage the frozen column for participants (line 177: `path: sql.ref('bp.anchor_path')`). packages/plugins/assessment/core/src/server/index.ts:1566-1583 — requireRosterReach reads managementAnchors + rosterAnchors (both return node IDs) and loops `yield* rbac.requireAt(as, MANAGE, nodeId)` at 1582; the frozen path never enters this decision. packages/plugins/base/rbac/src/server/authorization.ts:181-226 — canAt inner-joins OrgNode as target and left-joins OrgNode as anchor and passes `{ target: eb.ref('target.path'), anchor: eb.ref('anchor.path') }` to reaches() at 213-221: both sides live. packages/plugins/base/org/src/server/db.ts:404-424 — moveSubtree rewrites path for every row with `path <@ oldPath`. Verified in the running database that the containment actually breaks: `select 'r.a.c'::ltree <@ 'r.u.a'::ltree` → f. packages/plugins/base/org/src/coverage.ts:1-14 already names this as the drift risk ("the fifth place the coverage rule could have been written down"), and no test in the repo moves an org node with a live batch: `rg -l 'moveNode|setNodePlacement'` matches only org's own api.ts, server/index.ts, OrgPage.tsx and org's two tests.
- 最小修法:Make both guards ask the same question in one place. The cheapest consistent reading is to resolve the frozen anchor NODE IDs to their live paths at query time (join org_nodes on batch_participants.assessment_anchor_node_id and pass n.path to scopeCoverage instead of bp.anchor_path), which makes withinReach agree with canAt/requireRosterReach — the frozen node id already carries the "who was admitted" fact, and anchor_path is a cache of an ancestry that org owns. If the product genuinely wants a batch to stay with the unit it was created under after a restructure (design §32.7), then requireRosterReach must be expressed against the frozen path too, and the frozen lineage must be resynced or…
- **反驳者校正**:Corrected statement: batch administration is decided twice against different data. requireRosterReach (packages/plugins/assessment/core/src/server/index.ts:1564-1583) resolves the batch's management anchors and roster anchors to node IDs and calls rbac.requireAt per id, which canAt (packages/plugins/base/rbac/src/server/authorization.ts:181-226) answers entirely against LIVE org_nodes paths. withinReach (packages/plugins/assessment/core/src/server/db.ts:151-194) answers the same question with a mixture: live mn.path for management anchors (db.ts:159-164) but the FROZEN bp.anchor_path for participants (db.ts:177). Since org's moveSubtree (packages/plugins/base/org/src/server/db.ts:404-424) rewrites live paths and nothing resyncs batch_participants.anchor_path for an active row (assessment db.ts:1483-1495; auto-sync dropped in db/migrations/20260811200227), one PUT /api/org/nodes/{id}/placement makes the two guards disagree.

The two real divergences, both requiring the corrected setup:

- Write-without-read. Tree 校 R → 学院 A → 班 C; batch created by importing class C, so management anchors = {C} and participants are frozen at C with anchor_path 'R.A.C'. Move C under college B. Administrator N, scoped subtree at B, now passes requireRosterReach for every anchor (live 'R.B.C' <@ 'R.B') and can advance phases, add/exclude participants, edit items and manage the access list — while withinReach denies them (frozen 'R.A.C' is not under 'R.B'), so the batch never appears in their list and readDetail refuses via requireBatchVisible (index.ts:1546-1553, 2218). A batch mutable by someone…
- **反驳者校正**:The finding is real, but the fixture for its first half is wrong: `withinReach`'s management-anchor half is live (db.ts:159-166 joins `org_nodes mn` and uses `mn.path`), so only the PARTICIPANT half is frozen. The two reachable outcomes are therefore:

(a) Lock-out — requires a management anchor that did not move. Tree 校 R → 学院 A → 班 C; the batch is created by importing college A, so `batch_management_anchors = {A}` while participants are frozen at their class with `assessment_anchor_node_id = C`, `anchor_path = 'R.A.C'`. M holds `assessment.batch.manage` at A with coverage='subtree'. The registrar moves C under college B (PUT /api/org/nodes/{C}/placement → moveSubtree, packages/plugins/base/org/src/server/db.ts:404-424). Now `withinReach` still says yes for M — anchor A live 'R.A' ⊆ 'R.A', participants frozen 'R.A.C' ⊆ 'R.A' — so the batch stays listed and `manageable` is projected true (db.ts:126, db.ts:410), while every write dies in `requireRosterReach` because `rbac.requireAt(M, MANAGE, C)` compares live 'R.B.C' against 'R.A' (index.ts:1582; authorization.ts:213-221). M is offered controls that always refuse.

(b) Blind write authority — this is the auditor's fixture, and it holds. `batch_management_anchors = {C}`, participants frozen 'R.A.C'; after the move N (subtree grant at B) fails `withinReach` (frozen 'R.A.C' ⊄ 'R.B') and therefore fails `requireBatchVisible`/`getBatch`, yet passes `requireRosterReach` for every anchor (live 'R.B.C' ⊆ 'R.B') and can drive `updateBatch` (index.ts:2239), `replacePlan` (index.ts:3041), the roster writes behind `rosterWriteGuards` (…

- **反驳者校正**:The defect is real, current at HEAD 9f33737e, and unruled — but scenario (a) and the "diff panel" evidence are wrong, and the corrected picture is stranger than the claim.

**What is confirmed.** Three different answers to "whose batch is this" coexist. `withinReach` (db.ts:153-194) tests management anchors against the **live** path (line 160 joins `org_nodes mn`, line 168 passes `mn.path`) and participants against the **frozen** `bp.anchor_path` (line 177). `requireRosterReach` (index.ts:1581-1583) loops both anchor sets as bare node IDs through `rbac.requireAt` → `canAt` (rbac/src/server/index.ts:413-418; authorization.ts:213-221), which is **live on both sides**. `anchor_path` is resynced only on re-admission (db.ts:1491, guarded `status <> 'active'` at 1495), the auto-sync switch was dropped (db/migrations/20260811200227_drop-anchor-auto-sync.sql), and org's `moveSubtree` (org/server/db.ts:404-424) rewrites every live path under the moved node. So an org move does split the read guard from the write guard.

**Correction 1 — scenario (a) does not happen as described.** The claim stipulates `batch_management_anchors = {C}`, and management anchors are always written at creation from the import's nodes (index.ts:1993). `withinReach`'s _first_ clause tests those against the live path. After C moves to B, C's live path is `R.B.C`, so M fails that clause and loses **read as well as write** — M is not "sees the batch but every write is refused", M is fully locked out. The read-without-write split M is claimed to suffer requires a batch with zero management anchors and a non-emp…

### [major · phase-engine] Saving the phase plan silently clears every phase's item and participant allowance, widening the gate

- 位置:packages/plugins/assessment/core/src/client/PhaseTimelineEditor.tsx:155
- 判定:CONFIRMED,3/3 upheld(第三轮)
- 主张:getPhases returns each phase's itemScope and participantScope, but the editor's draft model drops them and the save payload omits them; the server reads an absent scope as an empty one and replaces the stored rows with nothing. Any unrelated plan edit therefore erases the supplementary-phase allowances, and the gate stops narrowing.
- 失败场景:A supplementary phase S is configured (via PUT .../phases, the only surface that accepts it) with itemScope=[item X] so that during S participants may only create/edit/submit entries for X. S becomes the phase in effect. Later an administrator opens the stage editor and renames a future stage, then presses save. PhaseTimelineEditor.tsx:155-162 sends {id, phaseKey, displayName, description, entryNote, permissionProfile} for every row — no itemScope, no participantScope. In writePlanOrder (index.ts:1175-1195) normalScope(undefined) is [], which differs from existing.itemScope, so scopesChanged is true and replacePhaseScopes writes the empty set. scopeRefusals (index.ts:1292-1300) only refuses a scope change on an _ended_ phase, so the phase in effect is rewritten without complaint. gateAllows (phase/gate.ts:59-70) then sees itemScope.size === 0 and skips the item check entirely, so every participant may now file and submit against every item of the round during a phase that was opened for one item only. Nothing is reported to the administrator and the change is not even recorded as a scope-changed event for the untouched phases beyond the generic one.
- 证据:api.ts:175-189 — phaseView returns itemScope and participantScope, so the client reads them. client/phase/model.ts:17-34 — PhaseDraft and draftOf omit both fields, so the round trip loses them. PhaseTimelineEditor.tsx:150-165 — savePlan sends exactly the draft fields. index.ts:1175-1195 — writePlanOrder recomputes both scopes from the spec and calls replacePhaseScopes when they differ. index.ts:441 normalScope treats undefined as []. index.ts:1292-1300 — the only protection is 'ended-phase-name-only', which does not cover the current or future phases. phase/gate.ts:59-70 — an empty scope means unrestricted. Grepping the whole client for itemScope/participantScope returns nothing, so nothing anywhere puts them back.
- 最小修法:Carry itemScope/participantScope through PhaseDraft and the save payload so the write is a faithful read-modify-write, or make the server treat an absent (as opposed to empty) scope array as 'leave unchanged' — the two fields are the only ones in phaseSpec whose omission destroys state.
- **反驳者校正**:The mechanism holds: PhaseTimelineEditor's save is a lossy read-modify-write that clears every phase's itemScope/participantScope, and on an active batch that silently widens the gate for the phase in effect and every future phase. Four corrections/refinements:

1. "Not even recorded as a scope-changed event" is wrong. On the active path `writePlanOrder` is called with `events: true` (server/index.ts:2989-2992), and lines 1206-1213 insert a `scope-changed` phase event for each phase whose scopes changed. `recordConfigChange` also lists those phase ids as edited, because `editedIds` includes `scopesChanged` (server/index.ts:2996-3019). So there is a per-phase audit record and a config-revision bump; what is missing is any signal to the administrator in the response or the UI — the editor treats the write as a plain success (PhaseTimelineEditor.tsx:167-172).

2. The wipe leaves no trace only on the draft path, where `writePlanOrder` is called with `events: false` (server/index.ts:2901-2904). A draft has no phase in effect, so there the loss is configuration damage, not gate widening.

3. Second symptom the audit did not name: if the scoped phase has already ended, the same unrelated save is not silent — it hard-fails the whole plan with `ended-phase-name-only` (server/index.ts:1292-1300), a refusal the administrator cannot act on because the UI has no scope control to restore. So the bug presents as either a silent widening (current/future phase) or an inexplicable blanket refusal (ended phase).

4. Second site with the same root cause: the template-application path rebuilds…

- **反驳者校正**:The claim is real but mis-stated in three respects.

1. Wrong file. All the server citations ("index.ts:1175-1195", ":441", ":1292-1300") are in packages/plugins/assessment/core/src/server/index.ts, not src/index.ts, which is only 412 lines. The line numbers themselves are right for server/index.ts.

2. The event assertion is wrong for the path described. On the active-batch path writePlanOrder is called with events: true (server/index.ts:2990), so the wipe DOES insert a per-phase 'scope-changed' event (server/index.ts:1206-1211) and the phase id lands in editedIds, producing a recordConfigChange entry (server/index.ts:2996-3010). The silent, unrecorded cases are the draft path (events: false, server/index.ts:2902) and the template-apply path (events: false, server/index.ts:2867). So the accurate statement is: it is silent to the administrator in the UI in all cases, and leaves no audit trail at all on the draft and template paths.

3. Missed a second, purely server-side instance of the same read-modify-write loss that needs no client involvement. In the fromTemplateId branch, `kept` is rebuilt from existing rows as {id, phaseKey, displayName, description, permissionProfile} (server/index.ts:2859-2865) — no itemScope, no participantScope, and no entryNote either. Passing that to writePlanOrder therefore wipes every existing phase's scopes AND resets entryNote to '' (server/index.ts:1183, `entryNote: spec.entryNote ?? ''`) for every pre-existing phase, merely because an administrator appended a timeline template. This path is not covered by the proposed client-side fix; only…

- **反驳者校正**:The defect is real and unhandled at HEAD 9f33737e — no test, no ADR, no §30/§32 ruling, no docs/notes entry, and none of today's twelve fixes touched the code — but the write-up is wrong in two places and understates the blast radius in a third.

1. The loss IS recorded, on the plan-save path. `writePlanOrder` inserts a `scope-changed` phase event whenever the scopes differ and events are on (packages/plugins/assessment/core/src/server/index.ts:1204-1212), and the phase lands in the config-change's `edited` set (index.ts:2997-3011). The claim's "not even recorded as a scope-changed event" is false here. What is silent is the administrator's screen, not the ledger.

2. Severity is over-stated as "major" for the product as it ships. No client surface writes a scope: grepping packages/plugins and apps/web for `itemScope`/`participantScope` yields only api.ts:185-186 (read) and 207-208 (optional write), server/index.ts, phase/gate.ts, and test fixtures. Phase templates are refused a scope outright (`scope-in-template`, index.ts:1252-1257). So the state destroyed can only have been created by a direct PUT /assessment/batches/{batchId}/phases. The wipe is a genuine API-contract defect and a live trap for the moment the editor grows a scope control, but no admin can reach it through the UI today in either direction.

3. There is a second, unmentioned instance that is strictly worse. The `fromTemplateId` branch rebuilds the retained rows as `{id, phaseKey, displayName, description, permissionProfile}` (index.ts:2855-2865) — dropping the two scopes AND `entryNote` — and calls writeP…

### [major · phase-engine] Appending a timeline template rewrites the retained phases from a lossy projection, wiping their entry notes and allowances

- 位置:packages/plugins/assessment/core/src/server/index.ts:2859
- 判定:CONFIRMED,3/3 upheld(第三轮)
- 主张:The template branch is documented as adding the template's phases to the end of the plan, but it re-submits every existing phase through writePlanOrder using a five-field projection that omits entryNote, itemScope and participantScope. Those three are then overwritten with empty values on rows the operation was not supposed to touch.
- 失败场景:A draft round has phases configured by hand: stage 'Supplement' carries entryNote='waiting on the college's approval' and participantScope=[the 12 students who missed the window]. The administrator presses the template button in the stage editor (PhaseTimelineEditor.tsx:336 -> addFromTemplate at 176-183, payload {fromTemplateId}) to append a standard tail of stages. replacePlan takes the template branch: kept (index.ts:2859-2865) maps each existing row to {id, phaseKey, displayName, description, permissionProfile} only, and writePlanOrder then writes entryNote: spec.entryNote ?? '' (index.ts:1184) and compares normalScope(undefined)=[] against the stored participant scope (1187-1195), replacing it with the empty set. After a pure append the administrator's entry notes are blank and the supplementary phase now admits the entire roster rather than the twelve people it named — with no refusal, no warning and (events:false on this path) no phase event recording the change.
- 证据:index.ts:2851-2872 — the branch's own comment says the template 'adds its phases to the end of the plan', yet kept re-specifies the existing rows with five fields. index.ts:1177-1195 — writePlanOrder unconditionally writes entryNote and replaces the scopes when they differ from the spec. index.ts:441 — normalScope(undefined) is []. Draft plans can legitimately carry both: the draft branch validates and writes scopes (index.ts:2885-2904 with scopeRefusals at 1267-1303), and entryNote is a first-class spec field (api.ts:204-205). The path is reachable from the UI, not only the API (PhaseTimelineEditor.tsx:176-193, 336, 400, 543-548).
- 最小修法:Build kept from the full PlanPhase row — include entryNote, itemScope and participantScope — or skip retained rows entirely on this path and only insert the template's phases at ordinals after the existing tail.
- **反驳者校正**:Confirmed with two narrowings and one widening.

Confirmed core: the template branch of `replacePlan` (packages/plugins/assessment/core/src/server/index.ts:2834-2873) appends without deleting, but re-submits every retained row through `writePlanOrder` as a five-field projection (index.ts:2859-2865) that omits `entryNote`, `itemScope` and `participantScope`. `writePlanOrder` then writes `entryNote: spec.entryNote ?? ''` through `updatePhaseFields`, which spreads any defined value including `''` (index.ts:1184, db.ts:755-771), and — because `normalScope(undefined)` is `[]` (index.ts:441) — sees `scopesChanged` and calls `replacePhaseScopes` with two empty arrays, which DELETEs both scope tables for the phase and re-inserts nothing (index.ts:1187-1195, db.ts:1277-1315). The transaction commits (packages/plugins/infra/database/src/server/orm.ts:104-119). An emptied participant/item scope is "unrestricted" to the gate (packages/plugins/assessment/core/src/phase/gate.ts:59-70), so once the round activates the phase admits everyone.

Narrowing 1 — reachability differs per field. The trigger (the template button, PhaseTimelineEditor.tsx:176-183 and the dialog at ~543-548) is UI-only, and `entryNote` is UI-editable and UI-saved (phase/PhaseDetailsPanel.tsx:89-97; PhaseTimelineEditor.tsx:150-163), so the entry-note wipe is a pure-UI data loss. `itemScope`/`participantScope`, however, cannot be set from any client file — the editor's save payload omits them — so the scope wipe presupposes the scopes were written through the API (they are valid on a draft: the roster exists from `creat…

- **反驳者校正**:Appending a timeline template re-submits every retained phase through writePlanOrder using a five-field projection (index.ts:2859-2865) that omits entryNote, itemScope and participantScope, so writePlanOrder blanks entryNote (1184) and replaces both scopes with the empty set (1187-1195) on rows a pure append should not touch, with events:false suppressing any record.

The entryNote loss is reachable end to end through the UI: a draft round is fully editable (readOnly is only `archived`, PhaseTimelineEditor.tsx:131), entryNote is an editable field on each stage (PhaseDetailsPanel.tsx:88-99) persisted by savePlan (PhaseTimelineEditor.tsx:160), and the template button (336 -> 176-183) then blanks every saved note with no warning.

The scope loss is real but not reachable from the stage editor as the claim narrates: no client file sets itemScope or participantScope, and savePlan's own payload omits them, so allowances can only be created by a direct API call or inherited from the active branch before a round reverts to draft (index.ts:3081-3089). Consequently the same wipe already happens on an ordinary UI plan save, and the fix should cover both callers: carry entryNote/itemScope/participantScope on retained rows (or leave retained rows untouched and insert the template's phases at ordinals past the existing tail), and stop the editor's save payload from silently clearing scopes it never renders.

- **反驳者校正**:The claim stands as written; three refinements sharpen it.

(1) SCOPE OF IMPACT IS DRAFT-ONLY. The template branch refuses non-draft rounds first (index.ts:2836-2840, `template-requires-draft`), so only draft plans can be damaged. This narrows the blast radius but does not dissolve the bug: draft plans legitimately carry all three fields — the draft branch validates and persists scopes (index.ts:2885-2904, scopeRefusals at 1267-1303, which imposes no batch-status or phaseKey condition), the roster exists from batch creation (§32.45), and entryNote is edited from the phase detail panel per §32.51. The claim's failure scenario (a hand-configured draft, then the template button) is exactly the reachable case.

(2) THE RULING IS AGAINST THE CODE, NOT FOR IT. docs/assessment-design.md:967 (§32.41) decides 应用 = 追加到计划末尾（不再整体替换、不碰已有阶段) — "append to the end of the plan (no longer a wholesale replacement, does not touch existing phases)". The claim cites only the inline code comment; the stronger citation is this recorded ruling, which makes the rewrite of retained rows a violation of a frozen decision rather than merely undocumented behavior.

(3) THE OMISSION IS A MISSED CALL SITE, WITH A DATE. `kept` was written 2026-08-11 (c8734c8f); `entryNote: spec.entryNote ?? ''` was added to writePlanOrder 2026-08-12 (d44a00244, index.ts:1184). The entryNote loss is a one-day-old regression from adding a field without updating this projection; the scope loss dates from the branch's own authorship (scope handling landed 2026-08-09, 5256dbb65). Both are accidental. The proposed fix is the righ…

### [major · rbac-internals] Withdrawn grants still count as authority in every rbac administration query

- 位置:packages/plugins/base/rbac/src/server/authorization.ts:796
- 判定:CONFIRMED,3/3 upheld(第三轮)
- 主张:`revokeGrant` withdraws a grant by setting `revoked_at`, and the authorization statements honour it via the `inForce` predicate — but none of the administration/guard queries filter it, so a withdrawn grant keeps blocking legitimate writes and keeps being displayed as live authority.
- 失败场景:A batch administrator staffs batch B: `addStaff` (assessment/core/src/server/index.ts:2720) calls `createScopedAssignment`, which inserts a `role_grants` row for user U with the org-kind role "reviewer" anchored at class1 (that role's `eligibility_mode` is the default `allow-list`, naming only the teacher type). Later they call `removeStaff`, which calls `rbac.revokeAssignment` (index.ts:2763 → rbac/src/server/db.ts:366) and only sets `revoked_at = now()`; the row stays. Authorization correctly stops (the assessment test at tests/effect-assessment.test.ts:1179 asserts `afterRevoke` is empty). Now an IAM administrator changes U's user type from teacher to staff. auth/src/server/users.ts:609 asks `rbac.grantsBlockingUserType`, which at authorization.ts:800-840 filters on tenant, user, `not admitsUserType` and "not the canonical admin" and nothing else — it counts the dead row — so the change is refused with GRANT_INCOMPATIBLE{grantCount:1} citing authority U does not have, permanently until someone hard-deletes the row. The same omission makes `grantRows` (grants.ts:123-192, the /iam/role-grants and /iam/users/{id}/role-grants screens) list the withdrawn grant with `manageable: true` as if it were live, while the `listUserRoles` port for the same user hides it (authorization.ts:327 does filter `inForce`); `grantsBlockingOrgType` (authorization.ts:672-702) likewise refuses an org-type change over a withdrawn grant, `countGrantsOfRole` (roles.ts:149) blocks role deletion with RoleInUse forever, `grantsStrandedByEligibility` (roles.ts:403) blocks narrowing eligibility, and `admi…
- 证据:authorization.ts:107-111 defines `inForce` (`revoked_at is null and valid_from/valid_until in range`) and it is used only by `held` (line 140), `userRoleHoldings` (327), `applicableAssignments` (378) and `ruleAllowsAppointment` (db.ts:218-224). `rg -n "revoked" packages/plugins/base/rbac/` shows no other occurrence: grantRows (grants.ts:141), oneGrant (grants.ts:306), countGrantsOfRole (roles.ts:149), grantsStrandedByEligibility (roles.ts:403), roleProjection's grantCount (db.ts:121), grantsBlockingOrgType (authorization.ts:672), grantsBlockingUserType (authorization.ts:796), administratorSurvivors (authorization.ts:613) and holdsCanonicalAdmin (grants.ts:281) all omit it. The unique indexes at db/entities.ts:311,317 are explicitly partial `where revoked_at is null`, confirming a revoked row is meant to be inert.
- 最小修法:Give the administration queries the same `inForce` (or at least `revoked_at is null`) predicate the authorization queries use, so the one definition of "a grant that is in force" decides both. `grantRows` additionally needs it to stop reporting withdrawn authority as manageable.
- **反驳者校正**:Corrected statement: the rbac administration/guard queries ignore `inForce`, but the only grants that can be in a not-in-force-yet-still-present state are **resource-scoped assignments** — either withdrawn through the `revokeAssignment` port (rbac/src/server/index.ts:395-398 → db.ts:366-378, soft `revoked_at`, sole caller assessment `removeStaff` at assessment/core/src/server/index.ts:2763) or simply **expired** via the `validUntil` accepted by `createScopedAssignment` (grants.ts:343-344). The ordinary IAM revoke hard-deletes the row (`deleteGrant`, grants.ts:351-353, called at grants.ts:778; pinned by effect-rbac.test.ts:1519-1523 and effect-parity.test.ts:514-531), so no `revoked_at` row is left by that path.

Consequently the real, reachable harms are: after `removeStaff` (or after a scoped assignment's `validUntil` passes) the dead row keeps being counted by `grantsBlockingUserType` (authorization.ts:796-844) — refusing a user-type change with GRANT_INCOMPATIBLE until someone hard-deletes the row — and by `grantsBlockingOrgType` (672-702), `countGrantsOfRole` (roles.ts:149, blocking role deletion with RoleInUse), `grantsStrandedByEligibility` (roles.ts:403), `roleProjection.grantCount` (db.ts:107-124), and `grantRows`/`oneGrant` (grants.ts:123-192, 306), which list the dead grant with `manageable: true` while `userRoleHoldings` (authorization.ts:327) correctly hides it.

Two of the auditor's cited sites should be dropped: `administratorSurvivors` (authorization.ts:613-645) and `holdsCanonicalAdmin` (grants.ts:281) both concern the canonical tenant-admin role, which is `…

- **反驳者校正**:The defect is real and reachable, but narrower than stated.

Correct statement: the only writer of `revoked_at` is `revokeGrant` (rbac/src/server/db.ts:366), reachable in production solely through `rbac.revokeAssignment` (rbac/src/server/index.ts:395) from assessment `removeStaff` (assessment/core/src/server/index.ts:2763). Every soft-revoked row is therefore a resource-scoped `assessment/batch` grant of a role whose permissions are a subset of `BATCH_STAFF_CODES` (validated at assessment index.ts:2674-2678). The IAM revoke path is a hard delete (grants.ts:351/777-778 via DELETE /iam/role-grants/:grantId, api.ts:445), so no other grant is ever left in the revoked state.

Against that, the administration queries that omit `inForce` and are actually reachable with a dead row are: `grantsBlockingUserType` (authorization.ts:796) — an IAM user-type change is refused with GRANT_INCOMPATIBLE over a grant that was withdrawn when the batch unstaffed the person; `grantsBlockingOrgType` (authorization.ts:672); `countGrantsOfRole` (roles.ts:149) blocking role deletion with RoleInUse; `grantsStrandedByEligibility` (roles.ts:403) blocking eligibility narrowing; and `grantRows`/`oneGrant` (grants.ts:123-192, 306), which list the withdrawn grant with a live `manageable` while `userRoleHoldings` (authorization.ts:327) correctly hides it — the same grant appears on /iam/role-grants and is absent from the user's roles panel.

Two sites named in the claim are NOT reachable and should be dropped as preventive: `administratorSurvivors` (authorization.ts:613) and `holdsCanonicalAdmin` (grants.ts:…

- **反驳者校正**:The defect is real and unaddressed, but the population of withdrawn rows is narrower than the claim implies, which changes which of the nine listed sites are reachable today.

Only the resource-scoped path soft-revokes. The general IAM revoke — `grants.revoke` at packages/plugins/base/rbac/src/server/grants.ts:755, which calls `deleteGrant` at grants.ts:351-354 — issues a hard `deleteFrom('RoleGrant')`. The only producer of `revoked_at is not null` rows in the repo is `revokeGrant` (db.ts:365-378), reached solely through the `revokeAssignment` port (index.ts:395-399), whose sole production caller is assessment's `removeStaff` (assessment/core/src/server/index.ts:2763) unstaffing a batch-scoped assignment created by `addStaff` (:2720 → `createScopedAssignment`).

Consequently:

- Reachable today: `grantsBlockingUserType` (authorization.ts:796) refusing a retype with GRANT_INCOMPATIBLE, `grantsBlockingOrgType` (authorization.ts:672) refusing an org-type change, `countGrantsOfRole` (roles.ts:149) blocking role deletion with RoleInUse, `grantsStrandedByEligibility` (roles.ts:403) blocking an eligibility narrowing, `roleProjection`'s `grantCount` (db.ts:121) overstating holders, and `grantRows` (grants.ts:123) listing the dead grant with `manageable: true` while `listUserRoles` for the same user hides it (authorization.ts:326 does filter `inForce`). Each is permanent until someone hard-deletes the row.
- Latent only: `administratorSurvivors` (authorization.ts:612) and `holdsCanonicalAdmin` (grants.ts:281). A canonical-administrator grant can only be removed through the deleting p…

### [major · rbac-internals] setPermissions can strip the capability that made an appointment edge legal, and nothing re-checks it

- 位置:packages/plugins/base/rbac/src/server/roles.ts:710
- 判定:CONFIRMED,3/3 upheld(第三轮)
- 主张:The "granter must itself carry the matching grant-manage" invariant that keeps `role_grant_rules` free of latent edges is enforced only when the edge is written; editing the granter role's permission set afterwards is unguarded, and `ruleAllowsAppointment` never re-asks.
- 失败场景:Tenant configures org role "batch-clerk" carrying {iam.grant.manage, iam.grant.read} and, via setRoleGrantableRoles, the edge batch-clerk → reviewer; the edge is accepted because roles.ts:910-913 sees `iam.grant.manage` in the granter's codes. Policy then changes: clerks should no longer appoint anyone, so an administrator with `iam.role.manage` calls setRolePermissions on batch-clerk with codes {iam.grant.read}. setPermissions (roles.ts:710-750) validates the wanted codes against the catalog, the role kind and the author's own authority, re-runs assertComplete and keepsAdministrator — but never looks at `role_grant_rules`, so the batch-clerk → reviewer edge survives with a granter that can administer no grants at all. Bob holds batch-clerk plus an unrelated role "registrar" that carries iam.grant.manage subtree at node X but has no edge to reviewer. Bob calls createRoleGrant to appoint Carol as reviewer at X: `mayAdministerGrantsAt` passes on registrar's grant.manage, and `mayAppointRole` → `ruleAllowsAppointment` (db.ts:189-241) only asks whether Bob holds a live, non-resource-scoped grant of some role that has an edge to reviewer — it never checks that role's permissions — so batch-clerk still supplies the edge. Bob keeps an appointment power the administrator believes they removed, and it is exactly the edge "that only works when its holder happens to hold some other role" that CLAUDE.md's access-model section and errors.ts:181-193 say must not exist.
- 证据:roles.ts:905-914 is the only place the invariant is checked (`granterCodes.has(needed)` → RoleAppointmentInvalid{reason:'granter-capability'}); tests/effect-rbac.test.ts:1711 proves it for the write path only ('latent' case). roles.ts:710-750 setPermissions contains no role_grant_rules read. db.ts:189-241 ruleAllowsAppointment joins RoleGrantRule → RoleGrant → Role and checks only `gr.status = 'active'`, resource-scope, validity and node coverage. Disabling the granter role does invalidate the edge (that status check), so the gap is specific to permission edits.
- 最小修法:After replacing a role's permissions, re-assert the invariant for that role's outgoing edges inside the same tenant-locked transaction — either refuse the edit while edges stand, or drop the edges — or move the check into `ruleAllowsAppointment` so it is decided from live state rather than from write-time state.
- **反驳者校正**:The mechanism is correct as stated; the framing and the proposed fix need adjusting.

(a) It is not a privilege escalation and does not hand anyone new authority. Bob could already appoint Carol the moment before the permission edit; nothing widens. What breaks is that a configuration state the code declares impossible ("granter-capability", errors.ts:181-193) becomes reachable and stays reachable, and an administrator's removal silently fails to have the effect the UI implies. Severity is better read as "invariant not maintained after the fact / removal does not take" than "major authorization hole". Aggravating factor: RoleEditor.tsx:396-434 hides the standing edge list once the permission is gone, so the residue is invisible where it would be inspected.

(b) The claim's third fix option — "move the check into `ruleAllowsAppointment` so it is decided from live state" — contradicts the model this code was deliberately re-ruled into on 2026-08-20 (roles.ts:867-871: an edge is "a persistent grant of appointment authority, not a hint the grant re-litigates against permission sets"; grants.ts:407-415; escalation.ts:74-78). Only the first two options (re-assert the invariant over the role's outgoing edges inside `setPermissions`' tenant-locked transaction — refuse the edit, or drop the edges and say so) are consistent with the settled design.

(c) Two scope notes on the write-time check itself, adjacent to but distinct from the reported gap: `setStatus` re-activating a role (roles.ts:648-679) likewise never re-asserts the edge invariant, and `rolePermissionCodes` (db.ts:155-166…

- **反驳者校正**:Correct statement: the "granter must itself carry the matching grant-manage" invariant (roles.ts:905-914) is enforced only when an appointment edge is written, and setPermissions (roles.ts:710-750) can afterwards strip that capability from the granter role without consulting role_grant_rules, while ruleAllowsAppointment (db.ts:189-241) checks only the granter role's status, resource scope, validity and coverage. The result is a persistent edge whose granter administers no grants - exactly the latent edge errors.ts:185-193 forbids - and it is reachable through the shipped API, since setRolePermissions needs only iam.role.manage (index.ts:830) whereas repairing the graph needs iam.role.appointment.manage (index.ts:870).

Two corrections to the report. First, no privilege escalation occurs: permission removal can only shrink authority, a holder of the stripped role alone is still refused by mayAdministerGrantsAt (grants.ts:400-404), and the only people who keep the appointment power are those who already had it legally before the edit. The defect is a failed removal plus a broken configuration invariant, not a route to new authority, so major overstates it.

Second, the report understates the operational half. RoleEditor.tsx:396-398 gates the appointment section on the locally edited permission list, so unticking iam.grant.manage immediately replaces the grantable-roles editor with the grantableNeedsManage message (RoleEditor.tsx:429-434): the standing edges vanish from the only UI that can clear them, and the permission-save confirm dialog (RoleEditor.tsx:290-302) warns about…

- **反驳者校正**:The claim stands on mechanics and on prior art; three refinements.

1. Scope is wider than stated — reactivation has the same hole. Besides `setPermissions` (roles.ts:710-748), `setStatus`'s activation branch (roles.ts:662-670) calls only `assertComplete` (which per roles.ts:525-540 never reads grant rules) and `assertMayDefineRole`. So a role can also be disabled, stripped, and re-activated with its outgoing edges intact. Any fix belongs at both write sites, or in `ruleAllowsAppointment`.

2. The claim understates the consequence by omitting the UI half: RoleEditor.tsx:396-398 renders the appointment checkbox group only when `permissions.includes(role.kind === 'tenant' ? 'iam.tenant-grant.manage' : 'iam.grant.manage')`. Once the capability is stripped, the standing edges are not merely unguarded — they are invisible in the editor, so the administrator cannot see them, cannot clear them, and `getRoleGrantableRoles` (roles.ts:853-864) still returns them to a query the UI no longer issues for that role. The surviving edge is unreadable off the configuration, which is precisely what the "latent edge" prohibition (errors.ts:185-193) exists to prevent.

3. The framing "an appointment power the administrator believes they removed" is a shade strong: the administrator removed a permission, and CLAUDE.md:92 deliberately separates appointment authority (the WHAT, `role_grant_rules`) from grant administration (the WHERE, `iam.grant.manage`), so an edge surviving a permission edit is not per se contrary to the model. What is definitely violated is the stated write-time-soundness prope…

### [major · rbac-internals] A self-grant is measured on permission codes only, so it can hand its taker appointment authority the graph withheld

- 位置:packages/plugins/base/rbac/src/server/escalation.ts:96
- 判定:CONFIRMED,3/3 upheld(第三轮)
- 主张:`assertNoSelfEscalation` compares the target role's `role_permissions` against the actor's reach and nothing else, but a role also carries its outgoing `role_grant_rules` edges; self-appointing a role whose permission set is a subset therefore collapses the deliberately non-transitive appointment DAG into its transitive closure.
- 失败场景:Configuration: org role P = {iam.grant.manage} with edge P → T; org role T = {iam.grant.manage} with edge T → S; org role S = {assessment.review.process, ...}. All three edges are legal at write time (same kind, no cycle, each granter carries iam.grant.manage). Alice holds P at node X with subtree coverage, so `reachAt(X)` gives her iam.grant.manage at reach 'subtree'. Alice calls createRoleGrant on herself for T at X/subtree: `mayAdministerGrantsAt` passes; `mayAppointRole` passes on the P → T edge; `eligible` passes; and because `actor.userId === input.userId`, grants.ts:684-690 runs `assertNoSelfEscalation`, whose org-node branch (escalation.ts:96-105) filters `role.codes` = ['iam.grant.manage'] against her reach — she already holds it at 'subtree', so `short` is empty and the grant is written. Alice now holds T, and `ruleAllowsAppointment` will honour the T → S edge for her: she can appoint any eligible person at X to S, a power the graph gave only to T's holders and which she could not exercise a moment earlier (edges are not transitive — db.ts:189-241 matches only direct edges from roles the actor holds). She appoints an accomplice as a reviewer for a live batch. This is a self-grant that added authority, which the 2026-08-20 ruling forbids without exception.
- 证据:escalation.ts:80-106: the only inputs are `role.codes`/`role.allActive` (from `carriedBy`, grants.ts:505-514 → `rolePermissionCodes`) and the actor's reach; `role_grant_rules` is never read. grants.ts:684-690 is the sole call site for a self-grant. db.ts:189-241 confirms appointment is decided per-edge from the roles the actor currently holds, so acquiring T genuinely acquires T's edges. db/entities.ts:212-224 states the appointment edge is authority distinct from permissions, and CLAUDE.md's access-model section states 任命权是角色自己的一部分 alongside 自授绝不得扩权.
- 最小修法:Include the target role's outgoing appointment edges in the self-grant comparison: refuse when the role being taken names an appointment target the actor cannot already appoint (or, equivalently, require the target role's edge set to be a subset of the union of edges from roles the actor already holds).
- **反驳者校正**:The mechanism is exactly as described and the cited lines are on the path; the framing overstates the consequence in one respect.

Accurate: `assertNoSelfEscalation` (escalation.ts:80-106, both the tenant branch at 85-93 and the org-node branch at 96-105) measures a self-grant on the target role's `role_permissions` only. A role's outgoing `role_grant_rules` edges are appointment authority (db/entities.ts:212-224) that `ruleAllowsAppointment` (db.ts:189-241) honours per direct edge from whatever roles the actor holds right now, so an actor whose own role can appoint T, and whose reach already covers T's permission set at the requested coverage, may take T and thereby acquire T's outgoing edges — authority they demonstrably did not have a moment earlier. This is routine rather than exotic in this design: appointment edges require the granter role to carry `iam.grant.manage` (roles.ts:910-913), so chains of "personnel offices" all carrying that one permission and differing only in their edges are the intended shape (fixture at tests/effect-rbac.test.ts:1696-1714), and between any two such offices the permission comparison is a guaranteed no-op.

Overstated: the graph is not thereby collapsed into its transitive closure in terms of _achievable states_. A P-holder could already put a confederate into T (third-party grants compare nothing by design, grants.ts:678-690 and the comment at escalation.ts:20-24), and that confederate could then appoint to S. What the gap actually removes is the second person: the actor personally gains appointment authority the graph assigned to T's h…

- **反驳者校正**:Confirmed, with narrower preconditions than stated.

Accurate core: `assertNoSelfEscalation` compares only permission codes (escalation.ts:98-101; the anchor line 96 is the `reachAt` lookup, the comparison is 98-101), so a self-grant can hand its taker the target role's outgoing `role_grant_rules` edges, which `ruleAllowsAppointment` (db.ts:189-241) then honours per-edge. The tenant branch (escalation.ts:85-93) has the identical gap for tenant-kind chains.

Corrections:

- The captured role is not arbitrary. It must be one the actor may _already_ appoint: `mayAppointRole` (grants.ts:417-431, 678) still demands an existing edge from a role she holds, standing over the anchor node, and `eligible` (grants.ts:458-502) still applies. So the exposure is precisely "promote yourself into an appointing office you were already permitted to staff", and only when that office carries no permission beyond your reach at the requested coverage.
- Because roles.ts:910-913 forces every granter role to carry `iam.grant.manage`, the intermediate office in any chain shares that code with its appointer by construction; the gap therefore opens by default for the pure-personnel pattern the design recommends (STATUS.md:6613-6615) and closes only incidentally, when the intermediate office happens to carry an extra business permission.
- Severity is better read as moderate than major: the actor could already install any other eligible person into T (third-party grants compare nothing — grants.ts:678-690 skips the guard when the recipient differs), so the same downstream appointment was already one coo…
- **反驳者校正**:The defect is real as described, with three corrections.

(1) Two call sites, not one. `assertNoSelfEscalation` is called at grants.ts:685 (the write, inside `grantRole`) and at grants.ts:595 (the read-only `options` probe). The probe shares the omission, so the offer list — and the RolePicker that renders `refusal` (packages/plugins/assessment/core/src/client/access/RolePicker.tsx:18-24) — will actively advertise role T as self-grantable with `refusal: null`. The hole is surfaced in the UI, not merely reachable by API.

(2) The same omission exists on the edge-write side, so this is one defect with two faces. roles.ts:915-923 measures only `added.flatMap((target) => target.codes)` against the author via `assertMayDefineRole`; an author drawing P→T must hold T's _permissions_ tenant-wide but needs neither S's permissions nor T's outgoing edges. So a holder of `iam.role.appointment.manage` (enforced at server/index.ts:870) can also assemble the closure at configuration time. A fix that only patches escalation.ts leaves this half open.

(3) Severity is closer to minor/moderate than major, because the resulting state is already reachable without any self-grant. Third-party grants "compare nothing" by the 2026-08-20 ruling, so Alice can already appoint any eligible person to T (edge P→T), and that person can then appoint anyone to S. The self-grant gap therefore converts a two-party path into a unilateral one and defeats the stated non-transitivity of the DAG; it does not create an otherwise-unreachable authority. That is still a genuine violation of "自授绝不得扩权" as CLAUDE.md:92 d…

### [major · scoring-results] Archiving a batch strands open review rounds: the permanent account silently drops the claim and the round can never be closed

- 位置:packages/plugins/assessment/core/src/server/index.ts:2377
- 判定:CONFIRMED,3/3 upheld(第三轮)
- 主张:The archive transition's only gate is "the last phase has been entered"; it never checks that review rounds are terminal, and every review write refuses once the batch is archived, so a claim that was in review at archive time is frozen forever and vanishes from the student's permanent account without even an anchor line.
- 失败场景:A round reaches its last phase with one student's entry still `in_review` (or `blocked`/`awaiting_supplement`). The administrator opens batch settings and confirms 归档 (BatchSettingsForm.tsx:492-499 is a bare confirm dialog with no preflight). setBatchStatus only runs ratifyPending and checks `state.index !== plan.length - 1` (index.ts:2376-2383), then writes status='archived'. From that instant: decideReview (review/service.ts:955), and the other four review mutations (1501, 1707, 1800, 1944) all return BatchReadOnly, so neither the reviewer nor an administrator can approve, reject or void the round — the reviewer's inbox still lists it and every action fails. The entry stays `in_review`, and calc.ts:241 emits no line at all for `in_review` ("draft, in_review, voided: no line"), so getMyResult on the archived batch — which provisional-scoring.test.ts asserts stays readable forever — returns a total with the claim missing and nothing in the ledger to say it ever existed. Meanwhile patrolReviewRounds (index.ts:3580) keeps the round alive indefinitely: tenantsWithOpenRounds (review/db.ts:1003) and openInstances (review/db.ts:881) filter only on `state in ('active','blocked')` with no batch-status predicate, so the patrol goes on flipping the round's state and writing rows into a batch that is supposed to be write-frozen. The only escape is reopen, which creates a whole new phase and a 'reopened' lifecycle event.
- 证据:index.ts:2367-2384 — `if (input.status === 'archived') { if (from !== 'active') ... const { state } = yield* ratifyPending(...); if (state.index !== plan.length - 1) return ... 'last-phase-not-entered'; yield* updateBatchFields(tenantId, batchId, { status: 'archived' }) }` — no query of review_instances anywhere in the branch. Contrast item/service.ts:398, which does consult `openRoundsOfItem` before voiding an item. docs/assessment-design.md §18 rules the gate as "final Publication 已 PUBLISHED + 全部复审轮终态 + 无业务 blocker"; the "rounds terminal" half is independent of publication and is simply absent.
- 最小修法:Refuse the archive transition (a new BatchStatusInvalid refusal, e.g. 'rounds-in-flight') when any review_instance for the batch is in state active/blocked/awaiting_supplement, and give the admin the count so they can finish or void the rounds first. Independently, scope the patrol's queries to non-archived batches so it stops writing into a frozen round.
- **反驳者校正**:setBatchStatus's archive branch (packages/plugins/assessment/core/src/server/index.ts:2367-2392) gates only on "the last phase has actually been entered" and never consults review_instances, so an administrator can archive a batch that still has rounds in active/blocked/awaiting_supplement. Once archived, no path can drive those rounds terminal: all five review mutations refuse with BatchReadOnly (review/service.ts:955, 1501, 1707, 1800, 1944), the entry-side escapes are equally shut (entry/service.ts:723, 878, 966, 1468; item/service.ts:748), and no admin void-round method exists. Separately, the patrol's queries are not batch-scoped (review/db.ts:882-909, 1003-1009 filter only on state) and mayReview deliberately excludes the phase gate (review/db.ts:50-53), so the patrol keeps re-resolving and can keep writing state and panel-seat rows into a batch the write path treats as frozen.

The consequences are much narrower than claimed. The stranded round is not visible-but-dead in the reviewer's inbox: listReviewInbox filters batches through openBatches -> reviewGate -> effectivePhaseIndex, which returns null for any non-active batch (index.ts:1350) and yields 'no-active-phase' (phase/gate.ts:55), so the round disappears from the queue. Nothing is dropped from the student's record: getMyResult is explicitly `mode: 'provisional'` (scoring/service.ts:19, 113), the schema has no Publication/publication_rows entity at all, an in_review entry produced no breakdown line before archiving just as after (calc.ts:241), and the entry still shows with standing in_review in listMyEntries (…

- **反驳者校正**:The gap is real and reachable, but the claim overstates it in three checkable ways; corrected statement:

**What is true.** `setBatchStatus`'s archive branch (packages/plugins/assessment/core/src/server/index.ts:2367-2394) gates only on `from === 'active'` and `state.index === plan.length - 1`; it never consults `review_instances`, and the UI offers a bare confirm dialog with no preflight (client/batch/BatchSettingsForm.tsx:439, 492-499). Nothing terminates rounds on phase entry (advancePhase, index.ts:3105-3167) and the state check-constraint lets a round stay open forever (db/entities.ts:1110). So an administrator can archive a batch with rounds still in active/blocked/awaiting_supplement, and from that instant the round cannot be advanced by anyone: the phase gate is shut (`effectiveIndexOf` returns null for any non-active status, index.ts:1329, feeding `reviewGate` at index.ts:1913) and every review mutation additionally refuses with BatchReadOnly (review/service.ts:955, 1501, 1707, 1800, 1944). There is no void-round path for an administrator at any time — the only mutations are decideReview/appealReview/request-/cancel-/answerSupplement. Independently, the patrol's queries have no batch-status predicate (review/db.ts:1003, 881), so it keeps flipping state, inserting `review_events` and ending panel seats (index.ts:3688-3735) inside a batch docs/assessment-design.md:510 (§18) declares write-frozen. Contrast item/service.ts:398, which does consult `openRoundsOfItem` before voiding an item.

**Correction 1 — the reviewer's inbox does NOT still list it.** `listReviewInbox…

- **反驳者校正**:The core defect is real and uncovered, but three parts of the statement are wrong and the severity is overstated.

REAL AND UNCOVERED: setBatchStatus's archive branch (packages/plugins/assessment/core/src/server/index.ts:2367-2394) gates only on from==='active' and last-phase-entered; it never reads review_instances, and no refusal code for it exists (packages/plugins/assessment/core/src/server/errors.ts:252-266). No test anywhere archives a batch with an open round. After archive, all five review mutations refuse (packages/plugins/assessment/core/src/review/service.ts:955, 1501, 1707, 1800, 1944), the entry stays in_review and therefore contributes no breakdown line (packages/plugins/assessment/core/src/scoring/calc.ts:241) with no pending indicator in the payload (packages/plugins/assessment/core/src/api.ts:815-836), and the patrol keeps flipping the round's state because neither of its queries scopes to batch status (packages/plugins/assessment/core/src/review/db.ts:909 and 1002-1007). The archive UI is a bare ConfirmDialog with no preflight (packages/plugins/assessment/core/src/client/batch/BatchSettingsForm.tsx:491-501). The in-Core precedent for the missing check exists at packages/plugins/assessment/core/src/item/service.ts:398 (openRoundsOfItem, defined at packages/plugins/assessment/core/src/item/db.ts:617).

WRONG — the inbox does not keep listing it: listReviewInbox filters candidate batches through openBatches, which drops any batch whose reviewGate is closed (packages/plugins/assessment/core/src/review/service.ts:506-514, called at 799-800), and an archived bat…

### [major · scoring-results] Breakdown lines are never quantized to two places, and the pages round them independently with JS floats, so the ledger does not add up and the total can be two cents low

- 位置:packages/plugins/assessment/core/src/scoring/calc.ts:322
- 判定:CONFIRMED,3/3 upheld(第三轮)
- 主张:calcParticipant sums raw ten-thousandths with no line-level quantization, and every display site rounds each string separately through Number(x).toFixed(2), so a sub-cent amount makes the printed lines disagree with the printed subtotal and makes the printed total differ from the ruled HALF_AWAY_FROM_ZERO value.
- 失败场景:An administrator configures three `fixed@1` questions in one group at value "0.335" each (decimalString/decimalAmount deliberately admit four decimal places — builtins.ts:26-31, api.ts:352 — and §16 anticipates sub-cent amounts, e.g. the 0.125/83.245 worked examples and `decrement`'s 集体减半). All three are approved. The engine keeps 3350n per line and 10050n for the group, so the wire carries lines "0.335", itemsTotal "1.005", total "1.005". MyResultPage then prints each line via two() as "0.34" (Number('0.335').toFixed(2)), the group row as "1.00" and the grand total as "1.00" (Number('1.005').toFixed(2) === '1.00', verified with node). The student reads three lines of 0.34 above a subtotal of 1.00: the ledger visibly fails to add up, and the ruled answer is 1.02 (each line quantized to 0.34, then exact 2dp arithmetic). MyEntriesPage repeats the same float rounding on the same figures (lines 768, 812, 891, 1388).
- 证据:calc.ts:247-266 folds and accumulates raw scaled integers (`itemsTotal += folded.total`) and returns `formatAmount(total)` unrounded at line 322; formatAmount (builtins.ts:54-63) emits up to four decimals verbatim. MyResultPage.tsx:36-40 — `const two = (value) => { const parsed = Number(value); return Number.isFinite(parsed) ? parsed.toFixed(2) : String(value) }` — applied per line (403), per group (365-371) and to the total (166, 282). A repo-wide grep for rounding in the plugin finds no HALF_AWAY_FROM_ZERO helper anywhere. docs/assessment-design.md §16: "量化点在行级：每个条目/调整行的最终贡献量化到百分位（2dp），之后组内求和、cap、floor、大项、总分全部是 2dp 精确算术 ... 学生从任何层级手加明细，永远严丝合缝", with the rounding mode fixed as HALF_AWAY_FROM_ZERO (toFixed also disagrees with it directly: (1.005).toFixed(2) is "1.00", (2.675).toFixed(2) is "2.67").
- 最小修法:Quantize each BreakdownLine's contribution to 1e-2 in calcParticipant with an explicit HALF_AWAY_FROM_ZERO helper on bigints before it enters itemsTotal, and make every display site print the already-2dp string as-is rather than re-rounding it through Number().
- **反驳者校正**:The defect is real; two details in the write-up should be tightened.

Scope of the site: calc.ts:322 is only where the grand total is formatted. The missing quantization is at every accumulation point inside walk() — calc.ts:211, :266, :273, :276 and :319 — so a fix must quantize each BreakdownLine's contribution (the values written at :207, :257, and the cap/floor adjustment values at :286/:298) before it enters itemsTotal/childrenTotal, not just at the return. Line 322 is on the path but is the last of six, not the cause.

Rounding-mode argument: the claim's aside that "toFixed also disagrees with HALF_AWAY_FROM_ZERO directly" is not quite right about the mode. Number.prototype.toFixed negates first and rounds the magnitude, so on exactly representable halves it _is_ half-away-from-zero — node gives (0.125).toFixed(2) === '0.13', (-0.125).toFixed(2) === '-0.13' and (83.245).toFixed(2) === '83.25', matching the doc's own worked examples. The real disagreement is binary representation, not mode: 1.005 and 2.675 are not exactly representable and sit below their decimal halves, so toFixed rounds them down. That does not weaken the finding — it means the display rounding is unpredictable rather than uniformly wrong — and it strengthens the second half of the proposed fix: the engine should emit an already-2dp string and the pages should print it as-is instead of round-tripping through Number().

Reachability caveat worth recording alongside the severity: with every configured amount, cap and floor at two decimals (the only shape any current fixture or test uses — tests/provisi…

- **反驳者校正**:Confirmed with two corrections to the claim's framing. Correct core: `calcParticipant` never quantizes a BreakdownLine's contribution to 2dp (calc.ts:211/266/273/319, returned via `formatAmount` at calc.ts:322, builtins.ts:54-63), the wire carries up to four decimals unconstrained (api.ts:818/835), and every display site independently re-rounds each string through `Number(x).toFixed(2)` (MyResultPage.tsx:37-40 used at 166/170/201/208/216/281/362/366/369/371/403/442-443/449; MyEntriesPage.tsx:768/812/891/1388), so with any amount carrying more than two decimals the printed lines stop summing to the printed subtotal and the printed total departs from the value the frozen HALF_AWAY_FROM_ZERO line-level rule yields (assessment-design.md §16 / §32.17 line 923, acceptance at lines 698 and 732). Correction 1 — reachability is narrower than stated: `decrement`'s 集体减半 and `range` cannot produce a sub-cent figure today, because the installed catalog is only `[fixed1, sum1, max1, topNSum1]` (builtins.ts:138). The single reachable source is an administrator typing three or four decimals into the free-text fixed-value field (ItemConfigEditor.tsx:1168-1169, validated only for emptiness at 872) or into a group cap/floor (GroupEditor.tsx:219, `decimalAmount` at api.ts:352/481-482) — permitted by the schema, which documents four places on purpose, and blocked by nothing. With every amount configured at two decimals the figures are exact multiples of 0.01 and the display coincidentally agrees, which is why this has not been noticed. Correction 2 — the negative-boundary sub-argument is wrong:…
- **反驳者校正**:The defect is confirmed as described; three amendments to the statement.

(1) `decrement`'s 集体减半 is not a live trigger. Only fixed@1, sum@1, max@1 and top-n-sum@1 are registered (builtins.ts:138 `builtinScoringDrivers`); lookup/range/decrement are M4 calculators that have not landed (docs/assessment-design.md:468, :731). The reachable trigger today is narrower but entirely real: an administrator typing a 3- or 4-decimal value into a `fixed@1` config, or into a score group's `cap`/`floor` — all three go through the same 4dp-admitting parser (builtins.ts:26-31, api.ts:352, item/service.ts:1037), and builtins.ts:22-25 documents the four places as deliberate.

(2) Milestone framing, which bears on severity rather than validity: the design schedules 完整精度包与属性测试 as M4 scope (docs/assessment-design.md:731), and the codebase is mid-M4 — nested group trees, cap/floor and top-n-sum@1 have landed while the precision package has not. So this is a ruled-but-unbuilt item on a page already serving students provisional scores, not a regression against previously working code. §32.17 was decided 2026-08-09 and no deviation was ever recorded, so nothing licenses shipping the student-facing ledger without it.

(3) The claim lists MyResultPage.tsx and MyEntriesPage.tsx but misses a third display site with an identical helper: Paper.tsx:153-155, `const two = (value) => { const parsed = Number(value); return Number.isFinite(parsed) ? parsed.toFixed(2) : String(value) }`. A fix that quantizes in calcParticipant must also cover the cap/floor adjustment lines (calc.ts:286, 298) and this third page,…

### [major · tenancy] The review patrol's staffing cache is keyed without the batch, so one batch's unaccepted roles permanently freeze another's rounds

- 位置:packages/plugins/assessment/core/src/server/index.ts:3591
- 判定:CONFIRMED,3/3 upheld(第四轮)
- 主张:patrolReviewRounds memoizes reviewer-count answers under `${nodeId}:${roleIds}` while calling a batch-dependent query with `round.batchId`, so rounds from different batches of the same tenant share one answer; a false zero blocks a perfectly staffed round and never releases it.
- 失败场景:Tenant T runs batch A (autumn, live) and batch B (a make-up round just created, whose staff access sources have not yet been accepted through the AccessSyncDialog). Both have open rounds standing at class node N with role set {monitor R}, and Li holds R at N. `openInstances(tenantId)` (review/db.ts:882-910) returns rounds from BOTH batches with no ORDER BY. If B's round is scanned first: `reviewersAt({batchId: B, nodeId: N, roleIds: [R], subject: NOBODY, actor: NOBODY})` returns [] because `mayReview` requires `bas.batch_id = B` (review/db.ts:65) — B accepted nothing — so `staffing.set('N:R', 0)`. A's round then hits the cache at line 3592, gets `members === 0`, short-circuits to `eligible = []` (line 3618) without ever asking with batchId=A, and the round is written `blocked` with reason `no-assignee` plus an `assignee-not-found` event (index.ts:3687-3711). Li's round now vanishes from her queue — `inboxPage` filters `.where('ri.state','=','active')` (review/db.ts:623) — and if she reaches it by direct link a terminal approval fails forever, because `completeInstance` is a CAS on `state='active'` (review/db.ts:691) and returns ReviewConflict. The release branch cannot recover it: `actionable = eligible.length > 0` is false on every subsequent tick for exactly the same cached reason, so `round.state === 'blocked' && actionable` never fires. reviewAlerts additionally tells the administrator to appoint someone who is already appointed.
- 证据:index.ts:3589-3606 builds `const staffing = new Map<string, number>()` inside the per-tenant loop and keys it `const key = `${round.currentNodeId}:${[...round.currentRoleIds].sort().join(',')}`` while passing `batchId: round.batchId` into `reviewersAt`. reviewersAt is batch-dependent in three independent clauses of `mayReview` (review/db.ts:62-65 `bas.batch_id = ${r.batchId}`, 89-94 `batch_access_denies ... dn.batch_id`, 95-102 `rg.resource_id = ${r.batchId}`). batchId is the ONLY input that varies between rounds and is absent from the key — subjectUserId/actorId are the constant NOBODY on this call, and tenantId is fixed by the enclosing loop. Commit 773b2e83 edited this exact loop (adding the `at: {route, stageId}` narrowing to setInstanceState and openPanelOf) to fix a staleness bug in the same read-then-write pattern, and left the key untouched.
- 最小修法:Include the batch in the memo key: `const key = `${round.batchId}:${round.currentNodeId}:${[...round.currentRoleIds].sort().join(',')}``. Nothing else needs to change; the second, per-round reviewersAt call already passes the right batchId.
- **反驳者校正**:The bug is real but is order-dependent flapping, not a permanent freeze, and only one of the two contamination directions changes a round's state. Corrected statement: patrolReviewRounds memoizes the stage-membership count under `${currentNodeId}:${sorted roleIds}` (packages/plugins/assessment/core/src/server/index.ts:3591) inside a per-tenant loop (3589, opened 3584) while the memoized reviewersAt call passes `batchId: round.batchId` (3594-3606). Because openInstances(tenantId) (review/db.ts:882-909) returns every open round of the tenant across all batches with no ORDER BY, and mayReview is batch-dependent in three clauses (review/db.ts:65, 91, 100), one batch's answer is served to another batch's round. If a round of batch B with zero members at node N / roles R is scanned first — such rounds are durable, since a stage with nobody in it is written blocked on arrival (review/service.ts:1353-1372) and blocked rounds stay in the patrol's open set — then a properly staffed round of batch A at the same N/R hits the cached 0, skips the correct per-round query, short-circuits `eligible` to [] (index.ts:3617-3618) and is written blocked with reason 'no-assignee' plus an assignee-not-found event (3690-3711). It then leaves the reviewer's inbox (inboxPage requires ri.state='active', review/db.ts:623) and its decisions are refused (review/service.ts:1193; completeInstance CASes on state='active', review/db.ts:691). Contrary to the claim, this is not unrecoverable: the map is rebuilt each tick, so a tick that scans the healthy round first stores the non-zero answer and the release b…
- **反驳者校正**:The defect is real and the proposed one-line fix is correct, but two supporting claims are wrong and one is overstated.

1. Overstated: "permanently freeze" / "never releases". Nothing in the code makes the poisoning permanent. openInstances (packages/plugins/assessment/core/src/review/db.ts:882-925) has no ORDER BY, and the memo is rebuilt fresh each sweep (index.ts:3589), so which batch's answer wins is re-decided every tick from an unspecified row order. Physical order is stable enough in practice — and the candidate index idx_review_instances_inbox on (tenant_id, state, current_node_id) (db/entities.ts:1161-1165) would group rows by state then node — that a wrong answer can persist for an unbounded run of ticks; but a tick that happens to visit the staffed round first releases it. Accurate statement: a correctly staffed round can be blocked and stay blocked for an arbitrary number of one-minute sweeps, with no bound the code establishes, rather than "forever by construction".

2. Wrong premise in the scenario: "batch B ... has not yet accepted anything through the AccessSyncDialog" cannot coexist with B having an open round at N. A round stands at N only if B has participants there, and importing a roster auto-accepts the applicable staff assignments as origin 'inherited' (index.ts:2016-2025: acceptAssignments(tenantId, batchId, rbac.listApplicableAssignments({codes: BATCH_STAFF_CODES, nodeIds}), 'inherited', as.userId)). The reachable divergences are instead: (a) the reviewer's assignment was granted after B's import and accepted only in A through the sync dialog (inde…

- **反驳者校正**:The defect is real and correctly located, but two details are overstated or incomplete.

(1) "Permanently freeze ... never releases it" is not established. openInstances (review/db.ts:882-910) has no ORDER BY, so which of the two rounds populates the shared key is scan-order dependent and is not guaranteed stable across ticks — the patrol rewrites the round it blocks, which moves the tuple. The correct statement is that whichever round of the colliding pair the sweep reaches first dictates the answer for the other on that tick, so a wrongly blocked round stays blocked for as long as the order persists and heals only by accident of ordering. Sticky and non-deterministic, not provably permanent.

(2) The claim only describes the false-zero direction. The false-non-zero direction is also wrong: when a well-staffed batch's round is scanned first, a round of an unaccepted batch skips the members===0 branch, its per-round reviewersAt (correctly passing its own batchId at index.ts:3619-3627) returns [], and it is still blocked — but with reason 'no-independent-reviewer' (index.ts:3630) or, inside a constituted panel, 'panel-seat-unfilled' (index.ts:3673). Per docs/assessment-design.md line 1335 the administrator alert panel splits rows by blocked_reason, so this mislabels a genuine staffing gap as a recusal conflict and points the administrator at the wrong remedy.

The proposed fix (prefixing round.batchId onto the key at index.ts:3591) addresses both directions, and the second reviewersAt call already passes the right batchId.

### [major · tenancy] The supplement-ownership rule (§32.70) is enforced on the round view but not on the attachment door or the entry history, so a sidelined colleague still reads the evidence

- 位置:packages/plugins/assessment/core/src/attachment/service.ts:118
- 判定:CONFIRMED,3/3 upheld(第四轮)
- 主张:`mayRead` narrows `awaiting_supplement` to the reviewer who asked, and 773b2e83 just folded the same narrowing into the activity feed's openDoor; but the attachment authorizer (userMayReview) and getEntryHistory (mayReviewEntry) are still bare `mayActOn`, so a same-stage peer who has been refused the round can still download its cited files and read its full history.
- 失败场景:Round R sits at the class stage, whose shared pool holds Alice and Bob (review-access.test.ts:145-148 asserts both hold all three doors while R is active). Alice calls requestSupplement; R.state becomes 'awaiting_supplement' with an open request whose requested_by is Alice. Per §32.70 Bob no longer holds the task: `getReviewInstance(R, Bob)` now returns ASSESSMENT_REVIEW_NOT_FOUND, because `mayRead` (review/service.ts:774-783) checks `openAskRequesterOf(...) !== as.userId` before consulting mayAct. Bob then calls `GET /assessment/attachments/{fileId}/content` for the certificate the student filed. `citingInstances` returns R — `awaiting_supplement` is inside OPEN_REVIEW_STATES (review/db.ts:275) and the query admits it at attachment/db.ts:126 — and `userMayReview` (review/db.ts:294-313) is nothing but `mayActOn`, with no requested_by clause, so it answers true and Bob receives the bytes. The same call as `getEntryHistory(entry, Bob)` succeeds too: entry/service.ts:1357 gates on `deps.mayReviewEntry`, which is `ri.state in OPEN_REVIEW_STATES` AND `mayActOn` with no ownership term (review/db.ts:1970-1983). The service comment at review/service.ts:761-773 asserts mayRead is 'the same boundary the entry side's mayReviewEntry expresses in SQL'; since §32.70 landed, it is not.
- 证据:attachment/service.ts:113-131 collects `citingInstances`/`supplementCitingInstances` and admits the reader on `userMayReview` alone. attachment/db.ts:126 and 180 gate on `[...OPEN_REVIEW_STATES]`, which review/db.ts:275 defines as ['active','blocked','awaiting_supplement']. review/db.ts:294-313 shows userMayReview is a bare mayActOn wrapper; review/db.ts:1960-1987 shows mayReviewEntry likewise. Contrast review/service.ts:774-783, where mayRead special-cases 'awaiting_supplement'. docs/assessment-design.md:1383 freezes the rule as '其他同级审核员不再持有该任务…也不以 reviewer 身份读取该轮', and :1385 says the three concepts '不再共用一个 mayActOn'. The gap is untested: review-access.test.ts:277 checks only instance and history, only for the asker; the three-door shape() that includes `file` (review-access.test.ts:126-140) is never exercised during an open ask.
- 最小修法:Give userMayReview and mayReviewEntry the same open-ask term mayRead has — an `and not exists (select 1 from review_supplement_requests ask where ask.tenant_id = ri.tenant_id and ask.review_instance_id = ri.id and ask.status = 'open' and ask.requested_by <> <reader>)` guard, or better, one shared SQL fragment consumed by all four doors so they cannot drift again. Then extend the review-access open-ask case to run doors() as the peer, asserting file and history are shut while the asker's stay open.
- **反驳者校正**:The finding is real and its mechanism is correctly identified, but three details need tightening.

(a) It is four doors, not two: `openAttachment`, and through it `describeAttachment` and `describeAttachments` (attachment/service.ts:204-238), all share the same `authorize`, so the peer also keeps reading filenames/sizes, not just bytes. On the history side the gate is `mayReviewEntry OR requireRosterReach` (entry/service.ts:1356-1363); the reviewer term is the leaking one, the roster term is the legitimate §32.70 ③ administrative view.

(b) Exposure requires ids the peer already holds. Every discovery surface already closes during the ask: the inbox filters `ri.state = 'active'` (review/db.ts:623), the reviewer desk filters 'active' / ('active','blocked') (review/db.ts:1697, 1729), `awaitingPage`'s open branch requires `sr.requestedBy = userId` (review/db.ts:1812-1825), the activity feed's `openDoor` got the narrowing in 773b2e83 (entry/db.ts:1385-1394), and `getReviewInstance` refuses via `mayRead`. So this is a stale-handle read by a colleague who was legitimately reading the same round seconds earlier, not a route by which a sidelined peer can find new material. That makes it a consistency gap in a one-day-old policy narrowing rather than a confidentiality breach against an outsider — moderate, not major.

(c) The proposed guard's polarity should match the existing one rather than invert it: use the `openDoor` form (`ri.state <> 'awaiting_supplement' or exists(... and ask.requested_by = <reader>)`), which is what mayRead/awaitingPage already express; the claim's `not exi…

- **反驳者校正**:The mechanism, the reachability and the untested gap are all as stated; the severity is overstated, and two details need fixing.

1. Severity is moderate, not major. The leak is read-only and the reader is not a stranger. Writes are already shut: `decideReview` refuses with `awaiting-supplement` at review/service.ts:985-987, and `cancelSupplement` checks `requestedBy` first (review/service.ts:1798-1816, per §32.70). No listing surface exposes the round either — `inboxPage` filters `ri.state = 'active'` (review/db.ts:583), `awaitingPage` already carries the §32.70 branch (review/db.ts:1815-1822), and the activity feed was narrowed in 773b2e83 (entry/db.ts:1385-1394). So the peer must already be holding the entryId/attachmentId from before the ask, and the bytes they get are ones they lawfully read minutes earlier and will lawfully read again the moment the student answers. This is a conformance gap against §32.70's "不以 reviewer 身份读取该轮", not a privilege escalation.

2. `getEntry` is not a third door — it admits only the owner or roster reach (entry/service.ts:832-844), no reviewer term. The exposure is exactly the two doors named: `openAttachment` via `userMayReview` (attachment/service.ts:117-132) and `getEntryHistory` via `mayReviewEntry` (entry/service.ts:1357, wired at server/index.ts:1900-1901). `userMayReview` has only two consumers repo-wide (attachment/service.ts:119 and review/service.ts:746), so those two plus the already-fixed feed are the whole set.

3. The proposed SQL should mirror the positive form the feed already uses rather than the `not exists (... requeste…

- **反驳者校正**:The defect is real and the mechanism is correct, but three details are mis-stated and one nuance is worth recording.

(1) Wrong test cited as the coverage gap. The §32.70 regression test is packages/plugins/assessment/core/tests/review-workbench.test.ts:862-945, not review-access.test.ts:277. That test DOES run a peer through two doors during the open ask — getReviewInstance at :904 (asserted ASSESSMENT_REVIEW_NOT_FOUND at :940) and cancelSupplement at :908 (asserted not-requester at :941). What is missing is the peer against openAttachment and getEntryHistory. review-access.test.ts's contribution is that its three-door helper including `file` (:125-131) is exercised only while the round is active (classPeer asserted OPEN at :196), and its open-ask case (:206-286) drops to two doors run only as the asker (:225-228). The claim's conclusion — the peer/open-ask combination is untested for the file and history doors — holds; the file:line attribution does not.

(2) Line offsets. The attachment gate spans attachment/service.ts:100-135 with userMayReview at :119 (claim says 113-131); the three-door shape is review-access.test.ts:125-131 (claim says 126-140) and classPeer is one line, :146 (claim says 145-148).

(3) Nuance the claim does not address, which softens the attachment half specifically. docs/assessment-design.md:514 froze the bound-attachment reader set as "entry 的 subject ∪ 具审核/复查资格者 ∪ batch admin ∪ 归档打印读者" — keyed on review ELIGIBILITY, which is §32.70's concept ① (stage membership), and §32.70 explicitly preserves ① for the pool. So there is a defensible reading in w…

### [major · ui-projection] The permission catalog crosses the wire as pre-chosen Chinese, and the role editor prints it verbatim

- 位置:packages/plugins/base/rbac/src/client/RoleEditor.tsx:261
- 判定:CONFIRMED,3/3 upheld(第四轮)
- 主张:Permission names and descriptions are authored as zh-CN string literals in each plugin's permissions.ts, shipped over /iam/permissions as plain Schema.String, and rendered raw as checkbox labels — a server response carrying an already-chosen language, which the frozen i18n boundary forbids and which no gate can see.
- 失败场景:An operator holding iam.role.read/manage sets the language to en-US (a declared SupportedLocale, packages/contracts/i18n/src/index.ts:102) and opens /organization/roles → edit a role. The permission checkbox list renders '查看角色', '管理角色', '管理测评批次', '强制切换阶段', '查看组织架构' — the raw strings from packages/plugins/base/rbac/src/permissions.ts:13 and packages/plugins/assessment/core/src/permissions.ts:14. Switching locale changes nothing, because these strings never became MessageDescriptors and so have no catalog key in any locale. The same operator, on the assessment phase-permission editor, sees the SAME code assessment.review.process rendered correctly as 'Review submissions' (packages/plugins/assessment/core/src/client/i18n.ts:3935, id 'assessment/permission/review-process') — one screen honours the boundary, the other does not, for identical data.
- 证据:packages/plugins/base/rbac/src/api.ts:170-181 declares listPermissions success as `name: Schema.String, description: Schema.NullOr(Schema.String)` — not UiText. Values come from plugin source literals: packages/plugins/base/rbac/src/permissions.ts:13 `name: '查看角色'`; packages/plugins/assessment/core/src/permissions.ts:14 `name: '管理测评批次'` plus a Chinese description; packages/plugins/base/org/src/permissions.ts:8 `name: '查看组织架构'`. packages/plugins/base/rbac/src/client/RoleEditor.tsx:259-263 maps them straight onto the control: `options={(catalog.data?.permissions ?? []).map((permission) => ({ value: permission.code, label: permission.name, hint: permission.code }))}` — no format(), no formatText(). CLAUDE.md's frozen i18n boundary: the server may only send UiText (message(id, en) or literal(value)) and never a string in an already-chosen language; browser copy goes through the catalog. These are authored product copy, not tenant business data. The gate is structurally blind: tools/tests/catalogs.test.ts:56-72 collects ids only from values shaped {kind:'message', id, defaultMessage}, so plain strings are invisible, and packages/contracts/i18n/src/index.ts:279-288 only checks namespaces of declared descriptors. Contrast packages/plugins/assessment/core/src/client/i18n.ts:3930-3941, which gives the very same codes proper descriptors.
- 最小修法:Make PermissionDefinition carry a UiText (or a message id the owning plugin translates) instead of a display string, have /iam/permissions send that, and render it through LocalizedText/format in RoleEditor — mirroring what assessment already does for PHASE_GATED_CODES. The catalogs gate then covers permission labels automatically.
- **反驳者校正**:The violation is real, but three details in the claim are wrong or overstated.

1. Only NAMES are displayed, not descriptions. RoleEditor.tsx:259-263 renders `label: permission.name` and `hint: permission.code` — the Chinese `description` crosses the wire (api.ts:176, server/index.ts:625) but is never rendered anywhere in the client. The user-visible defect surface is the checkbox labels only; the descriptions are dead weight on the response.

2. The five example strings cannot appear in one list. RoleEditor.tsx:58-62 queries with `query: { target: role.kind === 'org' ? 'org-node' : 'tenant' }`, and the handler filters on that (server/index.ts:608-611, service at :267-274). So editing a TENANT role shows only tenant-target codes ('查看角色', '管理角色', '定义超出自身权限的角色'), and editing an ORG role shows only org-node codes ('管理测评批次', '强制切换阶段', '查看组织架构'). Each string does render untranslated, just on one of two disjoint lists.

3. The contrast case works by a different mechanism than the claim implies, which changes the proposed fix. packages/plugins/assessment/core/src/client/PermissionProfileEditor.tsx does NOT receive UiText from the server: it imports PHASE_GATED_CODES client-side from ../permissions.ts:13 and looks up its own catalog with `format(m[`permission.${code}`])` at :134 (hint at :136). So "mirroring what assessment already does for PHASE_GATED_CODES" means a client-side catalog keyed by permission code, not a server-sent UiText. Both fixes are viable — a server UiText field has its own precedent at packages/plugins/infra/ui-registry/src/api.ts:22,36 — but only the client-c…

- **反驳者校正**:The defect is real and reachable, with three corrections to scope.

(1) Descriptions cross the wire but never render. `/iam/permissions` has exactly one consumer in the repo — `packages/plugins/base/rbac/src/client/RoleEditor.tsx:58-62` — and it uses only `permission.code` and `permission.name` (`:259-263`, `label: permission.name`, `hint: permission.code`). The Chinese `description` strings (e.g. `packages/plugins/assessment/core/src/permissions.ts:15 '批次、阶段、题目与花名册的全生命周期管理'`) are shipped to the browser and dropped. So the user-visible violation is 24 checkbox labels on one control, on one screen; the description is a latent wire-level violation only.

(2) No single screen shows the mixed list the scenario prints. The query is target-filtered by role kind (`RoleEditor.tsx:59-62`, `query: { target: role.kind === 'org' ? 'org-node' : 'tenant' }`). '查看角色'/'管理角色' are `target: 'tenant'` (`packages/plugins/base/rbac/src/permissions.ts:14,19`) while '管理测评批次'/'强制切换阶段'/'查看组织架构' are `target: 'org-node'` (`packages/plugins/assessment/core/src/permissions.ts:16,22`; `packages/plugins/base/org/src/permissions.ts:10`). An en-US operator sees the Chinese tenant subset when editing a tenant role and the Chinese org-node subset when editing an org role — never both at once.

(3) One extra consequence the report misses, which constrains the proposed fix: `name` is also a server-side search key — `packages/plugins/base/rbac/src/server/index.ts:610-615` filters on `definition.name.toLowerCase().includes(search)`. Replacing the string with a UiText removes the only human-readable thing that pre…

- **反驳者校正**:The claim is real and no prior art covers it, but two details are overstated.

1. Only `name` reaches the screen. RoleEditor's single consumer maps `label: permission.name, hint: permission.code` (packages/plugins/base/rbac/src/client/RoleEditor.tsx:259-263) — `description` crosses the wire unlocalized (packages/plugins/base/rbac/src/api.ts:176) but is never rendered anywhere. RoleEditor is in fact the only client consumer of `listPermissions` in the repo (grep over packages/plugins/_/_/src/client and apps/web finds only RoleEditor.tsx:59 plus two test stubs), so the blast radius is one control on one screen, not a family of screens.

2. The contrast with assessment is not "one screen honours the boundary for identical data" — assessment's phase/staff editor never fetches /iam/permissions at all. packages/plugins/assessment/core/src/client/access/permissions.ts:6-10 says so explicitly: labels are keyed off the plugin's own `BATCH_STAFF_CODES` "rather than by a catalog fetched from the server", so a code without a label fails to compile. The codes overlap; the data path does not.

Two things the proposed fix has to account for and does not mention. The server filters `search` against `definition.name.toLowerCase()` (packages/plugins/base/rbac/src/server/index.ts:606,613-614), so turning `name` into a `UiText` moves permission search to the client or requires searching the id/defaultMessage instead. And the same literal is also persisted as `permissions.name varchar(100)` (packages/plugins/base/rbac/src/db/entities.ts:43), upserted by the registry and seeded before any plugin…

### [major · unbounded-work] The review patrol runs one heavy authorization query per open round in the tenant, every minute, with no bound — and Schedule.fixed removes the pause once it overruns

- 位置:packages/plugins/assessment/core/src/server/index.ts:3585
- 判定:CONFIRMED,3/3 upheld(第四轮)
- 主张:patrolReviewRounds loads every active/blocked ReviewInstance in a tenant into one array and then issues an un-memoized reviewersAt query per round; at cohort scale the sweep cannot finish inside its minute, and Schedule.fixed then re-runs it with zero delay, so the database is saturated continuously.
- 失败场景:A batch of 5,000 participants × 20 items, ~30% of claims in review = ~30,000 rows in state active/blocked. Every 60s: openInstances(tenantId) (review/db.ts:882-911 — no LIMIT, no keyset) materializes all 30,000 rows including ri.effectiveChain, the fully resolved two-route policy jsonb per round (~1-3 KB each → 30-90 MB in one JS array, plus the pg driver's own copy). Then the loop at index.ts:3590 runs. The membership probe at 3594 is memoized by (nodeId, roleIds) — but the eligibility probe at 3614-3630 is keyed by nothing and runs once per round: ~30,000 executions of reviewersAt (review/db.ts:212-263), each a `select distinct` over role_grants with the mayReview predicate and an ltree containment subquery. Panel-quorum stages add openPanelOf + livePanelSeats per round on top. At an optimistic 2 ms per authorization query that is 60 s of pure query time for one tenant — already the whole budget — and each is a separate withDb acquire/release against the pool, competing with live traffic. Once the sweep exceeds the interval, repos/effect/packages/effect/src/Schedule.ts:948-950 shows `fixed` computes `runningBehind ? 0 : boundary`, i.e. delay 0: the next sweep starts immediately, forever. The result is a background loop that permanently occupies the connection pool and makes every student submission and reviewer decision slow. Note the asymmetry with the sibling sweep: sweepDueBoundaries is capped by SWEEP_BATCH_LIMIT = 200 (index.ts:472, 3781); the patrol has no counterpart cap and no cursor.
- 证据:packages/plugins/assessment/core/src/server/index.ts:3580-3728 (patrolReviewRounds; unmemoized reviewersAt at 3614-3630); packages/plugins/assessment/core/src/review/db.ts:882-911 (openInstances — no LIMIT, selects effectiveChain per row); packages/plugins/assessment/core/src/review/db.ts:212-263 (reviewersAt, mayReview + ltree); packages/plugins/assessment/core/src/phase/scheduler.ts:24,78 (SWEEP_INTERVAL '1 minute', Schedule.fixed); repos/effect/packages/effect/src/Schedule.ts:948-950 (runningBehind → zero delay); contrast packages/plugins/assessment/core/src/server/index.ts:472,3781 (SWEEP_BATCH_LIMIT = 200 on the other sweep).
- 最小修法:Page the patrol (a keyset over ReviewInstance with a per-tick ceiling like SWEEP_BATCH_LIMIT, resuming next tick), and key the eligibility probe by (nodeId, roleIds, subjectUserId, actorId, route) the way the membership probe is already keyed — or fold the whole per-round question into one set-returning query.
- **反驳者校正**:Real, with a narrower blast radius and a different hot-spot than stated.

What is true: patrolReviewRounds (index.ts:3580) has no per-tick ceiling anywhere on its path. tenantsWithOpenRounds (review/db.ts:1003-1013) is an uncapped distinct over every open instance; openInstances (review/db.ts:882-925) then materializes every active/blocked ReviewInstance in a tenant, including a parsed ResolvedPolicy per row, with no LIMIT and no cursor; and the eligibility probe (index.ts:3614-3631) is un-memoized, so it costs one reviewersAt round-trip per open round, with openPanelOf + livePanelSeats added per panel-quorum round. The sibling sweep is capped at SWEEP_BATCH_LIMIT = 200 (index.ts:472, 3781; server/db.ts:1801-1802); the patrol has no counterpart. Once one sweep exceeds the interval, Schedule.fixed sets delay 0 and keeps setting it (repos/effect/packages/effect/src/Schedule.ts:948-951), so the loop runs back-to-back indefinitely. The patrol additionally runs on the one-minute sweep (phase/scheduler.ts:24,44,78) while docs/assessment-design.md:189,380 specify a five-minute tier, a 5x deviation, and design line 380 further specifies resolving per (roleId, nodeId) group ("a batch has at most tens to hundreds of groups"), which the membership probe honours but the eligibility probe does not.

What is mis-stated: (a) reviewersAt contains no ltree containment subquery. mayReview (review/db.ts:56-105) requires rg.org_node_id = nodeId, an exact anchor by decision (db.ts:36-40, design 32.23); the cost is a correlated EXISTS joining role_grants, roles, users, batch_access_sources and b…

- **反驳者校正**:`patrolReviewRounds` (packages/plugins/assessment/core/src/server/index.ts:3580-3631) has no per-tick ceiling and no cursor: `openInstances` (review/db.ts:882-911) materializes every `active`/`blocked` round in a tenant with its parsed `effectiveChain`, and the loop then issues one un-memoized `reviewersAt` per round (index.ts:3614-3631; the membership probe at 3594 is memoized by (nodeId, roleIds), the eligibility probe is keyed by nothing), plus `openPanelOf` + `livePanelSeats` per round on quorum-`all` stages (3638, 3649). Cost is therefore linear in open rounds, with the sibling sweep's `SWEEP_BATCH_LIMIT = 200` (index.ts:472, 3781) having no counterpart here, and the patrol riding the 1-minute `Schedule.fixed` sweep (phase/scheduler.ts:24, 78) rather than the five-minute tier the design specifies (docs/assessment-design.md:189, 380) — where §32.9 also specified de-duplicating to per-(roleId, nodeId) groups ("一个批次最多几十上百组"), a bound the later `blocked_reason` ruling (docs/assessment-design.md:1335) invalidated for the eligibility probe without replacing it. At the scale the design itself anticipates ("一所学校几万活跃实例", docs/assessment-design.md:378) that is tens of thousands of sequential queries per sweep, and once a sweep exceeds its minute `Schedule.fixed` returns delay 0 (repos/effect/packages/effect/src/Schedule.ts:948-951), so sweeps run back to back indefinitely.

Corrections to the claim's mechanism: (a) there is no ltree containment — `mayReview` anchors exactly on `rg.org_node_id = nodeId` (review/db.ts:57-104, comment at :38-41, §32.23), so each probe is indexed eq…

- **反驳者校正**:Real and unruled, but overstated in its mechanics.

What holds. `patrolReviewRounds` loops every tenant with open rounds (packages/plugins/assessment/core/src/server/index.ts:3581-3584) and, per tenant, materializes every `active`/`blocked` ReviewInstance with no LIMIT and no cursor, including the per-round resolved policy jsonb (packages/plugins/assessment/core/src/review/db.ts:882-911; `ri.effectiveChain` at 901, state filter at 909). Membership is memoized per `(nodeId, sorted roleIds)` (index.ts:3591), but the eligibility probe (index.ts:3614-3630) is issued per round, so at the scale the design doc itself names — "一所学校几万活跃实例" (docs/assessment-design.md:378) — the tick issues tens of thousands of `reviewersAt` queries (review/db.ts:212-263, `select distinct` over role_grants with the mayReview predicate). `Schedule.fixed` does return delay 0 once behind (repos/effect/packages/effect/src/Schedule.ts:948-950), so an overrunning sweep re-runs back-to-back. And the asymmetry with the sibling is real: SWEEP_BATCH_LIMIT = 200 caps `sweepDueBoundaries` (index.ts:472, 3781) with no counterpart here.

The claim is also stronger than stated on cadence: §32.9 / §14 ruled the patrol onto the 五分钟档 (docs/assessment-design.md:380; docs/adr/0007-missing-reviewer-has-three-causes.md:27), and the implementation runs it every minute on the phase sweep (phase/scheduler.ts:24,78) — 5x tighter than the ruling, recorded only in STATUS.md:4129 with no §32 entry. Likewise, §32.9 ruled the patrol's cost model as per-`(roleId, nodeId)` group ("一个批次最多几十上百组"); the per-round probe entered later with…

### [major · unbounded-work] An item edit with reroute-all walks every open round one at a time inside the transaction that holds the batch row lock, freezing the whole batch

- 位置:packages/plugins/assessment/core/src/item/service.ts:514
- 判定:CONFIRMED,3/3 upheld(第四轮)
- 主张:propagate loops over every open review round of the item issuing ~10-15 statements each (two of them heavy reviewersAt probes) while updateItem holds `select ... for update` on the batch row, and every student filing and every reviewer decision in that batch blocks on the same lock.
- 失败场景:An administrator edits a question's review policy on a running batch of 5,000 participants and answers the impact dialog with review.open = 'reroute-all' (api.ts:431). updateItem takes lockBatch at item/service.ts:833, and propagate's loop at 514-651 then runs per round: participantOf, resolvePolicy (chain.ts:251-265 → resolveRoute per stage, one nearestRoleNode query per role-selector stage on both routes), nodePathOf, cancelReviewInstance, insertReviewEvent, stageArrival (chain.ts:367 and 371 — two reviewersAt executions of the mayReview predicate), nextRoundNo, insertReviewInstance, sometimes createPanel or a second insertReviewEvent, setEntryState. With 5,000 open rounds that is roughly 50,000-75,000 round trips, ~10,000 of them heavy authorization queries, in one transaction. At 0.5 ms each the batch row is held exclusively for 25-40 s; at realistic per-statement latency it is minutes. Meanwhile lockBatch is the front door for createEntry (entry/service.ts:721), setEntryStatus, and — since a124973b moved it there — decideReview (review/service.ts:954). So for the whole propagation no student in that batch can file or revise a claim and no reviewer can record a decision; they queue on the lock and time out. Nothing bounds the loop and nothing resumes it, so an abort part-way rolls all of it back and the operator retries the same unbounded transaction.
- 证据:packages/plugins/assessment/core/src/item/service.ts:833 (lockBatch), :438-651 (propagate), :514 (the per-round loop), :524-530 resolvePolicy, :584 stageArrival, :591-619 insert path; packages/plugins/assessment/core/src/review/chain.ts:367,371 (two reviewersAt per arrival), :227 (nearestRoleNode per stage); packages/plugins/assessment/core/src/item/db.ts:617-645 (openRoundsOfItem — no LIMIT); lock contenders: packages/plugins/assessment/core/src/entry/service.ts:721, packages/plugins/assessment/core/src/review/service.ts:954.
- 最小修法:Bound the work under the lock: either cap the rounds a single reroute may move and report the remainder as work still to do, or drive the reroute as a resumable job that takes the batch lock per chunk of rounds rather than once for all of them.
- **反驳者校正**:The claim is correct in mechanism and in every citation; two refinements to the statement.

(1) The loop is over `moving`, not literally "every open round": item/service.ts:505-510 excludes rounds whose entry is in `sendBack` (entries the form answer sends back for revision), and for `review.open = 'reroute-blocked'` it further filters to `round.state === 'blocked'`. In the worst case described (reroute-all, no form incompatibilities) `moving` is every open round of that item, so the bound is the same; but the loop is scoped to open rounds of the one _item_, not of the batch.

(2) The per-round statement count of "10-15" is only right when the policy uses `roleId` selectors. `resolveRoute` (review/chain.ts:216-229) issues `nearestRoleNode` only for the non-`roleAt` branch — a policy whose stages are all `roleAt` costs zero queries there, putting the floor around 10 statements per round (of which 2 are the heavy `reviewersAt` probes). The heavy-query estimate of ~2 per round is exact.

If anything the claim understates the work under the lock: the send-back loop at item/service.ts:461-495 is a second unbounded loop over live entries in the same transaction (cancel, event, setEntryState, entry event, attention bump per entry), and `impactUnder` at :388-427 already decodes every live payload before either loop starts.

- **反驳者校正**:The claim stands as a real, reachable defect. Three refinements to its statement:

1. resolvePolicy does not issue "one nearestRoleNode query per role-selector stage on both routes" unconditionally. resolveRoute (review/chain.ts:195-240) resolves a `kind: 'roleAt'` stage purely in memory from the participant's frozen lineage (:218-228, no query); only the other selector kind falls through to `nearestRoleNode` (:225-234). So the per-round statement count is roughly 9 fixed plus one per non-roleAt stage across both routes — commonly 9-11 rather than 10-15. This lowers the multiplier slightly; it does not change the order, and the two reviewersAt probes (chain.ts:367, :371) — the expensive part — are unconditional whenever the landing stage has a nodeId.

2. The lock is already held over O(entries) work before propagate runs, for every config edit including `review.open = 'keep'`: impactUnder (item/service.ts:394-399) loads every live entry payload and every open round of the item and decodes each payload in process. That part is only 2 queries, so it is not the freeze; but it means the fix cannot be "only bound the reroute path" if the goal is a bounded lock hold — the read side needs the same treatment once entry counts are large.

3. Worth adding as corroboration rather than correction: the codebase already implements the bound the claim proposes, for the identical computation, in the patrol (server/index.ts:3585-3607) — membership memoized per (currentNodeId, currentRoleIds) with a comment naming exactly this "forty entries ask the same question forty times" cost, and the…

- **反驳者校正**:The defect stands, but three parts of the statement are wrong or overstated.

(a) Per-round DB work is smaller and partly conditional. resolvePolicy (chain.ts:251-265 → resolveRoute) issues a nearestRoleNode query ONLY for `roleNearest` selectors; a `roleAt` stage resolves from the frozen lineage entirely in memory (chain.ts:216-224, `input.lineage.find(...)`, `continue` before the query). A policy built only from roleAt stages — the shape the design's own examples use — costs zero queries there. The guaranteed per-round cost is roughly participantOf, nodePathOf, cancelReviewInstance, insertReviewEvent, two reviewersAt (chain.ts:367,371), nextRoundNo, insertReviewInstance, setEntryState, plus an optional createPanel or second event: ~9-10 statements, two of them the heavy authorization probes. "~10-15 statements, two heavy probes" is the right order of magnitude but the "one nearestRoleNode per role-selector stage on both routes" is conditional, not a floor.

(b) The loop is over the open rounds of ONE item, not of the batch (item/db.ts:634, `where e.itemId = ...`). Its bound is that question's open rounds, so 5,000 is a full-roster worst case for a single question rather than a typical figure; the derived "25-40 s at 0.5 ms" is an upper-bound illustration, not a measured or typical number. Nothing in the repo states a target roster size, so neither the claim's figure nor a smaller one is evidenced.

(c) The reroute branch is not the only unbounded work under the lock. The send-back branch at item/service.ts:462-498 loops over `input.live` (every in_review/approved entry of…

### [major · unbounded-work] Roster import writes one INSERT per admitted participant, sequentially, inside the batch lock — beside a bulk statement that does the roster in one

- 位置:packages/plugins/assessment/core/src/server/index.ts:1457
- 判定:CONFIRMED,3/3 upheld(第四轮)
- 主张:recordAdmissions is Effect.forEach over the admitted set (sequential by default) calling a single-row insertParticipantEvent, so importing a college costs one round trip per person while the batch row lock is held — the statement immediately above it admits the entire roster with a single INSERT ... SELECT.
- 失败场景:An operator creates a batch for one college (40 classes × ~125 students = 5,000 people) or later runs importParticipants over the same selection. rosterWriteGuards takes lockBatch (index.ts:1470); insertParticipants (server/db.ts:1449-1506) admits all 5,000 in ONE `insert ... select ... from unnest(...) ... returning id, (xmax = 0)`; then recordAdmissions (index.ts:1457-1466) turns the returned 5,000 rows into 5,000 sequential single-row inserts into batch_participant_events. Effect.forEach is sequential unless a concurrency option is passed (repos/effect/packages/effect/src/Effect.ts:731-734), and none is. At 0.3-1 ms per round trip that is 1.5-5 s of pure round-trip time added to the import, all of it inside the transaction holding `for update` on the batch row, during which no claim can be filed and no review decided in that batch. Re-importing a term's worth of colleges multiplies it. The shape is provably avoidable: the bulk form of exactly this write is the line above.
- 证据:packages/plugins/assessment/core/src/server/index.ts:1450-1466 (recordAdmissions = Effect.forEach + single-row insert), called at :2006 (batch creation with import), :3347 (importParticipants), :1439; packages/plugins/assessment/core/src/server/db.ts:2103-2125 (insertParticipantEvent, one row); packages/plugins/assessment/core/src/server/db.ts:1449-1506 (insertParticipants, one bulk statement for the same set); lock at packages/plugins/assessment/core/src/server/index.ts:1470; repos/effect/packages/effect/src/Effect.ts:731-734 (forEach is sequential by default).
- 最小修法:Write the admission events with one multi-row INSERT built from the ids insertParticipants already returned, the same way the participants themselves are written.
- **反驳者校正**:The shape is real: `recordAdmissions` (packages/plugins/assessment/core/src/server/index.ts:1450-1466) is an option-less `Effect.forEach` over the admitted rows calling the single-row `insertParticipantEvent` (server/db.ts:2103-2125), immediately after `insertParticipants` (server/db.ts:1448-1506) admitted the entire roster with one `insert ... select ... from unnest(...) ... returning id, (xmax = 0)`. `Effect.forEach` is sequential absent a `concurrency` option (repos/effect/packages/effect/src/Effect.ts:731-734), and `transaction` (packages/plugins/infra/database/src/server/orm.ts:104-120) pins the whole block to one forked EntityManager on one connection, so an N-person roster costs N serialized round trips where one multi-row INSERT would do. `importCandidates` (server/db.ts:1386-1429) has no LIMIT, so N is the size of the selection.

Two corrections to the scenario as stated:

1. The batch-creation path does not hold a contended lock. `createBatch` (index.ts:1966-2035) calls `insertParticipants`/`recordAdmissions` directly and never calls `rosterWriteGuards` or `lockBatch`; the batch row is created by that same transaction and is invisible to everyone else. There the cost is request latency and a connection held open, not blocked concurrent work. The "no claim can be filed, no review decided" consequence applies to the roster-mutation endpoints that do take the guard: `importParticipants` (guard at index.ts:3327, recordAdmissions at 3347), `addParticipants` (3291-3293) and `setParticipantStatus` (3420-3434). That consequence is genuine: `lockBatch` is `select ... for u…

- **反驳者校正**:`recordAdmissions` (packages/plugins/assessment/core/src/server/index.ts:1450-1466) writes one admission event per admitted person with a sequential, un-concurrent `Effect.forEach` (:1457) over single-row `insertParticipantEvent` (server/db.ts:2103-2125), directly after `insertParticipants` (server/db.ts:1449-1506) admitted the identical set with one `insert ... select ... unnest(uuid[],uuid[]) ... returning id, (xmax = 0)`. N is unbounded: `importCandidates` (server/db.ts:1386-1429) has no `limit`, and neither the `importParticipants` payload (src/api.ts:1954-1964) nor `addParticipants`' `Schema.Array(id)` (src/api.ts:1930-1936) caps it, so N is the eligible population under the selected org nodes — hundreds to thousands per the design's own sizing (docs/assessment-design.md:977). Two corrections to the failure scenario: (a) the scenario's own example, batch creation, does NOT hold the batch lock — `createBatch` (index.ts:1966-2011) never calls `rosterWriteGuards`/`lockBatch` and the batch row it inserts at :1972 is invisible to other transactions, so the large first-draw roster (§32.45, docs/assessment-design.md:170) costs the creating operator seconds of transaction time but blocks no claim filing; the lock harm exists only on `importParticipants` (index.ts:3327) and `addParticipants` (:3291), which do serialize against `createEntry` (entry/service.ts:721) and review decisions (review/service.ts:954) on the same `for update` row. (b) "re-importing multiplies it" is false — `importCandidates` excludes already-active participants (server/db.ts:1417-1423), so a repeat impor…
- **反驳者校正**:The finding stands, with two narrowings. (1) The batch-creation call site is not inside a batch row lock: createBatch (packages/plugins/assessment/core/src/server/index.ts:1964-2006) inserts a brand-new batch in its own transaction and never calls lockBatch, so no concurrent claim filing or review decision can be blocked there — the "no claim can be filed and no review decided" consequence applies only to importParticipants (index.ts:3326 guards, :3347 recordAdmissions) and admitParticipants (index.ts:1439), which do pass through rosterWriteGuards -> lockBatch (index.ts:1470, db.ts:45-56). (2) admitParticipants is not a clean bulk-then-N+1: its validation loop already makes one userLivePosition query (db.ts:2040-2054) and one rbac.canAt call per user, sequentially (index.ts:1425-1436), before the bulk insert, so batching the events there removes only part of the per-person round trips. The described shape — one bulk INSERT ... SELECT admitting the whole roster (db.ts:1449-1506) followed by N sequential single-row inserts into batch_participant_events (index.ts:1457-1466 over db.ts:2103-2125), inside a transaction holding `for update` on the batch row — is exact for the import paths, where candidates come from a single importCandidates query (db.ts:1386). The same per-row shape also exists at index.ts:1634 (acceptAssignments), on a much smaller set.

### [major · unbounded-work] Every item configuration save reads the whole live-entry set into memory with the form config duplicated per row, and decodes each payload in JavaScript

- 位置:packages/plugins/assessment/core/src/item/service.ts:397
- 判定:CONFIRMED,3/3 upheld(第四轮)
- 主张:impactUnder calls liveEntryPayloads with no bound and then runs driver.decodePayload once per row; the query joins AssessmentItemRevision and selects formConfig on every row, so the item's whole form config is materialized once per live entry — and the flow costs this twice per real edit.
- 失败场景:An administrator saves any config change on an item that 5,000 participants have already filed on (status in_review or approved). item/service.ts:929 calls impactUnder, which at :397 runs liveEntryPayloads (item/db.ts:666-694): no LIMIT, and its select list carries both EntryRevision.payload and AssessmentItemRevision.formConfig for every row. A form config of 4 KB and a payload of 2 KB give 5,000 × 6 KB = ~30 MB materialized in the driver plus the mapped JS array — and the 25 MB of that is the same form config repeated 5,000 times. The loop at :401-412 then runs driver.projectPayload and an Effect.result(driver.decodePayload(...)) per row: 5,000 schema decodes on the request fiber, inside the transaction that holds the batch row lock (:833). Because the flow is preview-then-answer (the first save returns ItemChangeDecisionRequired with an impactToken at :940, the second replays with effects at :945), one human edit pays this twice. Two administrators saving concurrently, or one retrying, multiply the heap by that many; on a modest server this is a real OOM path, and in every case it is tens of seconds of lock-held CPU during which no student in the batch can file.
- 证据:packages/plugins/assessment/core/src/item/service.ts:388-427 (impactUnder), :397 (liveEntryPayloads), :401-412 (per-row decode), :929-950 (called on every save with a config, twice per edit via the impactToken round trip), :833 (batch lock held); packages/plugins/assessment/core/src/item/db.ts:666-694 (liveEntryPayloads — no LIMIT, selects AssessmentItemRevision.formConfig per row).
- 最小修法:Drop formConfig out of the per-row projection (fetch the distinct item revisions once and join them in JS by id), and stream or chunk the compatibility scan instead of materializing the whole live set — the counting result is an aggregate, not a list.
- **反驳者校正**:The finding stands on mechanism; four statements need correcting, one of them in the auditor's favour.

(a) "Twice per real edit" is conditional, not universal. The second pass happens only when `missingDecisions` (item/impact.ts:209-219 via `decisionNeeded`, impact.ts:201-206) is true — i.e. the form config changed AND at least one live entry came back incompatible, or the review policy changed AND there is at least one open round. A benign config save (field reorder, optional field added, no policy change) against 5,000 live entries pays the full scan exactly once and returns success. The doubling is real for the disruptive edits, and on that first pass the entire transaction rolls back (service.ts:941), so that scan's cost is pure waste.

(b) The per-row decode cost is driver-dependent, and for the only non-trivial driver it is worse than described. `constantDriver.decodePayload` is `Effect.succeed(payload)` with no `projectPayload` (item/constant.ts:15-19) and `declarationDriver` likewise (item/declaration.ts:15) — for those item types the loop is near-free and only the fetch is wasteful. For `evidenceDriver` (packages/plugins/assessment/evidence/src/driver.ts:345-353) each row runs `project` (driver.ts:288-312), which calls `decodeConfig` on both the row's formConfig and the new one, and then `decode` (driver.ts:137-242), which calls `decodeConfig` again — and `decodeConfig` is a full `Schema.decodeUnknownResult(evidenceConfig)(config)` (driver.ts:124-127). That is ~3 full config schema validations per live entry, so the duplicated formConfig is not merely materialized…

- **反驳者校正**:Real, but four things are mis-stated.

(1) "Twice per real edit" is conditional, not universal. The second pass only happens when a decision is required: missingDecisions/decisionNeeded (item/impact.ts:201-219) return false unless the form changed AND some entry is incompatible, or the review policy changed AND an open round exists. A benign config save on 5,000 live entries pays the scan once, not twice.

(2) "Two administrators saving concurrently multiply the heap" is false for the same batch, which is the interesting case. lockBatch (src/server/db.ts:45-56) is SELECT ... FOR UPDATE on the batch row and is taken at item/service.ts:833, before impactUnder at :929, so two admins editing items in the same batch serialize — the second blocks at the lock and only then scans. Only edits in different batches overlap.

(3) The per-row CPU is understated in kind and the wall-clock figure is unmeasured. With the only shipped driver, each row costs up to three Schema decodes of the form config, not one payload decode: project() decodes both toConfig and fromConfig (evidence/src/driver.ts:285, :288) and decode() decodes config again (:143), all via Schema.decodeUnknownResult(evidenceConfig) (:122-124). Conversely, "tens of seconds" and the 4 KB/2 KB byte figures are extrapolation with no measurement behind them — the evidence formConfig is a field list with label <=100 chars and key <=63 (driver.ts:19-84), so ~1 KB for a ten-field form is more typical than 4 KB, and the OOM path needs an unusually large config plus an unusually large live set.

(4) The proposed fix's "the counting r…

- **反驳者校正**:The claim stands, with two refinements. Worse than stated: the per-row decode loop (item/service.ts:401-412) is unconditional on whether formConfig actually changed — impactOf only compares configs afterwards (item/impact.ts:151-153) — so a review-policy-only or scoring-only edit still decodes every live payload under the batch lock. Narrower than stated: the flow costs the scan twice only when a decision is required (missingDecisions → 409 at item/service.ts:940); a save that disturbs nothing pays one full scan. Two notes for the fix: the same unbounded pattern exists one scope wider at item/db.ts:525 (liveBatchPayloads, whole batch) consumed at server/index.ts:2300-2316 during a materialRange narrowing under the same lock, so both sites should move together; and while the report itself is aggregate counts (item/impact.ts:34-59), impactTokenOf (item/impact.ts:99-111) genuinely needs every live entry's id/status/entryRevisionId — it is only the payload and the per-row formConfig duplication that need not be materialized.

## 次要(minor)

### [minor · assembly-cli] The capability-provider exemption in the config guard silently swallows @qualy/plugin-rbac's manifest block

- 位置:packages/core/assembly/src/resolve.ts:319
- 判定:CONFIRMED,3/3 upheld(第三轮)
- 主张:resolve() exempts every capability provider from the "nobody reads this config" refusal on the assumption that a provider consumes providerConfig during a work phase, but the permissions provider (@qualy/plugin-rbac) has no generate, deploy, modules or commands, so its work context is never even constructed — a config block written for it is accepted, hashed and then read by nothing.
- 失败场景:An operator adds to qualy.yml:

  '@qualy/plugin-rbac':
  config:
  escalation: strict

`pnpm qualy resolve` succeeds and writes a new manifestHash; `pnpm qualy resolve --frozen-lockfile` passes; a production boot passes. Nothing reads the block anywhere: rbac's descriptor takes no config channel, so runtimeLayers/loadAssembly skip it, and the permissions provider declares no work phase, so capabilityContext (and therefore providerConfig) is never built for key 'permissions'. The setting looks applied and is not — verbatim the failure the comment at resolve.ts:312-316 says this check exists to prevent. By contrast the same typo under @qualy/plugin-database is refused twice (assembly/work.ts:34-42 and server/config.ts:68 with onExcessProperty:'error').

- 证据:resolve.ts:277 builds providerOwners from every loaded provider's pluginId; resolve.ts:317-321 flags a config block only when `!providerOwners.has(id) && descriptor.config === undefined`, so a provider is exempt unconditionally. packages/plugins/base/rbac/src/index.ts:17-19 — Plugin.define('@qualy/plugin-rbac', { dependsOn: [...] }, ...) declares no `config`, and plugin-kit/src/index.ts:229 only sets descriptor.config when options.config is present. packages/plugins/base/rbac/src/assembly/index.ts:31-96 — the provider object has key/parseContribution/contributionFromDescriptor/resolve/retainsPlugin/plan and nothing else; capabilityWork (core/assembly/src/work.ts:45-56) only builds a context for generate/deploy/commands, none of which exist here.
- 最小修法:Narrow the exemption to providers that can actually receive the block: exempt `id` only when the loaded provider for the capability it owns declares at least one of generate/deploy/commands (i.e. `providerOwners` should be built from providers whose work phases exist), so a config block on a resolve-only provider is refused like any other unread block.
- **反驳者校正**:The finding is accurate as stated; only the proposed fix needs amending. Filtering providerOwners down to providers that declare generate/deploy/commands is not the right predicate, because that is not the only route by which providerConfig reaches a provider plugin. apps/cli/src/main.ts:229-233 builds a full capabilityContext (hence providerConfig) for a descriptor-declared `Cli.command` whose `context` is 'capability', resolving the key with capabilityKeyOf (main.ts:255-260, which maps a plugin id to the capability it provides). That consumer lives on the descriptor, not on the provider object, so the proposed narrowing would wrongly refuse a legitimate config block for a provider whose only reader is such a command. The exemption should be "this plugin has at least one reader of providerConfig" = provider declares generate, deploy or commands, OR the descriptor declares a Cli.command with context 'capability'. Two incidental notes: listing `modules` among the missing hooks is irrelevant, since CapabilityModuleContext omits providerConfig by design (contracts/assembly/src/index.ts:113-118); and the guard message at resolve.ts:322-324 has no test anywhere in the repo, so any change here is currently unguarded.
- **反驳者校正**:The defect is real as a mechanism, with three corrections. (1) Scope: the providerOwners exemption is load-bearing for exactly one plugin in this repo. Only two capability providers exist (grep `Plugin.capability(`: database/src/index.ts:17 and rbac/src/index.ts:42), and @qualy/plugin-database already carries `descriptor.config` (database/src/index.ts:11-13), so it would remain exempt without the providerOwners clause. The clause's only effect today is to exempt @qualy/plugin-rbac. (2) The claim's "refused twice under database" is imprecise: assembly/work.ts:34-42 does not run at resolve or boot — it fires only during generate/deploy/`qualy database` commands. The resolve/boot-time refusal for database comes from its descriptor config channel with onExcessProperty:'error'. (3) `modules` is not a phase that could ever receive the block — CapabilityModuleContext (contracts/assembly/src/index.ts:105-118) omits providerConfig by design — so the proposed predicate correctly reduces to "provider declares at least one of generate/deploy/commands". Severity is genuinely minor rather than merely mis-scaled: rbac exposes no configuration at all (no env reads, no documented option), so the swallowed block can only be an invented key or one mis-attributed from another plugin; the guard's original motivating incidents involved real settings (auth env vars, database url) that had a plausible origin. Note also that no test currently pins this refusal, so whichever way it is decided it is unfrozen behavior.
- **反驳者校正**:The finding holds, with two refinements.

1. The exemption is not merely too wide for rbac - it is currently dead for the only provider it was written for. @qualy/plugin-database already satisfies the third clause on its own: packages/plugins/infra/database/src/index.ts:11-13 is Plugin.define('@qualy/plugin-database', { config }, ...), so descriptor.config !== undefined and the block is exempt through `descriptor.config === undefined` regardless of providerOwners. Since packages/contracts/assembly/src/index.ts and the two plugin providers are the entire provider set (grep for defineCapabilityProvider), @qualy/plugin-rbac is the sole plugin the `!providerOwners.has(id)` clause actually exempts, and it is exactly the plugin that can never receive the block. Deleting the clause outright would change behaviour for rbac only.

2. The proposed fix is slightly under-specified. providerConfig reaches a provider through one more door than generate/deploy/commands: apps/cli/src/main.ts:229-233 hands capabilityContext(resolution, capabilityKeyOf(resolution, entry.plugin), args) to any descriptor-declared Cli.command whose context tier is 'capability'. A capability owner with no provider work phases but such a command would legitimately read the block. Note also that `modules` must NOT be counted - packages/contracts/assembly/src/index.ts:106-118 documents CapabilityModuleContext as deliberately excluding providerConfig because module output lands in the working tree. So the exemption should be "the loaded provider declares generate, deploy or commands, OR the plugin declares a Cli.com…

### [minor · assembly-cli] Every database CLI phase silently falls back to localhost while the server refuses to assume a database

- 位置:packages/plugins/infra/database/src/assembly/work.ts:50
- 判定:CONFIRMED,2/3 upheld(第三轮)
- 主张:databaseWork() resolves the connection as `process.env.DATABASE_URL ?? LOCAL_FALLBACK` with no warning and no environment awareness, so `qualy deploy`, `qualy database adopt` and `qualy generate` can apply or mutate a completely different database than the one intended and report success — the runtime path deliberately refuses this same fallback in production.
- 失败场景:A developer with the compose stack up (postgres://qualy:qualy@localhost:5432/qualy exists) runs `pnpm qualy deploy` intending the staging database, having set DATABASE_URL in the deployment's environment but not in this shell, and with no .env for process.loadEnvFile() to pick up. databaseWork returns LOCAL_FALLBACK; assertDatabaseExists finds the local `qualy` database and succeeds; runMigrations applies the pending lineage to the local development database and prints `database: applied 3 migration(s) (412ms)`. Nothing in the output names a host or a database. Staging is untouched and still behind, and the next production start (QUALY_MIGRATIONS defaults to off via run.ts:31) fails with MigrationsBehind far from the cause. `qualy database adopt` in the same shell is worse: it writes the whole lineage into the local ledger and runs applyBaseline against it.
- 证据:assembly/work.ts:50 `url: process.env.DATABASE_URL ?? LOCAL_FALLBACK` — the only place the CLI decides which server it reaches; consumed by deploy (assembly/index.ts:82), adopt (assembly/index.ts:121,146,152), migrate-command.ts:17 and structuralDiff (diff.ts:188, which also CREATEs and DROPs scratch databases on that server). The runtime side does the opposite for exactly this reason: server/config.ts:75-87 asks whether DATABASE_URL was set at all and `Effect.die`s in production ('a production instance will not assume one'), warning otherwise. defaults.ts:1-8 states the invariant this breaks: 'the two reaching different databases - neither of which anything would notice'.
- 最小修法:Have databaseWork log which database it fell back to when DATABASE_URL is unset (the CLI already prints to console), and name the target host/database in the deploy/adopt success lines so a wrong-database run is not indistinguishable from a right one.
- **反驳者校正**:The core defect is real: packages/plugins/infra/database/src/assembly/work.ts:50 resolves the connection as `process.env.DATABASE_URL ?? LOCAL_FALLBACK` with no environment awareness and no log line, while the runtime path (src/server/config.ts:75-87) is environment-aware and always speaks up. The write-hazard is narrower than stated, though.

Actually dangerous: `qualy deploy` (assembly/index.ts:81-82 -> migrator.ts:161 runMigrations) and `qualy database migrate` (migrate-command.ts:14-17) apply the pending lineage to the fallback database, and `qualy database adopt` (index.ts:119-155) runs applyBaseline (diff.ts:270) and adoptMigrations (migrator.ts:145) against it. migrator.ts:82-99 assertDatabaseExists rejects only a missing database, so an existing local `qualy` sails through, and the success lines (index.ts:86-90, :147-149, :156-160) name no host or database.

Not dangerous, contrary to the claim: `qualy generate` never applies or mutates the fallback database. generate.ts:60 -> diff.ts:182-193 uses work.url purely as the admin connection for scratchDatabase (diff.ts:49-76, :188, :210); the lineage runs inside the scratch (diff.ts:190) and both diff sides are scratch databases by design (diff.ts:41-48), so a wrong server produces an identical migration file plus `qualy_gen_*` databases that are created and dropped there. Likewise "every database CLI phase" is loose: check, custom, where and drop-guard call databaseWork but never open a connection.

Two smaller corrections. The runtime does not "do the opposite" — it takes the same fallback in development (config.ts:90…

- **反驳者校正**:The claim stands, with two overstatements to trim.

(1) The title's "every database CLI phase" is wrong: most phases never open a connection. `resolve` and `plan` touch nothing outside the repo by design, and the `check`, `custom`, `where` and `drop-guard` commands read only `work.migrations` (assembly/index.ts:94-186). Exactly four paths reach a server on the fallback URL: `deploy` (index.ts:80-88), `generate` (via structuralDiff, diff.ts:188, which CREATEs and DROPs scratch databases there), `database adopt` (index.ts:121-152) and `db migrate` (migrate-command.ts:17).

(2) `adopt` is not unguarded. index.ts:121-131 runs `diffAgainstDeclared` first and throws ('this database is not the schema this assembly declares') whenever the structural diff is non-empty, so adopt can only mis-fire against a database that already matches the declared schema. That does not rescue the scenario as written — a developer's local database built from the same lineage matches by construction, so the ledger write and `applyBaseline` land on it — but on an arbitrary wrong target adopt aborts rather than writing. The unconditional wrong-target writers are `deploy`/`db migrate` (apply the lineage) and `generate` (CREATE/DROP scratch databases).

Everything else verified as stated, including run.ts:31 and the absence of any host or database name in the success output (index.ts:83-87, migrate-command.ts:20-25, apps/cli/src/main.ts:186).

- 反对票(未采纳):The mechanism at packages/plugins/infra/database/src/assembly/work.ts:50 (`url: process.env.DATABASE_URL ?? LOCAL_FALLBACK`) exists as described, but the harm path is not reachable in this repository, and two of the claim's three supporting arguments are inverted.

1. The fallback branch is unreachable in the documented setup, and the scenario's preconditions contradict each other. apps/cli/src/main.ts:26-28 calls `process.loadEnvFile()` before any command runs — added for exactly this issue in an earlier audit round (STATUS.md:237 item ②: "`qualy deploy` 与全部 `qualy database` 命令**不读 .env**…DATABASE_URL 失效后静默打到 localhost 兜底——CLI 顶部补 `process.loadEnvFile()`", also docs/assembly-design.md:1724…

### [minor · assembly-cli] The permissions capability sorts its lock state with localeCompare, so lock bytes depend on the developer's locale

- 位置:packages/plugins/base/rbac/src/assembly/index.ts:74
- 判定:CONFIRMED,2/3 upheld(第三轮)
- 主张:The permissions provider orders capabilities[permissions].state.order with String.prototype.localeCompare, which uses the process's default ICU collation; every other sort that reaches the lock uses byte comparison, so two machines whose manifests say the same thing can write different qualy.lock.json bytes and therefore different resolutionHash values.
- 失败场景:A plugin whose id contains a digraph the default collation reorders is added — e.g. `@qualy/plugin-aaa-import` alongside `@qualy/plugin-ab`. On a machine with LANG=da_DK.UTF-8 (or nb_NO), localeCompare places 'aa' after 'z', so resolve writes order ['@qualy/plugin-ab', ..., '@qualy/plugin-aaa-import']; on en_US it writes the reverse. Both are legitimate `pnpm qualy resolve` runs on an identical tree, but the two locks differ byte for byte and carry different resolutionHash values. CI's `pnpm qualy resolve --frozen-lockfile` step (.github/workflows/ci.yml:53) then fails for whichever developer committed, the lock churns on every resolve, and the staged web bundle's .qualy-assembly.json (packages/build/web/src/stage.ts:27) stops matching AssemblyInfo, so a production boot dies with WebUnservable. I confirmed the divergence: with Intl.Collator('da-DK'), ['@qualy/plugin-aa','@qualy/plugin-ab','@qualy/plugin-z'] sorts as ab, z, aa, against byte order aa, ab, z; en-US and sv-SE agree with byte order.
- 证据:packages/plugins/base/rbac/src/assembly/index.ts:74 `.sort(([a], [b]) => a.localeCompare(b))`, whose result becomes `{ order: active.map(([pluginId]) => pluginId) }` (line 85) and is stored verbatim as capabilities.permissions.state in the lock (core/assembly/src/lock.ts:69-75) and hashed into resolutionHash (lock.ts:82-86). Everywhere else in the assembly is locale-free: hash.ts:14 and manifest.ts:171 use `(a < b ? -1 : a > b ? 1 : 0)`, graph.ts:30,40 and resolve.ts:356,372 use bare .sort(), and the database provider's state comes from topoSort. graph.ts:8-9 states the invariant this breaks: 'Two people whose manifests say the same thing must get the same lock, byte for byte.'
- 最小修法:Replace localeCompare with the codepoint comparison used everywhere else in the assembly layer: `.sort(([a], [b]) => (a < b ? -1 : a > b ? 1 : 0))` (runtime-plan.ts:88 has the same localeCompare, which is harmless today because runtime level order is not hashed, but is worth aligning for the same reason).
- **反驳者校正**:The mechanism is exactly as claimed: packages/plugins/base/rbac/src/assembly/index.ts:74 sorts the permissions capability's `state.order` with the process-default ICU collation, that array is stored verbatim in the lock (packages/core/assembly/src/lock.ts:69-75) and hashed into resolutionHash (lock.ts:82-86, with hash.ts:8-18 preserving array order), and it is the only locale-dependent sort that reaches the lock — every other assembly sort is codepoint (resolve.ts:221,356,358; hash.ts:14; the database provider's contribution.ts:109,111 / index.ts:70). I reproduced the divergence: Node 24.18 with LC_ALL=da_DK.UTF-8 resolves its default locale to da-DK and sorts ['@qualy/plugin-aa','@qualy/plugin-ab','@qualy/plugin-z'] as ab, z, aa.

Two refinements to the write-up:

(a) The defect is latent, not live. The committed lock's order is ['@qualy/plugin-assessment','@qualy/plugin-auth','@qualy/plugin-org','@qualy/plugin-rbac'], which is identical to codepoint order and identical under en-US, en-US-POSIX, da-DK, nb-NO, sv-SE, zh-CN, de-DE, cs-CZ and tr-TR collation; the same holds for hyphenated near-collisions in the current naming style. It takes a newly added id whose collation diverges (a Danish/Norwegian 'aa', per the auditor's own example) before any lock churn occurs. LC_ALL=C resolves to en-US and agrees with byte order, so CI is not the diverging side — a developer machine with a Scandinavian locale is.

(b) The first symptom would be a boot refusal, not the web-bundle mismatch. apps/server/src/main.ts:32 runs verifyAssembly on every start, which re-resolves (apps/server/sr…

- **反驳者校正**:The finding stands as stated; one point of precision on impact. The divergence is latent, not active: every plugin id in the current tree ('@qualy/plugin-assessment', '-auth', '-org', '-rbac', per qualy.lock.json capabilities.permissions.state.order) sorts identically under codepoint order and under every locale I probed, so no lock churn or resolutionHash split is happening today. The bug arms itself only when an id containing a locale-reordered digraph is added (verified: da-DK and nb-NO place 'aa' after 'z'), which makes 'minor' the right severity — a latent determinism hole in a file whose sibling module (packages/core/assembly/src/graph.ts:8-9) declares byte-for-byte reproducibility as the invariant. Worth adding: this is a regression, not an original choice. Commit 4edbe1cc ('refactor(assembly): permissions contributions come from the descriptor') replaced the previous locale-free `[...context.contributions.keys()].filter(...).sort()` with the localeCompare comparator while moving contributions to descriptors, so the proposed fix restores the ordering this very file already had.
- 反对票(未采纳):The MECHANISM is real, but the FAILURE is not reachable in this tree, and reaching it requires inventing a package name that does not and would not plausibly exist.

What I confirmed as accurate:

- packages/plugins/base/rbac/src/assembly/index.ts:74 does sort with `a.localeCompare(b)`, and line 85 turns that array into `{ order: ... }`.
- That value is stored verbatim as `capabilities.permissions.state` (packages/core/assembly/src/lock.ts:69-75) and folded into `resolutionHash` (lock.ts:78-86 — the hashed object literally includes `capabilities`).
- Everywhere else that reaches the lock is locale-free: hash.ts:15 `(a < b ? -1 : a > b ? 1 : 0)`, resolve.ts:218-221 `.sort()` for `runtime.plugi…

### [minor · auth-identity] The documented 12-character password minimum is enforced nowhere; the protected recovery account can be provisioned with a one-character password

- 位置:packages/plugins/base/auth-local/src/password.ts:13
- 判定:CONFIRMED,2/3 upheld(第三轮)
- 主张:PASSWORD_MIN_LENGTH is exported and has zero importers anywhere in the repository. The only path in the system that ever sets a credential — the seed's provisionAdmin — hashes whatever QUALY_ADMIN_PASSWORD contains with no length or content check, so the control described in docs/notes/auth-security.md does not exist.
- 失败场景:An operator runs the bootstrap with QUALY_ADMIN_PASSWORD=a (a typo, or a placeholder they mean to change later). seed-cli.ts:19 passes it straight through; seed.ts:407-424 sees no existing identity, and calls hashPassword(options.adminPassword) with no validation, then reports admin: 'created' and exits 0. The account it just created is the system-account user: users.setEnabled refuses to disable it (users.ts:655 SystemAccountProtected), users.update refuses to retype it (users.ts:600), userTypes.update refuses to close its password channel (user-types.ts:388-390 RecoveryChannelRequired), and it carries the canonical tenant-admin role whose permission_mode is 'all-active'. Nothing in the running system ever asks the operator to strengthen it and no later path can rotate it except re-running the seed with QUALY_RESET_ADMIN_PASSWORD, which applies the same absent check (seed.ts:436-441). The login endpoint accepts any password of length >= 1 (auth-local/src/api.ts:40) and answers a uniform INVALID_CREDENTIALS, so the weakness is silent.
- 证据:packages/plugins/base/auth-local/src/password.ts:13 `export const PASSWORD_MIN_LENGTH = 12`; `grep -rn PASSWORD_MIN_LENGTH` over the repo (excluding repos/) returns that single line and no importer. tools/fixtures/seed.ts:407-424 and :436-441 are the only credential-setting sites and neither inspects the string. tools/fixtures/seed-cli.ts:19-20 reads it from the environment verbatim. docs/notes/auth-security.md:28 states "最小长度 12(创建/重置路径,登录输入不做策略校验以免泄露策略)", i.e. the control is claimed on exactly the paths that omit it. packages/plugins/base/auth-local/src/api.ts:40 caps nothing but the minimum of 1.
- 最小修法:Have provisionAdmin (and the demo-password path at seed.ts:535) reject a password shorter than PASSWORD_MIN_LENGTH with the same loud error shape as the existing 'set QUALY_ADMIN_PASSWORD to create it' throw, so the constant is the single source of the rule the note already documents.
- **反驳者校正**:The finding stands, with two refinements to the statement. First, "the only path in the system that ever sets a credential — the seed's provisionAdmin" undercounts: there are three unvalidated credential-writing sites, all inside tools/fixtures/seed.ts — the create path (:424, guard at :407), the reset path (:440-441, guard at :437), and the demo-account path (:535, guard at :526). Each guard tests only that the string is non-empty. The proposed fix already names :535, so it is complete even though the summary sentence is not. Second, exposure is narrower than "the running system": the seed is reachable only through `pnpm seed` → tools/fixtures/seed-cli.ts (package.json:52); grepping importers of fixtures/seed yields only seed-cli.ts and tools/tests/seed.test.ts, so it is not invoked by `qualy deploy`, the CLI lifecycle, or server boot. That confines the weakness to the operator-run bootstrap — but it also means no runtime path exists that could ever enforce the rule, which is precisely why PASSWORD_MIN_LENGTH has no consumer. Severity "minor" is appropriate.
- **反驳者校正**:The claim stands. Two citation corrections and one framing correction:

1. Line numbers are off by one at two of the cited sites. The system-account disable guard is packages/plugins/base/auth/src/server/users.ts:654 (not 655) and the retype guard is users.ts:601 (not 600). user-types.ts:388-390, password.ts:13, api.ts:40, seed.ts:407-424/436-441/535 and seed-cli.ts:19 are all exact.

2. Framing: the defect is a documentation-reality mismatch, not an attacker-reachable hole. The only actor who can supply the weak password is the operator running `pnpm seed` with QUALY_ADMIN_PASSWORD in their own environment; nothing on the network reaches the credential-setting path. The claim's own "minor" severity is right, and its blast-radius reasoning (SystemAccountProtected at users.ts:654/:601, RecoveryChannelRequired at user-types.ts:388-390, tenant-admin permission_mode 'all-active') correctly explains why a weak value here is unusually sticky.

3. The doc side is at least as defensible a fix as the code side. docs/notes/auth-security.md:28 asserts a control that has never existed on those paths; docs/assembly-design.md:977 shows the same 12 as `z.string().min(12)` in the unbuilt `defineSetup` schema. Either wire PASSWORD_MIN_LENGTH into provisionAdmin (seed.ts:407-424), the reset branch (:436-441) and the demo branch (:526-535) as proposed, or amend the note to say the minimum is a design intent that lands with the setup capability. Silently leaving an exported constant with zero importers behind a note that claims enforcement is the part that is wrong.

- 反对票(未采纳):The mechanical facts check out, but the failure is not reachable by any actor the system defends against.

1. Only writer of credentials, and it is a repo-local fixture, not a deployment path. `grep -rn hashPassword` over packages/apps/tools returns exactly four production call sites, all in tools/fixtures/seed.ts (:395 resolve, :424 create admin, :441 reset admin, :535 demo users), plus one test. The seed is reached only through the root script `"seed": "tsx tools/fixtures/seed-cli.ts"` (package.json:47) and it anchors itself to the repository: tools/lib/manifest.ts:7 `repoRoot = path.resolve(fileURLToPath(new URL('.', import.meta.url)), '../..')`, and seed.ts:19-22 resolves `@qualy/plugin-…

### [minor · build-parity] optimizeDeps.entries replaces Vite's html crawl, so apps/web/index.html and main.tsx are never scanned and react-dom/client is discovered mid-run on every cold dev start

- 位置:packages/build/web/src/vite.ts:59
- 判定:CONFIRMED,2/3 upheld(第四轮)
- 主张:The plugin returns `optimizeDeps: { entries: [cacheFile, scanFile] }`. In Vite, a non-empty `optimizeDeps.entries` does not add to the default `**/*.html` crawl, it replaces it. The generated scan twin covers plugin components, i18n modules and browser modules, but nothing covers apps/web/index.html -> src/main.tsx, so any dependency reachable only from the host entry is missed by the cold-start scanner — which is precisely the failure class the file's own comment says destroyed the browser suite.
- 失败场景:Cold cache (fresh clone, CI, or after clearing apps/web/node_modules/.vite): run `pnpm dev`. The scanner crawls only apps/web/.qualy/plugins.ts and scan.ts, so `react-dom/client` — imported at apps/web/src/main.tsx:2 and nowhere else in the graph (the only other react-dom import in the repo is bare `react-dom` in packages/web/ui/src/components/portal.tsx:2) — is not pre-bundled. The dev server discovers it while transforming main.tsx on the first request, runs a re-optimize, and the page full-reloads ("optimized dependencies changed, reloading") before the app has finished mounting.
- 证据:packages/build/web/src/vite.ts:59 sets the entries. node_modules/vite/dist/node/chunks/node.js:31325-31344 `computeEntries`: `if (explicitEntryPatterns) entries = await globEntries(explicitEntryPatterns, …)` / `else … entries = await globEntries("**/*.html", …)` — mutually exclusive. I confirmed with tinyglobby 0.2.17 (the version vite resolves) that the two absolute patterns match exactly the two generated files and that `**/*.html` would have matched /Users/hangqi/Workspace/Web/qualy/apps/web/index.html, which is thus excluded. apps/web/.qualy/scan.ts contains 42 imports, none of them main.tsx, App.tsx or index.html. @vitejs/plugin-react does not cover the gap: node_modules/@vitejs/plugin-react/dist/index.js:148-154 includes only `react`, `react-dom`, `react/jsx-runtime`, `react/jsx-dev-runtime` — not `react-dom/client`. The local optimizer cache confirms `react-dom/client` is a separate optimized entry (apps/web/node_modules/.vite/deps/_metadata.json `optimized` list contains both `react-dom` and `react-dom/client`, and apps/web/node_modules/.vite/deps/react-dom_client.js exists).
- 最小修法:Include the host entry alongside the generated files, e.g. return `entries: [path.join(repoRoot, 'apps/web/index.html'), cacheFile, scanFile]` (or add the html glob back), so the scanner sees both halves of the graph.
- **反驳者校正**:The finding stands; two statements need sharpening.

(a) Mechanism of the reload. The auditor attributes it to discovery "while transforming main.tsx on the first request", which by itself would normally be absorbed without a reload — holdUntilCrawlEnd defaults to true (vite/dist/node/chunks/node.js:36225) and onCrawlEnd's `scannerMissedDeps` path (node.js:34515-34527) re-runs with `metadata.optimized` still empty, so `needsReload` is false. The reload is guaranteed by timing instead: in middlewareMode the optimizer is initialised inside `vite.createServer` at packages/plugins/infra/web/src/server/index.ts:241 (node.js:26265 → 26565 → 26548 → 35389), and `waitForRequestsIdle` resolves 50 ms later with no requests pending (node.js:35462, 35489-35496). The scan result is therefore already committed before any browser connects, so react-dom/client arrives as a genuine rerun, and that rerun changes an already-optimized file's content — react-dom.js gains `export { require_react_dom as t }` because react-dom_client.js imports it — making `needsReload` true (node.js:34385-34387) and firing `fullReload()` (node.js:34438-34452).

(b) Scope. This is not "every cold dev start" as the title says. `loadCachedDepOptimizationMetadata` (node.js:34283) short-circuits the whole path once apps/web/node_modules/.vite/deps/_metadata.json lists react-dom/client — which it currently does. The symptom is one extra optimize round plus one full reload on the first page load after a fresh clone, CI, a cleared .vite cache, or a configHash/lockfileHash change. Production builds are unaffected: dep opt…

- **反驳者校正**:Real and unhandled, with two narrowings. (1) Scope is `pnpm dev` only, not the browser suite: vitest browser mode enters through its own tester html under node_modules, and globEntries (node_modules/vite/dist/node/chunks/node.js:31378-31384) ignores node_modules for non-node_modules patterns, so the default `**/*.html` crawl never covered the test path either — the seven-test incident recorded at packages/build/web/src/vite.ts:14-20 and STATUS.md:1936-1942 was caused by the unscanned aggregate, not by the host entry, so this is an adjacent gap rather than a recurrence of that failure class. `vite build` is also unaffected (the scanner is a dev-server mechanism). (2) The visible symptom is a cold-start re-optimization; the full page reload is likely but not guaranteed. node.js:34386-34440 reloads only when `metadata.hash !== newData.hash` or some already-optimized dep's `fileHash` changed — bundling react-dom/client alongside the already-optimized react-dom plausibly re-splits their shared chunk and trips that, but if no existing fileHash moves, Vite commits the new dep with the old browserHash and only logs it. The single concrete consequence that is certain: `react-dom/client`, the only host-entry-only dependency in the repo (apps/web/src/main.tsx:2), is invisible to the cold-start scanner and is always discovered during the first transform of main.tsx. The proposed fix (adding apps/web/index.html to the entries array at packages/build/web/src/vite.ts:59) addresses it; adding `react-dom/client` to optimizeDeps.include would too, but only the entries fix keeps future host-e…
- 反对票(未采纳):MECHANISM: partly confirmed, but the harm does not follow.

What I confirmed myself:

1. `packages/build/web/src/vite.ts:59` returns `optimizeDeps: { entries: [cacheFile, scanFile] }`, and `apps/web/vite.config.ts:7` sets no other entries, so the plugin's two files are the whole entry list for dev.
2. Vite 8.2.0 `computeEntries` (node_modules/vite/dist/node/chunks/node.js:31325-31344) is genuinely mutually exclusive: `if (explicitEntryPatterns) … else if (input) … else globEntries("**/*.html")`. So apps/web/index.html is not crawled.
3. `apps/web/src/main.tsx:2` is the only importer of `react-dom/client` in the repo (the sole other react-dom import is bare `react-dom` at packages/web/ui/src/c…

### [minor · build-parity] A release bundle is a plugin superset but is fingerprinted with the active-selection resolutionHash, so enabling or disabling a plugin makes production refuse to boot on a byte-identical bundle

- 位置:packages/build/web/src/stage.ts:27
- 判定:CONFIRMED,2/3 upheld(第四轮)
- 主张:vite.ts builds release assets with `all: true` specifically so that "toggling a plugin on does not require rebuilding the assets", but stage.ts stamps the bundle with `lock.resolutionHash`, and that hash is computed over each plugin's `state` (active/disabled). Flipping a plugin's `enabled:` changes the hash while leaving the superset bundle's content identical, and the web plugin then dies at boot rather than serving assets that are in fact correct.
- 失败场景:A deployment ships client-dist built while `@qualy/plugin-storage-cos` is `enabled: false` (the committed qualy.yml:34-35). The operator moves the deployment to object storage: sets `enabled: true`, runs `pnpm qualy resolve`, restarts `pnpm start`. The staged bundle already contains cos's browser assets — it was built with `all: true`, and apps/web/.qualy/plugins.all.ts differs from plugins.ts by exactly that one import. But lockFromResolution now yields a different resolutionHash because `plugins['@qualy/plugin-storage-cos'].state` went from 'disabled' to 'active', so packages/plugins/infra/web/src/server/index.ts:147 sees a mismatch and Effect.die's with WebUnservable. The process never binds its port: the whole application, api included, is down until someone rebuilds the frontend on the box.
- 证据:packages/build/web/src/vite.ts:36-39 and its comment ("a release build carries the superset, so toggling a plugin on does not require rebuilding the assets"); packages/build/web/src/stage.ts:19-28 writes `{ resolutionHash: lock.resolutionHash }`; packages/core/assembly/src/lock.ts:59-66 and 81-86 show `state` is part of the `plugins` record that `canonicalHash` covers; packages/plugins/infra/web/src/server/index.ts:135-153 turns any difference into an Effect.die before the port binds (packages/plugins/infra/web/tests/effect-web.test.ts:139 pins that behaviour). CI never exercises the divergence because .github/workflows/ci.yml runs `qualy resolve`, `pnpm build` and the smoke boot in one job, so the two hashes are always equal there.
- 最小修法:Fingerprint the superset the bundle actually carries rather than the active selection — e.g. have stage.ts hash the sorted set of installed plugin ids/versions that collectWebPlugins({all:true}) walked, and have production() compare that against the same derivation from its own resolution. Alternatively drop the superset build and build the active set, so the bundle and the hash describe the same thing.
- **反驳者校正**:The finding stands, with two corrections.

(a) Mechanism, corrected on the port: the process DOES bind its port before it dies. apps/server/src/runtime.ts:70-88 creates the http.Server itself and registers a 503 "qualy is starting" request listener before the bind, exactly because — as its own comment at runtime.ts:58-68 says — upstream binds during layer construction and attaches the real handler only later. Confirmed upstream: repos/effect/packages/platform-node/src/NodeHttpServer.ts:93-127 (`make` calls `server.listen(...)` while the layer builds) and repos/effect/packages/effect/src/unstable/http/HttpRouter.ts:1222,1262-1266 (`serve` = `Layer.provideMerge(appLayer)` over an effect requiring HttpServer, so the server layer provided at runtime.ts:135 builds first). The sequence is therefore: port bound and answering 503 → routes layer builds → packages/plugins/infra/web/src/server/index.ts:147 sees the hash mismatch and `Effect.die`s → Layer.launch fails → NodeRuntime.runMain teardown (apps/server/src/main.ts:110-116) exits 1. Net effect is what the auditor claimed — the whole application, api included, is unavailable — but by crash-after-bind, not by never binding.

(b) Framing: this is not an overlooked hole, it is two committed intents contradicting each other, and the finding should name both. The exact-fingerprint guard is the documented design (CLAUDE.md:65, docs/plugin-descriptor-plan.md:149 "前端产物指纹 ... web 插件 production 拒绝错配/无指纹"). The superset promise is documented just as explicitly in packages/build/web/src/vite.ts:36-39 and pinned by tools/tests/generators.tes…

- **反驳者校正**:The claim is accurate as stated; three refinements worth carrying forward.

1. The operator sequence is not avoidable. The claim presents `pnpm qualy resolve` as one step the operator happens to take, but it is mandatory: without it, production refuses to boot on lock drift anyway (packages/core/assembly/src/lock.ts:223 compares `lock.resolutionHash` to the fresh resolution; CLAUDE.md's "start 只校验不修复" makes that a hard failure in production unless QUALY_FROZEN_LOCKFILE=0). So resolve-then-restart is the only correct path, and the web fingerprint is the sole remaining blocker on it — there is no operator workaround short of rebuilding the frontend on the box.

2. The severity framing "minor" understates the deployment consequence in one respect the claim does not name: a production image serves staged assets with sirv and has no need for vite, so a runtime image that ships without the frontend toolchain cannot rebuild in place at all. Whether that matters depends on the image (no Dockerfile exists in the repo yet), so this is a caveat rather than a confirmed escalation.

3. The claim reads as a code-level oversight, but it is better stated as an unresolved contradiction already present in the repo's own documents: packages/build/web/src/vite.ts:36-38 and docs/PLAN.md:57-58 promise zero-rebuild enable/disable via the superset bundle, while CLAUDE.md:65 and packages/plugins/infra/web/src/server/index.ts:147 enforce "前后端是同一装配的两次构建". Both cannot hold. Fixing the fingerprint (hash the installed id/version set the superset walked) satisfies both; the alternative — building the act…

- 反对票(未采纳):The mechanism is factually correct but the failure is unreachable in any deployment this repo supports, and the behaviour it calls a bug is an ADR-frozen invariant.

MECHANISM CONCEDED. `lockFromResolution` writes `state: plugin.state` into the `plugins` record (packages/core/assembly/src/lock.ts:61-67) and hashes that record into `resolutionHash` (lock.ts:82-86). `readEntries` admits `disabled` plugins only under `all` (packages/core/assembly/src/host.ts:76), and packages/build/web/src/vite.ts:39 sets `all = environment.command === 'build'` unconditionally, so the release dist is manifest-independent while packages/build/web/src/stage.ts:27 stamps it with the selection-dependent hash. tools…

### [minor · fresh-fixes] appealReview evaluates the phase/appeal gate outside the transaction, against the ordering its own commit recorded as the ruling

- 位置:packages/plugins/assessment/core/src/review/service.ts:1490
- 判定:CONFIRMED,3/3 upheld(第四轮)
- 主张:8d1db5f9 moved the instance/entry read under the batch lock and wrote §32.75 spelling out the order as `locate → transaction → lock batch → 重读 → 确认是当前结论 → phase/appeal gate → 建 appeal round`, but left `deps.authorize(as, 'assessment.entry.appeal', ...)` before the transaction, so the appeal window is checked on a pre-lock reading.
- 失败场景:A student clicks 'appeal' as the appeal phase is about to expire.

1. `appealReview` evaluates `deps.authorize(as, 'assessment.entry.appeal', located.batchId, {participantId})` at line 1490 — outside any transaction. The appeal phase is still current, so `decision.allowed` is true.
2. The minute scheduler runs `sweepDueBoundaries` (server/index.ts:3780-3795), takes `lockBatch` on this batch, ratifies the due boundary so the next phase's profile no longer contains `assessment.entry.appeal`, and commits.
3. The appeal transaction then acquires the batch lock at line 1500 and proceeds on the stale verdict: it inserts a new escalation-route ReviewInstance and runs `setEntryState(from: ['approved','rejected'], to: 'in_review', currentReviewInstanceId: opened)` (lines 1631-1637).
   Outcome: an appeal round is opened after the appeal window closed, and a settled `approved` entry stops counting toward the student's score — the phase gate's whole purpose defeated. decideReview does not have this hole: its `deps.reviewGate` call sits at line 979, inside the lock.

- 证据:review/service.ts:1490-1495 is outside `withDb(transaction(...))`, which begins at 1497 with `lockBatch` at 1500. `assessment.entry.appeal` is phase-gated: phase/gate.ts:25 puts it in `ENTRY_FAMILY`, and `gateAllows` refuses it when the current profile does not list it. Phase boundaries are ratified under the batch lock at server/index.ts:3787 (`const locked = yield* lockBatch(candidate.tenantId, candidate.id)`), which is what makes the interleaving reachable rather than theoretical. The ruling the same commit added, docs/assessment-design.md §32.75, states: "校验位置在事务里、批次锁之后的重读上 ... 顺序为 locate → transaction → lock batch → 重读 instance + entry → 确认是当前结论 → phase/appeal gate → 建 appeal round".
- 最小修法:Move the `deps.authorize` call inside the transaction, after `lockBatch` and the re-read, next to the `decision-superseded` check — matching decideReview's `reviewGate` placement and §32.75.
- **反驳者校正**:Real defect, wrong mechanism and overstated fix.

Correct statement: `appealReview` evaluates the phase gate on a pre-lock snapshot, in violation of the ordering its own commit recorded (docs/assessment-design.md:1465-1467) and against the house pattern of every sibling participant write path (entry/service.ts:752, 907, 986 all call `deps.authorize` after `lockBatch`; `decideReview`'s `deps.reviewGate` is at review/service.ts:981 inside the lock taken at 954). `deps.authorize` sits at review/service.ts:1490-1495, ahead of the transaction opened at 1497 and the `lockBatch` at 1500.

The interleaving that makes this reachable is NOT the minute scheduler. `sweepDueBoundaries` (server/index.ts:3775-3797) only runs `ratifyPending` (1068-1091), which writes `setPhaseActual` with the _planned_ instant (phase/engine/queue.ts:64-66, 86); `gateView` (server/index.ts:1356-1367) never reads the `current_phase` pointer and recomputes the phase from `effectiveState(plan, now)` (queue.ts:81-90), which already walks unratified crossed boundaries. queue.ts:3-8 says so outright: the clock, not the scheduler, decides the phase. So the gate answers identically before and after the sweep, and step 2 of the scenario cannot flip `decision.allowed`.

The writers that do flip it under the same batch lock are `replacePlan` (server/index.ts:2818, lock at 2822), which may strip `assessment.entry.appeal` from the current phase's `permissionProfile` or narrow its participant scope (phase/engine/edits.ts:193-195 restricts `set-profile` only for ended phases), and `advancePhase` (3105, lock at 3110), whic…

- **反驳者校正**:The ordering deviation is real: appealReview evaluates the phase/appeal gate at review/service.ts:1490-1495, before the transaction and batch lock that begin at 1497/1500, whereas docs/assessment-design.md:1466-1468 states the order as locate -> transaction -> lock batch -> re-read -> current-conclusion check -> phase/appeal gate -> open appeal round. Everything else in the report should be restated.

The scheduler cannot cause this. sweepDueBoundaries is answer-preserving with respect to the gate: gateView -> effectivePhaseIndex -> effectiveState(plan, now) (phase/engine/queue.ts:120-130) advances past any boundary whose plannedEntryAt <= now whether or not actualEntryAt has been written, and ratifyPending writes the planned instant, not now (server/index.ts:1078, queue.ts:65, 125). queue.ts:3-7 and phase/scheduler.ts:6-14 say so directly. Step 2 of the failure scenario is impossible: if the profile no longer lists assessment.entry.appeal after the sweep, it did not list it before, and the pre-lock authorize would already have refused.

decideReview has the same property, not a better one. Its reviewGate (server/index.ts:1913-1919) samples Clock.currentTimeMillis itself; the batch lock serializes concurrent writers, not the passage of time, so a scheduled boundary can cross between that gate read and the commit exactly as in appealReview. There is no soundness asymmetry, only a difference in window width.

The only reachable interleaving is an administrator write, not a timer: advancePhase holds lockBatch (server/index.ts:3110) and writes setPhaseActual(..., now) at 3155,…

- **反驳者校正**:The claim is correct in substance; three refinements. (1) Line numbers: the authorize call spans review/service.ts:1490-1495 (`.authorize(...)` on :1491); decideReview's comparison gate is at review/service.ts:981, not 979 (its lockBatch is at :954). (2) The stale evaluation is broader than the phase gate: authorizeAction evaluates all of layer one (batch authority / active-participant membership, server/index.ts:1857-1859) and layer two (the phase gate, :1869-1875) pre-lock, so a roster removal, a participant deactivation, or a revoked batch-accepted permission committed in the same window is equally missed — the in-transaction re-read only catches `participant.status !== 'active'` (review/service.ts:1541-1544), not the authority layer. (3) The strongest evidence is not only §32.75's prose but the divergence from every sibling: entry/service.ts evaluates the identical deps.authorize inside the locked transaction at :752, :907 and :986 (locks at :721, :877, :965), making appealReview the sole participant-action path that gates on a pre-lock reading. Severity `minor` is defensible on window size, but the fix is a pure move of lines 1490-1495 to just after the `decision-superseded` check at review/service.ts:1531, which is exactly what §32.75 already ordered.

### [minor · fresh-fixes] A sitting that refused the claim is recorded as having escalated it

- 位置:packages/plugins/assessment/core/src/review/service.ts:1263
- 判定:CONFIRMED,2/3 upheld(第四轮)
- 主张:962b98c7 added a terminal branch that turns a split panel vote at the resolved end of the ladder into a rejection, but it runs after `resolvePanel` has already written `resolution = 'escalated'`, so the panel row permanently claims it handed the matter up while the round it belongs to is completed `rejected`.
- 失败场景:Escalation route with stages [college(panel, quorum all), university]. For this participant the `university` stage's selector resolves to no unit, so `nextAfter` steps over it and `isRouteEnd(policy, college)` is true. The three college panel members vote approve/approve/reject. At line 1258 the seat count is met, `unanimous` is false, so line 1260-1264 writes `review_panels.resolution = 'escalated'` and `state = 'resolved'`. Control then falls to the new branch at 1281, which completes the instance with `outcome: 'rejected'` and moves the entry to `rejected`.
  Outcome: the persisted record of the sitting contradicts what the sitting did — `review_panels` says the panel escalated, `review_instances` says the round was refused, and nothing ever escalated. Any read of `review_panels.resolution` (the only place the sitting's own conclusion is stored; the votes say who said what, not what the sitting resolved) reports this refusal as an escalation.
- 证据:review/service.ts:1260-1264 unconditionally resolves the panel as `unanimous ? 'approved' : 'escalated'` before the branch added at 1281-1310 decides that the split is actually the refusal. `resolvePanel` (review/db.ts:2229-2240) types the field as `'approved' | 'escalated'`, and db/entities.ts:1249 documents that value set; `chk_review_panels_lifecycle_shape` (entities.ts:1253-1254) only requires the column be non-null when resolved, so nothing rejects the wrong value. The sibling escalate path at 1220-1231 writes `'escalated'` for an actual escalation, which is what makes the two indistinguishable afterwards.
- 最小修法:Decide the resolution before writing it: compute the terminal case first and pass a third value (e.g. `'rejected'`, widening `resolvePanel`'s input type and the documented value set) instead of resolving as `'escalated'` and then contradicting it.
- **反驳者校正**:The sequencing defect is real: for a split panel vote at a resolution-determined route end, service.ts:1260-1264 persists review_panels.resolution = 'escalated' before service.ts:1281-1304 completes the round as 'rejected', and nothing rewrites the row (completeInstance dissolves only state='open' panels, entry/db.ts:709-720), so the panel row permanently records an escalation that never happened. But the claimed consequences are wrong on two counts, which drops this to cosmetic/data-hygiene rather than a misreported outcome: (1) for a resolved panel the resolution column is write-only in this codebase — openPanelOf (review/db.ts:2019-2052) reads it only for state='open' rows, where chk_review_panels_lifecycle_shape forces it NULL, and resolvedPanelOpinions (review/db.ts:2276-2295) selects votes without it, so no query, API projection, or UI can currently surface the wrong value; (2) it is not the only place the sitting's conclusion is stored — the terminal branch writes a review_events row via sayFrom('rejected', null) (service.ts:1297) carrying that panel's own route and stageId (service.ts:1082-1090), and the trail and chain views read review_events (review/db.ts:711-749, 1161), so what a reader sees for that sitting is correctly 'rejected'; the 'escalated' event kind is written only on the climbing branch (service.ts:1309). The proposed fix (decide the resolution first, widen resolvePanel's input to include 'rejected', and update the documented value set at db/entities.ts:1248-1251) is still the right shape, but it corrects an unread column rather than a user-visible or…
- **反驳者校正**:The defect is real but its stated impact is overstated on two counts, both of which I verified by reading.

(1) "Any read of `review_panels.resolution` … reports this refusal as an escalation" — no production code reads it. `grep -rn resolution` over `packages/plugins/assessment/**/src` yields only the two writes (service.ts:1263, review/db.ts:2238) and one read: `openPanelOf` (review/db.ts:2029 selects the column, 2049 maps it), which filters `.where('state', '=', 'open')` (review/db.ts:2032) — so the value it can return is always null by construction. The one query that does read resolved panels, the chain-opinions query at review/db.ts:2286-2296, joins `review_panels p … where p.state = 'resolved'` but selects only `p.stage_id` and the vote columns, never `p.resolution`. Today the column is write-only.

(2) "the only place the sitting's own conclusion is stored; the votes say who said what, not what the sitting resolved" — the sitting's conclusion is also stored, correctly, in `review_events`. service.ts:1297 calls `sayFrom('rejected', null)`, and `sayFrom` (service.ts:1082-1090, commented "the round's own word, spoken by no one: a sitting concluding") writes an event carrying `route: here.route, stageId: here.id` and `actorId: null` — the panel's own stage. That is the same channel the genuine escalation uses and the same one review-panel.test.ts:324-326 asserts on (`climbed.kind === 'escalated'`, `climbed.actorId === null`). So the audit trail does record "the sitting at this stage refused"; the two records are not indistinguishable, they merely disagree, and the event…

- 反对票(未采纳):The write sequence is as described, but the harm is not: `review_panels.resolution` is a write-only column — no production code path ever reads it for a resolved panel, and the sitting's conclusion is in fact stored (correctly) somewhere else.

1. The mechanism exists. `/Users/hangqi/Workspace/Web/qualy/packages/plugins/assessment/core/src/review/service.ts:1260-1264` writes `resolution: unanimous ? 'approved' : 'escalated'` before the terminal branch at :1281 (`else if (isRouteEnd(policy, here))`) completes the instance `rejected` (:1291-1303). And it is reachable: `isRouteEnd` is resolution-based, not positional — `/Users/hangqi/Workspace/Web/qualy/packages/plugins/assessment/core/src/revi…

### [minor · org-tree] Deleting an org type that a user type's placement allow-list still names returns a 500 instead of ORG_TYPE_IN_USE

- 位置:packages/plugins/base/org/src/server/errors.ts:152
- 判定:CONFIRMED,3/3 upheld(第三轮)
- 主张:typeConstraints maps fk_role_allowed_org_types_type but not fk_user_type_allowed_org_types_org_type, and deleteType only pre-checks nodes and rules. A type still listed in a user type's placement allow-list therefore reaches the database, raises the RESTRICT violation under an unmapped constraint name, and writeAtRoot dies on the unmapped QueryFailed.
- 失败场景:A tenant administrator creates org type 教研室, then edits the 学生 user type's placement policy to allow-list mode including 教研室 (auth's create/update path accepts any existing org type; it never requires the type to have nodes — packages/plugins/base/auth/src/server/user-types.ts:329-336 only checks countOrgTypes). No org node of that type is ever created. The administrator later removes the type: DELETE /api/org/types/{id}. org's deleteType passes typeHasNodes (no nodes) and typeHasRules (no rules), issues the DELETE, and postgres raises SQLSTATE 23001 on fk_user_type_allowed_org_types_org_type. translateConstraints(typeConstraints) has no entry for that name, so the failure stays a QueryFailed and Effect.catchTag('QueryFailed', die) turns it into a 500 Internal Server Error. The caller gets an opaque server error instead of the 409 ORG_TYPE_IN_USE the same code path produces when a role's allowed types hold the type, and nothing in the response says which policy still references it.
- 证据:packages/plugins/base/org/src/server/errors.ts:152-157 — typeConstraints = { uq_org_types_tenant_code, uq_org_types_tenant_name, fk_role_allowed_org_types_type }; the comment at 127-137 states the intent explicitly ("has to arrive as the same domain error a pre-check would have raised, not as a 500"). packages/plugins/base/org/src/server/index.ts:717-734 — deleteType pre-checks only typeHasNodes and typeHasRules. packages/plugins/base/org/src/server/index.ts:418-424 — writeAtRoot pipes translateConstraints(typeConstraints) then Effect.catchTag('QueryFailed', (error) => Effect.die(error)); packages/plugins/infra/database/src/server/constraints.ts:73-77 documents that anything the map does not name stays exactly what it was. db/migrations/20260806105442_initial.sql:229 declares the key, and I confirmed it on the running database: `select conname, confdeltype from pg_constraint where conrelid='user_type_allowed_org_types'::regclass and contype='f'` → fk_user_type_allowed_org_types_org_type | r.
- 最小修法:Add `fk_user_type_allowed_org_types_org_type: () => new TypeInUse({ reason: 'user types still allow this org type' })` to typeConstraints in packages/plugins/base/org/src/server/errors.ts, and add the corresponding name to the constraint-name gate's expectations.
- **反驳者校正**:The defect is real as described, with two wording fixes. First, the SQLSTATE is 23503 (ERRCODE_FOREIGN_KEY_VIOLATION, what PostgreSQL's ri_restrict raises on a blocked delete), not 23001; this is immaterial because packages/plugins/infra/database/src/pg-errors.ts:15 puts 23505, 23503 and 23001 all in TRANSLATABLE, so constraintOf still extracts the name and the failure is purely the missing map key. Second, the gap is broader than the user-type allow-list: fk_org_nodes_org_type (db/migrations/20260806105442_initial.sql:133) is also ON DELETE RESTRICT into org_types and also absent from typeConstraints, so the exact race the comment at packages/plugins/base/org/src/server/errors.ts:127-134 says the map exists for — a node of that type inserted between typeHasNodes and the DELETE — dies as a 500 too. A complete fix adds both names (fk_user_type_allowed_org_types_org_type -> TypeInUse{reason:'user types still allow this org type'}, fk_org_nodes_org_type -> TypeInUse{reason:'nodes still use this org type'}), and note that tools/tests/constraint-names.test.ts is only a one-directional existence gate, so it will not prevent the next omission.
- **反驳者校正**:The claim's mechanism, site, and failure scenario are accurate as stated. Two adjustments.

(a) The second half of the proposed fix is unnecessary. tools/tests/constraint-names.test.ts:47-59 is directional: it scans translator maps for keys matching /^\s+(uq|fk|chk|idx)_[a-z0-9_]+:/ and asserts each name appears somewhere in db/migrations. Adding `fk_user_type_allowed_org_types_org_type: () => new TypeInUse({ reason: 'user types still allow this org type' })` to typeConstraints (packages/plugins/base/org/src/server/errors.ts:152-157) is therefore self-registering — there is no separate expectation list to update.

(b) Found while verifying: fk_org_nodes_org_type (db/migrations/20260806105442_initial.sql:133) is also ON DELETE RESTRICT and also absent from typeConstraints, so deleteType's typeHasNodes pre-check has the same unmapped-500 exposure under a concurrent node insert. That variant needs a race, so it is thinner than the allow-list case, but it is exactly the class of violation errors.ts:127-137 says the map exists to cover, and both names should go in together.

- **反驳者校正**:The defect is real and correctly diagnosed; two secondary details in the report are wrong.

1. The proposed fix's second half is unnecessary. There is no manual expectation list in the constraint-name gate. tools/tests/constraint-names.test.ts discovers translated names by regex over every non-test .ts under packages/plugins — `/^\s+((?:uq|fk|chk|idx)_[a-z0-9_]+):/gm` at :40 — and asserts only that each discovered name appears somewhere in db/migrations. Adding `fk_user_type_allowed_org_types_org_type` to typeConstraints is picked up automatically, and the name does exist in the lineage (20260806105442_initial.sql:229), so the gate stays green with no other edit. Note also that this gate is one-directional by construction: it verifies translated-implies-exists, never exists-implies-translated, which is precisely why it could not have caught this omission.

2. The SQLSTATE is stated wrong but harmlessly. PostgreSQL raises 23503 (foreign_key_violation), not 23001 (restrict_violation), for a plain ON DELETE RESTRICT foreign key. It does not change the outcome: packages/plugins/infra/database/src/pg-errors.ts:15 defines TRANSLATABLE as {23505, 23503, 23001}, so constraintOf extracts the constraint name under either code, and the failure is the unmapped key, not an unrecognized sqlstate.

3. Addendum the report missed. fk_org_nodes_org_type (20260806105442_initial.sql:133) is the third `on delete restrict` FK referencing org_types and is likewise absent from typeConstraints. The typeHasNodes pre-check at index.ts:725 covers the sequential case, but the map's own doc comment at e…

### [minor · org-tree] A node write succeeds and then answers 404, when the caller holds org.tree.manage but not org.tree.read

- 位置:packages/plugins/base/org/src/server/index.ts:850
- 判定:CONFIRMED,3/3 upheld(第三轮)
- 主张:updateNode, changeNodeType and setNodePlacement commit their write and then build the response with org.readNode, which resolves scope for org.tree.read. The two codes are independent entries in the catalog with no implication between them, so a manage-only principal commits the change and receives ORG_NODE_NOT_FOUND.
- 失败场景:A tenant administrator composes an org role that carries only org.tree.manage (the catalog offers the two codes independently — packages/plugins/base/org/src/permissions.ts:5-16 declares org.tree.read and org.tree.manage with nothing linking them) and grants it at a college with subtree coverage. The holder renames a class: PATCH /api/org/nodes/{id}. org.updateNode takes the tenant lock, re-checks requireAt(org.tree.manage) and commits the rename. The handler then calls org.readNode, whose first act is resolveScope(tenantId, as, 'org.tree.read'); the caller has no org.tree.read anywhere, so readScope has no anchors, coveredBy is false, and readNode fails with NodeNotFound. The API answers 404 ORG_NODE_NOT_FOUND for a write that is already durably committed. The browser treats it as a failed request and leaves the stale name on screen; repeating the request repeats the 404 forever. changeNodeType (index.ts:839) and setNodePlacement (index.ts:886) have the same shape, so a retype or a move reports "node not found" after actually retyping or moving the subtree.
- 证据:packages/plugins/base/org/src/server/index.ts:843-853 — the updateNode handler calls org.updateNode then `toNodeDto(yield* org.readNode(principal.tenantId, params.nodeId, principal))`; identical read-backs at 838-840 (changeNodeType) and 885-887 (setNodePlacement). packages/plugins/base/org/src/server/index.ts:613-622 — readNode resolves scope for 'org.tree.read' and returns NodeNotFound when `!coveredBy(readScope, node)`, with the deliberate comment that not-found and not-covered are indistinguishable. packages/plugins/base/org/src/permissions.ts:5-16 — the two codes are separate PermissionDefinitions; nothing in packages/plugins/base/rbac/src/server/authorization.ts grants one from the other. The createNode handler at index.ts:863-872 does NOT have the problem, because it builds the DTO from the row insertNode returned rather than reading it back.
- 最小修法:Build the response from the row the write already has, the way createNode does, rather than re-reading through the read permission: have updateNode/changeNodeType/moveNode return the updated NodeRow from inside the transaction and have the handlers project it with the manage-derived flags. Alternatively make the read-back use the manage scope the write just proved, so the response can never contradict a committed write.
- **反驳者校正**:The read-back defect is real, but it is API-only and the claimed browser symptom is impossible.

Confirmed: updateNode (packages/plugins/base/org/src/server/index.ts:848-851), changeNodeType (:835-840) and setNodePlacement (:878-887) commit their write and then build the response with org.readNode, whose first act resolves scope for 'org.tree.read' (:615) and which returns NodeNotFound when the node is not read-covered (:619). The write itself proves only 'org.tree.manage' (:330, :340 for write(); :561-562 for moveNode), the two codes are independent catalog entries (packages/plugins/base/org/src/permissions.ts:5-16) with no implication in authorizedScope's per-code `carries` predicate (packages/plugins/base/rbac/src/server/authorization.ts:230-271), and the write is already committed when the read-back runs because `transaction` is JOIN-EXISTING with commit-on-success (packages/plugins/infra/database/src/server/orm.ts:104-119) and nothing wraps the request in an outer transaction. So a principal holding manage-but-not-read at the target node gets a durable write answered with 404 ORG_NODE_NOT_FOUND (declared on all three endpoints: packages/plugins/base/org/src/api.ts:89, :110, :223).

Corrected on impact: this cannot be triggered from the UI. The org page is only delivered to principals holding org.tree.read (packages/plugins/base/org/src/index.ts:33, authorizer at packages/plugins/base/rbac/src/server/index.ts:532-537), and every node readForest returns is inside readScope by construction (index.ts:651, :661-667), so any node the tree can display passes the same coveredB…

- **反驳者校正**:Real, but the trigger is a direct API caller, not the browser. updateNode, changeNodeType and setNodePlacement (packages/plugins/base/org/src/server/index.ts:838-887) commit under org.tree.manage and then read the row back through org.readNode, which refuses on org.tree.read (index.ts:613-621), so a principal holding a manage-only org role (constructible: packages/plugins/base/rbac/src/server/roles.ts:710-750 and RoleEditor.tsx:259 impose no read companion) receives 404 ORG_NODE_NOT_FOUND for a durably committed rename, retype or move. The claimed UI symptom is unreachable: the org tree page is gated on org.tree.read (packages/plugins/base/org/src/index.ts:33), so such a principal never sees the page, and readForest (index.ts:645-652) never surfaces nodes outside the read scope, so no in-app flow can issue the request. The fix stands as proposed (project the row the write already holds, as createNode does at index.ts:863-871).
- **反驳者校正**:The defect is real at the API layer, but two things in the write-up need fixing, one narrowing and one widening.

NARROWING — the browser half of the failure scenario is not reachable through the product. packages/plugins/base/org/src/index.ts:33 declares the org tree page with `visibility: permissionOf('org.tree.read')`, so a manage-only principal never receives that page in their manifest and has no screen on which a stale name could sit. There is no other org client surface: `grep -rln "org" apps/web/tests/` finds only shell/identity/batch-admin/item-chain, none of which mount the tree editor. So "the browser treats it as a failed request and leaves the stale name on screen" is wrong — the reachable consequence is a direct API caller (or any future non-page client) receiving 404 ORG_NODE_NOT_FOUND for a write that already committed, i.e. a contract violation where the response contradicts durable state. Every role that can actually see the editing UI holds org.tree.read, which is why no test and no user has hit this. Minor severity is right, arguably generous.

WIDENING — the same shape occurs a fourth time, with a different error code, and the write-up misses it. packages/plugins/base/org/src/server/index.ts:927-934, the `updateType` handler, commits via `org.updateType` (which goes through `writeAtRoot`, gated on org.tree.manage at the root) and then builds its response from `org.listTypes`. packages/plugins/base/org/src/server/index.ts:681-688 shows `listTypes` opens with `if (!(yield* rbac.hasPermission(as, 'org.tree.read'))) return yield* new AccessDenied(...)`. Acc…

### [minor · phase-engine] Phase instants are entered and displayed in the viewer's device timezone, never the batch's declared calendar

- 位置:packages/plugins/assessment/core/src/client/batch/when.ts:23
- 判定:CONFIRMED,3/3 upheld(第三轮)
- 主张:assessment_batches.timezone is stored, defaulted, check-constrained and returned by the API, but no browser code ever consults it: the stage timeline renders instants with toLocaleDateString/toLocaleTimeString and the schedule picker builds them with setHours/toISOString, both of which use the device zone. The wall-clock a round announces is therefore whatever zone the reader's or scheduler's machine happens to be in.
- 失败场景:Batch timezone is 'Asia/Shanghai' (UTC+8). An administrator whose workstation is on UTC opens the schedule dialog and picks 5 Sept 23:59 as the submission deadline: date-time-picker.tsx:63-71 does next.setHours(23,59) in the device zone and toISOString(), so plannedEntryAt is stored as 2026-09-05T23:59Z — 6 Sept 07:59 in Shanghai. The gate opens and closes eight hours later than intended, and every student sees a different wall clock again (their own device's) when when.ts:23-34 formats it. The reverse mismatch closes the window early: a student on a UTC-set laptop reads the 5 Sept 23:59 Shanghai deadline as 5 Sept 15:59, plans against it, or trusts a 5 Sept 23:59 render that has in fact already passed. The server side is careful about exactly this — decisionsToday/votesToday compute the day with date_trunc('day', now() at time zone $tz) at time zone $tz (review/db.ts:1256-1258, 1284-1287) because 'a day is a timezone's, and only a batch has one' — so the two halves of the product disagree about what day and hour a boundary falls on.
- 证据:client/batch/when.ts:23-36 — day(), time(), moment() and sameDay() all use the device zone; there is no timeZone option. packages/web/ui/src/components/date-time-picker.tsx:61-71 — withDate/withTime use new Date(...).setHours(...) and toISOString(), i.e. device-zone parsing. `rg -n "timeZone" packages apps` returns no hits anywhere in the repo, so batch.timezone (api.ts:137, db/entities.ts:52, exposed on BatchDetail at index.ts:1017) is inert on the client. docs/assessment-design.md:154 rules the opposite: 「阶段等流程时间一律 timestamptz，按 batch.timezone 展示」.
- 最小修法:Thread batch.timezone into the formatter and the picker (Intl.DateTimeFormat with timeZone, and compose the chosen local wall time in that zone before converting to an instant), so a stage boundary means the same minute to whoever schedules it and whoever reads it.
- **反驳者校正**:The claim is correct on mechanism and should stand, with four sharpenings — three of which make it larger, not smaller.

1. The blast radius is much wider than the two files cited. Every instant renderer in the assessment client is device-zone with no `timeZone` option: `client/phase/PhaseRow.tsx:59`, `client/batch/BatchProgress.tsx:124`, `client/batch/BatchCard.tsx:141`, `client/BatchOverviewPage.tsx:525,541`, `client/BatchListPage.tsx:291`, `client/entry/EntrySheet.tsx:240,552`, `client/entry/EntryHistory.tsx:868`, `client/entry/Paper.tsx:851`, `client/review/model.ts:170,181,200`. Several of these pass `undefined` as the locale as well, so they ignore the chosen language too. Fixing only `when.ts` and the picker would leave the product still disagreeing with itself.

2. The sharpest instance of "the two halves disagree about what day" is one the auditor did not cite. `client/review/model.ts:92-97` buckets the reviewer inbox with `new Date(row.submittedAt).toLocaleDateString('en-CA')` — a device-zone day boundary — while the server's own daily counters use `date_trunc('day', now() at time zone ${timezone}) at time zone ${timezone}` (`review/db.ts:1258`, `:1287`) precisely because "a day is a timezone's, and only a batch has one". A reviewer on a non-Shanghai device sees their inbox grouped under one day and their "decisions today" counter computed against another.

3. `batch.timezone` is inert in both directions, not only on read. `rg -n "timezone" packages/plugins/assessment/core/src/client/` returns nothing, so neither `NewBatchForm.tsx` nor `batch/BatchSettingsForm.tsx…

- **反驳者校正**:The defect is real and reachable, but narrower in one respect and broader in another than stated.

Narrower: the picker and the formatter are mutually consistent on a single device (date-time-picker.tsx:61-71 composes via setHours in the device zone; when.ts:23-34 reads it back in the same zone), so an administrator who picks 23:59 also sees 23:59. The scheduling half of the failure therefore requires the scheduler's machine to be set to a zone other than the batch's calendar - it is not a mis-store that bites a correctly-configured admin. What is unconditional is that no reader is ever shown the batch's declared calendar, so two readers in different zones read different wall clocks for the same boundary, violating docs/assessment-design.md:154. Additionally, no client code ever sets timezone (the field exists in api.ts:1605,1640 but has no UI control - zero 'timezone' hits under src/client), so in practice every batch carries the DB default 'Asia/Shanghai' (db/entities.ts:52); the mismatch is device zone versus that default.

Broader: the site is not just client/batch/when.ts:23. The same device-zone assumption appears in client/phase/PhaseRow.tsx:59, client/batch/BatchProgress.tsx:124, client/batch/BatchCard.tsx:141, client/BatchOverviewPage.tsx:525,541, client/entry/EntrySheet.tsx:240,552, client/entry/Paper.tsx:851, client/entry/EntryHistory.tsx:868 and client/review/model.ts:170,181,200 (several of which also pass undefined as the locale, ignoring the app locale). The sharpest reachable instance is on one screen: ReviewInboxPage.tsx:342 shows handledToday, counted on t…

- **反驳者校正**:The defect is real and the two cited sites are correct, but the blast radius is wider and the practical trigger narrower than stated.

Wider: the fix is not confined to "the formatter and the picker". Thirteen files format instants with device-zone `toLocale*`: packages/plugins/assessment/core/src/client/batch/when.ts, .../batch/BatchCard.tsx, .../batch/BatchProgress.tsx, .../BatchListPage.tsx, .../phase/PhaseRow.tsx, .../entry/Paper.tsx, .../entry/EntrySheet.tsx, .../entry/EntryHistory.tsx, .../review/model.ts, plus packages/web/ui/src/components/{date-time-picker,date-picker,date-range-picker,calendar}.tsx. Any zone-threading pass has to cover the assessment ones, not just when.ts:23.

Narrower: no UI can set batch.timezone — `rg -n "timezone"` over the whole client tree is empty, so the field is only reachable through the API (src/api.ts:1605 on create, :1640 on update) and in practice sits at its 'Asia/Shanghai' default (src/db/entities.ts:52). The mismatch therefore fires only when a scheduler's or reader's device is off UTC+8, which is what keeps this at minor severity rather than major — the two halves disagree in principle on every deployment but produce identical wall clocks on the intended one.

Also worth noting as evidence the design intent holds elsewhere: the date-only path is genuinely safe and needs no change. packages/web/ui/src/components/date-picker.tsx:49 parses `${value}T00:00:00` in the device zone and formats in the same zone, so a date-only value round-trips to the same calendar day everywhere — exactly the outcome docs/assessment-design.md:154 gives…

### [minor · rbac-internals] The diagnostics endpoint answers "not allowed" for a tenant permission the principal holds, whenever a node is named

- 位置:packages/plugins/base/rbac/src/server/authorization.ts:583
- 判定:CONFIRMED,3/3 upheld(第三轮)
- 主张:`explainRows` switches its whole filter to the org-node reach predicate as soon as an orgNodeId is supplied, and a tenant-role grant (org_node_id null, coverage null) can never satisfy it, so `evaluate` contradicts `require()` for tenant-target codes.
- 失败场景:Alice holds a tenant-kind role carrying `iam.role.manage` through a tenant-wide grant (org_node_id null, coverage null). An auditor holding `iam.authorization.inspect` POSTs /iam/access-evaluations with {userId: alice, permissionCode: 'iam.role.manage', orgNodeId: <any real node>}. diagnostics.ts:128 only rejects the _missing_ node for org-target codes, so the tenant code passes through to `explain(tenant, alice, node)`. In explainRows the `where` at authorization.ts:582-595 takes the `reaches(...)` branch, whose three disjuncts are all-active (false for an ordinary role), `coverage = 'self' and org_node_id = $node` (null) and `coverage = 'subtree' and ...` (null) — the row evaluates to NULL and is dropped. `evaluate` finds no entry and returns {allowed: false, target: 'tenant', sources: []}, while `rbac.require(alice, 'iam.role.manage')` (index.ts:401-412, via hasTenantPermission) returns success. The audit tool whose entire purpose is to be trusted about why reports that an administrator lacks a permission she demonstrably has, with no sources to investigate. GET /iam/users/{id}/effective-permissions?orgNodeId=… drops every tenant permission for the same reason.
- 证据:authorization.ts:582-595 (`orgNodeId ? reaches(...) : eb('r.kind','=','tenant')`) versus authorization.ts:154-179 `hasTenantPermission`, which asks only `carries` on an active tenant-kind role and never consults a node. diagnostics.ts:120-138 `evaluate` guards only the org-target/no-node combination. diagnostics.ts:81 (`if (definition.target === 'tenant' && row.coverage !== null) continue`) shows the author expected tenant permissions to appear in this result set and be filtered per-source; with a node supplied they are excluded upstream instead, and with no node supplied a tenant-role grant always has null coverage, so that line can never fire. The module header at diagnostics.ts:20-22 states an explanation that disagrees with the answer is worse than none.
- 最小修法:Keep the tenant branch alive when a node is given — union the `reaches(...)` rows with the `r.kind = 'tenant'` rows — or have `evaluate` route tenant-target codes to the tenant predicate (and reject a node supplied with a tenant code, the way `canAt` rejects a tenant code) rather than silently answering false.
- **反驳者校正**:Confirmed, with two scope corrections.

Accurate as stated: `explainRows` switches its entire WHERE to `reaches(...)` the moment an orgNodeId is supplied (packages/plugins/base/rbac/src/server/authorization.ts:582-595), and a tenant-role grant has `org_node_id IS NULL` and `coverage IS NULL` by check constraint (packages/plugins/base/rbac/src/db/entities.ts:288) and by the write path (packages/plugins/base/rbac/src/server/grants.ts:487-492), so all three disjuncts of `reaches` (authorization.ts:81-85) evaluate to `false OR NULL OR NULL` = NULL and the row is dropped. `evaluate` lets the combination through — its only guard is the org-target/no-node one (packages/plugins/base/rbac/src/server/diagnostics.ts:128) — so it returns `{allowed: false, sources: []}` for a tenant permission that `require()` (packages/plugins/base/rbac/src/server/index.ts:400-412 → `hasTenantPermission`, authorization.ts:154-179, which consults no node at all) grants. `GET /iam/users/:userId/effective-permissions?orgNodeId=…` loses the same rows through the same query (diagnostics.ts:67, handler at index.ts:736-750).

Correction 1 — the loss is not total. A holder of a role with `permission_mode = 'all-active'` (the canonical tenant administrator) is unaffected, because `reachesEveryNode` is the first disjunct of `reaches` (authorization.ts:34-35, 82) and is true regardless of the node. The victims are ordinary tenant-kind roles. This is also why no test catches it: the only diagnostics test (packages/plugins/base/rbac/tests/effect-rbac.test.ts:964-1013) probes an org-target code, and the fixture's so…

- **反驳者校正**:The claim holds; three refinements to how it should be stated.

(1) Scope of the contradiction. It is not "whenever a node is named" for every subject: if the subject holds a role with `permission_mode = 'all-active'` (the canonical tenant-admin, `packages/contracts/rbac/src/canonical.ts:23,42`), the first disjunct of `reaches` (`authorization.ts:82`) is true, the row survives, its coverage is null so `diagnostics.ts:81` keeps it, and `evaluate` correctly answers true. The false negative hits exactly the subjects whose tenant authority comes from an ordinary tenant role — which is every administrator who is not the recovery account, so the practical impact stands.

(2) Blast radius today. No UI or test calls either endpoint: `rg` across the repo for `access-evaluations|effective-permissions` finds only `api.ts:454,471`, the two handlers at `server/index.ts:736-761`, and `tools/tests/support/frozen-routes.ts:17,53`. The wrong answer therefore reaches a human only through direct HTTP against the frozen route (which STATUS.md:190 records as the way this endpoint was actually exercised), not through any shipped screen. That supports the claimed "minor" severity, as does the fact that no enforcement path is affected — `hasTenantPermission`, `canAt` and the transaction-time re-checks are untouched.

(3) Preferred fix. Of the two options offered, rejecting the combination is the one consistent with the codebase's existing rule: `canAt` already dies on a tenant code plus a node (`server/index.ts:189-195`) precisely because "answering false would hide it". Unioning the tenant rows i…

- **反驳者校正**:The claim stands as written; three refinements of scope and detail.

(1) The bug spares the canonical tenant-admin. `reaches`'s first disjunct is `reachesEveryNode` = `permission_mode = 'all-active'` (authorization.ts:34-35, used at :82), so the `system-account` role still surfaces every tenant permission with a node supplied. The wrong answer is confined to ordinary tenant-kind roles — which is arguably worse for an audit tool, since the one principal whose answer is checked most often is the one that looks correct.

(2) The claim's aside that diagnostics.ts:81 "can never fire" is correct for the no-node route but not globally. In the no-node route the filter is `r.kind = 'tenant'` and grants.ts:487-488 forces every tenant-role grant to be tenant-wide, so `row.coverage` is always null and the line never fires — as claimed. In the with-node route the line is reachable: an org-anchored grant whose role carries a tenant-target permission passes `reaches` and is then dropped at :81. So line 81 is not dead code; it is the second of two places that independently guarantee a tenant-target code answers false whenever a node is named.

(3) `effectiveRows` (authorization.ts:463-477) carries the same three-way ternary and would have the same defect, but its `at` branch is currently unreached: the only callers pass `undefined` (index.ts:223, index.ts:250) or `'anywhere'` (index.ts:467). It is a latent copy, not a second live bug — worth fixing in the same pass so it does not become one.

Severity: no browser code calls either endpoint (grepping `getUserEffectivePermissions`/`evaluateA…

### [minor · scoring-results] The item editor's "本题上限" ignores the folding rule and multiplies by maxEntries, overstating what a max/top-n question can ever contribute

- 位置:packages/plugins/assessment/core/src/client/items/ItemConfigEditor.tsx:901
- 判定:CONFIRMED,3/3 upheld(第三轮)
- 主张:The ceiling shown beside the scoring controls is always value × maxEntries, even when the administrator has just selected 只计最高 or 取前 N 条 in the folding control right next to it, so the number that sizes group caps can be several times the amount the scorer will ever grant.
- 失败场景:An administrator configures 学生干部任职: fixed value "2.00", 最多 5 条, folding = 只计最高 (draft.folding 'max', written as `{ ref: 'max@1' }` at line 510-512). The ScoringSummary panel headlines 本题上限 as "10" and explains it as "每条 2 分 × 最多 5 条" (m.itemsCeilingHow at line 1583). The scorer will never grant more than 2.00 for that question — max@1 is `pick(entries, 1)` (builtins.ts:121-126), which counts exactly one line and zeroes the rest. Same for top-n: with n=2 and maxEntries 5 the panel says 10 where the real ceiling is 4. The administrator then sizes the enclosing group's cap against a figure that is 5x (or 2.5x) too large.
- 证据:ItemConfigEditor.tsx:898-903 — `const entries = draft.maxEntries.trim() === '' ? null : Number(draft.maxEntries); const each = Number(draft.fixedValue.trim()); const ceiling = entries === null || !Number.isFinite(each) ? null : amountOf(unitsOf(draft.fixedValue.trim()) * entries)` — `draft.folding` and `draft.topN` appear nowhere in the expression, though they are in scope and are turned into the aggregator ref eight lines below at 510-514. The value is passed straight to ScoringSummary at line 1256 and rendered as the headline number at 1569-1571.
- 最小修法:Derive the multiplier from the folding rule: 1 for 'max', min(topN, maxEntries) for 'top-n', maxEntries for 'sum' — and make the explanatory sentence name the rule it used.
- **反驳者校正**:Confirmed, and broader than stated. The item ceiling is computed as fixedValue × maxEntries with no reference to the folding rule, in two places: `ItemConfigEditor.tsx:902-905` (headlined by ScoringSummary at 1569-1571 and explained at 1583, also shown in the sidebar `Placed` panel via 1352) and `structure.ts:47-51` (`itemCeiling`), whose value is summed into the structure table's group subtotals (`structure.ts:69-85,133`) and into the placement subtotal fed back to the editor (`ItemSettingsPage.tsx:645-652`). Since `aggregatorOf` (`ItemConfigEditor.tsx:510-515`) stores `max@1`/`top-n-sum@1` for declaration and evidence items and the scorer folds with `pick(entries, 1)` / `pick(entries, n)` (`builtins.ts:78-136`, applied at `calc.ts:214-250`), the displayed ceiling overstates the real one by maxEntries× for `max` and by maxEntries/n× for `top-n`. Two corrections to the report: the wrong figure is not confined to the editor panel — it also inflates the group subtotals administrators size caps against — and the unlimited-entries branch errs the other way: with `maxEntries` empty the panel prints 无上限 (`ItemConfigEditor.tsx:1570,1582`) even under `max`/`top-n`, where the ceiling is in fact bounded at `each` or `n × each`. A fix must therefore multiply by 1 for `max`, `min(topN, maxEntries)` for `top-n`, `maxEntries` for `sum`, treat unlimited entries as bounded under `max`/`top-n`, and be applied to `itemCeiling` as well as to the panel, with the explanatory sentence naming the rule it used.
- **反驳者校正**:The finding is real, but it is narrower in its citation than in its blast radius: the same folding-blind arithmetic exists a second time in `packages/plugins/assessment/core/src/client/items/structure.ts:46-51`, where `itemCeiling(item)` returns `unitsOf(each) * item.maxEntries` reading only `calculator.config.value` (`eachOf`, lines 31-36) and never the aggregator. That function feeds `held()` (lines 69-84), which is what produces every group's `subtotal` row in the structure table (line 133). So the administrator's comparison of a group cap against "what the questions inside add up to at most" is inflated by the same factor, independently of the item editor's panel — fixing only `ItemConfigEditor.tsx:899-904` would leave the structure table still overstating. Corrected line numbers: the ceiling expression is at `ItemConfigEditor.tsx:899-904` (not 898-903) and `aggregatorOf` at 509-515 (not 510-514); the ScoringSummary hand-off (1256), headline (1569-1571), `m.itemsCeilingHow` (1583) and `builtins.ts:121-126` are cited correctly. A correct multiplier is 1 for 'max', `min(topN, maxEntries)` for 'top-n', `maxEntries` for 'sum', and it belongs in one shared helper used by both call sites, with the explanatory sentence naming the rule it applied.
- **反驳者校正**:The defect is real and current. In packages/plugins/assessment/core/src/client/items/ItemConfigEditor.tsx:900-905 the per-item ceiling is computed as `unitsOf(draft.fixedValue) * entries` with no reference to `draft.folding` or `draft.topN`, even though those same fields are converted into the persisted aggregator ref ten lines below (`aggregatorOf`, :511-515) and their controls sit in the same `!granted` block as the maxEntries input (:1183-1247). Because `max@1` is `pick(entries, 1)` and `top-n-sum@1` is `pick(entries, n)` (packages/plugins/assessment/core/src/scoring/builtins.ts:78-98, 122-137), a question with fixed value 2.00, 最多 5 条 and folding 只计最高 is headlined as 10 where the scorer can never grant more than 2.00.

Three corrections to the report as filed:

1. The label is 'Maximum score from this item' / 本项目计分上限 (i18n.ts:2955-2958; locales/zh-CN.ts:771), not 本题上限.
2. The wrong figure is rendered twice, not once: the ScoringSummary headline at :1569-1571 and again in `Placed` at :2018-2021 — the panel that lists this item's ceiling directly above the enclosing section caps and the paper total. That second site is the one that actually misleads cap sizing, and the report omits it.
3. The impact is confined to display. The saved aggregator (:511-515) and maxEntries (:601) are correct, and no scoring, validation, or API path reads this value, so real scores are unaffected — the harm is an administrator sizing a group cap against a number up to maxEntries times too large.

Supporting rather than excusing evidence: docs/assessment-design.md:1313 (§32.65) already rules tha…

### [minor · ui-projection] The one deliberate retry in the query client can never fire, because every Effect transport failure is tagged

- 位置:packages/web/runtime/src/index.tsx:120
- 判定:CONFIRMED,3/3 upheld(第四轮)
- 主张:The QueryClient retry predicate treats any object carrying a `_tag` as a decided server refusal, but Effect's HttpClientError is a Data.TaggedError, so a dropped connection is classified as a refusal and the single retry the comment says is kept for exactly that case never happens.
- 失败场景:A reader on flaky wifi loads the app; GET /api/app/manifest is dropped in flight (no response). Effect's http client fails with HttpClientError{_tag:'HttpClientError', reason:{_tag:'TransportError'}}. retry(0, error) evaluates `'_tag' in error` → true → returns false, so TanStack does not retry. RuntimeLoader takes the manifest.isError branch (packages/web/runtime/src/index.tsx:163-170) and the whole application renders the full-screen 'could not load' panel with a manual Retry button on the very first blip, where the intended behaviour was one silent second attempt. The same holds for every page query: one dropped packet turns each section into an error panel instead of retrying once.
- 证据:packages/web/runtime/src/index.tsx:117-121 — the comment states 'One retry is kept for the case the reader cannot see: a connection that dropped between here and the server, where the second attempt genuinely may succeed', then implements `attempt < 1 && !(typeof error === 'object' && error !== null && '_tag' in error)`. Vendored source shows the class is tagged: repos/effect/packages/effect/src/unstable/http/HttpClientError.ts:34 `export class HttpClientError extends Data.TaggedError("HttpClientError")<{...}>`. Verified against the installed package with node -e importing effect/unstable/http/HttpClientError and constructing one: printed `has _tag: true "HttpClientError" reason tag: TransportError`. The codebase already relies on this fact elsewhere — packages/web/i18n/src/format.ts:94-100 detects a network failure precisely by `candidate._tag === 'HttpClientError' && candidate.reason?._tag === 'TransportError'`, with a comment noting that checking for a bare TypeError 'matched nothing a screen actually sees'.
- 最小修法:Reuse the existing predicate instead of a second, weaker copy: `(attempt, error) => attempt < 1 && isTransportError(error)` (isTransportError is exported from @qualy/web-i18n), which retries only the undecided transport case and never a declared domain failure.
- **反驳者校正**:The defect is real but slightly over-stated as "can never fire". Precisely: the predicate at packages/web/runtime/src/index.tsx:120-121 excludes every failure carrying a `_tag`, and because Effect v4's runPromise squashes the Fail to the error itself (repos/effect/packages/effect/src/internal/effect.ts:299-303, :5476) while FetchHttpClient wraps a dropped fetch as a tagged HttpClientError{reason: TransportError} (repos/effect/packages/effect/src/unstable/http/FetchHttpClient.ts:74-80, HttpClientError.ts:34), the retry never fires for the transport case the comment says it exists for — nor for any 4xx/5xx StatusCodeError, DecodeError or SchemaError, which is the intended exclusion. It does still fire for untagged rejections: a defect thrown inside the fiber (Die, squashed at internal/effect.ts:303) and the plain `new Error("All fibers interrupted without error")` produced for a pure interrupt (internal/effect.ts:305). So the predicate is exactly inverted in effect: it retries only bugs and cancellations, and never the undecided network case. One further detail the claim does not mention: because query-core evaluates shouldRetry before the networkMode pause (retryer.js:89-97), a refused retry is not even parked while the browser is offline — it rejects straight into the error panel. The proposed fix is sound: isTransportError is exported from @qualy/web-i18n (packages/web/i18n/src/index.tsx:33, format.ts:63-99), which the runtime already imports (index.tsx:23), so no new dependency edge.
- **反驳者校正**:The retry predicate at packages/web/runtime/src/index.tsx:120-121 is not dead — it is inverted. Because runPromise rejects with the squashed cause (repos/effect/packages/effect/src/internal/effect.ts:5474-5476, 301-308), the '_tag' test excludes every declared failure AND the HttpClientError/TransportError that a dropped connection produces (FetchHttpClient.ts:75-76, HttpClientError.ts:34, HttpApiClient.ts:77), while it still returns true for the untagged residue: a Die defect (internal/effect.ts:303-304) and the synthesized Error("All fibers interrupted without error") for an interrupted fiber (internal/effect.ts:305-306). So the single retry fires only where a second attempt cannot help and is withheld exactly where the comment at index.tsx:117-119 says it is kept. User impact is limited to losing one silent re-attempt: the shell (index.tsx:163-171) and each section (packages/web/ui/src/components/admin.tsx:136-188) still render a manual Retry.
- **反驳者校正**:The claim holds as stated; two refinements.

(1) The predicate is not merely "never fires" — it is inverted. Because `Cause.squash` hands TanStack the raw failure value (repos/effect/packages/effect/src/internal/effect.ts:5474-5477), every failure the api runtime can produce through a declared channel is tagged: transport drops (`HttpClientError`, Data.TaggedError per repos/effect/packages/effect/src/unstable/http/HttpClientError.ts:34), 4xx/5xx response errors, and every schema-declared domain error. What is left untagged, and therefore what the surviving `attempt < 1` branch actually retries, is the residue: defects and interrupts. The predicate thus skips the one undecided case it was written for and retries the cases where a second attempt is least likely to help.

(2) Severity is minor and the user-visible cost should be stated as one extra click, not as data loss: `packages/web/runtime/src/index.tsx:163-170` renders the failure panel with a working manual Retry button (`manifest.refetch()`), and page-level sections do the same by design (the comment at index.tsx:111-116 says so). Nothing is lost; the intended silent second attempt is simply never made.

The proposed fix is sound and needs no new dependency: `isTransportError` is exported at packages/web/i18n/src/index.tsx:33 and packages/web/runtime already depends on and imports @qualy/web-i18n (package.json:24, index.tsx:23). Its behaviour is already frozen by packages/web/i18n/tests/i18n.test.ts:76,79. If the fix lands, the retry predicate deserves a test of its own — it currently has none, and packages/web/runtime…

### [minor · unbounded-work] The new stream backoff can never grow past one step, because the server sends a sync event on every connection and any received event resets it

- 位置:packages/web/runtime/src/api-stream.ts:61
- 判定:CONFIRMED,2/3 upheld(第四轮)
- 主张:useApiStream resets backoff to REDIAL_MS inside the per-event handler, and watchBatch emits a sync event as the first item of every stream, so a connection that opens and then fails re-dials on a fixed ~6s cycle forever — the 60s ceiling the fix added is unreachable on that path.
- 失败场景:The server accepts the SSE connection but the stream dies shortly after opening — an overloaded backend, a proxy that resets mid-body, the AssessmentLive relay throwing, a rolling restart where each new pod accepts and then drops. watchBatch always begins with Stream.concat(Stream.succeed({kind:'sync'}), ...) (server/index.ts:4253-4257), so the client always receives at least one event; api-stream.ts:56-63 sets backoff = REDIAL_MS on every received event, including that first sync and the 25s heartbeat. The .catch at :74-78 then doubles from 3000, never from a larger value: the cycle is open → sync → reset to 3000 → fail → 6000 → wait 6s → open → sync → reset. Ten dials per minute per open tab, indefinitely, which is the behaviour the commit message set out to stop. Each dial is not free on the server: watchBatch runs assertVisible plus capabilitiesFor per connect (server/index.ts:4215-4227), so 5,000 students with the page open produce ~830 authorized connects per second against a server already failing. The genuinely-down case (fetch never opens) does back off correctly; the degraded case does not.
- 证据:packages/web/runtime/src/api-stream.ts:56-63 (backoff = REDIAL_MS inside the per-event handler), :74-78 (doubling only in catch), :82 (setTimeout with that backoff); packages/plugins/assessment/core/src/server/index.ts:4253-4257 (every stream starts with a sync event), :4249-4251 (25s heartbeat also resets it), :4215-4227 (assertVisible + capabilitiesFor per connect).
- 最小修法:Reset the backoff on a connection that stayed up rather than on any event — e.g. record the open timestamp and only reset when the stream lasted longer than some threshold — so a connection that opens and immediately dies still escalates toward REDIAL_MOST_MS.
- **反驳者校正**:The claim is correct in substance; two refinements. (a) The title's unqualified "can never grow past one step" is only true on paths where each dial delivers at least one element or ends cleanly — consecutive dials that fail before delivering anything do escalate to the 60s ceiling, as the claim's own body concedes. (b) The claim understates the reset surface: api-stream.ts:68-73 also resets `backoff = REDIAL_MS` on a clean stream end, so a connection that opens, delivers nothing, and EOFs cleanly pins the cycle at 3s rather than 6s. Accurate statement: because watchBatch emits `sync` as the unconditional first element (server/index.ts:4255-4258) and because api-stream.ts:61 (any element) and :71 (clean end) both reset the counter, any dial that reaches the response body returns the backoff to REDIAL_MS; escalation toward REDIAL_MOST_MS therefore requires consecutive failures that neither deliver an element nor end cleanly, which the degraded server case (accept-then-drop, proxy reset mid-body, relay error) never produces.
- **反驳者校正**:The claim is correct and unaddressed by any test, ADR, design-doc ruling, or note. One refinement: `api-stream.ts:61` is not the only unconditional reset — the clean-end branch at `api-stream.ts:68-73` also sets `backoff = REDIAL_MS` on any resolved run, with no regard for how long the connection lived. A middlebox or backend that accepts the SSE request and then closes the response body without an error takes that path and produces an even tighter fixed 3s cycle, with the event handler never involved. So the proposed fix (reset only when the connection stayed up past some threshold) must gate both the per-event reset at :61 and the clean-end reset at :71; gating only :61 would leave the clean-end variant dialling every 3s.
- 反对票(未采纳):The mechanism is real as code, but every path the auditor names either backs off correctly, cannot happen, or is infrastructure this repository does not deploy — and the scale figure is invented and smaller than the traffic the same screen deliberately generates in the same degraded state.

MECHANISM (verified, not disputed): `packages/web/runtime/src/api-stream.ts:61` sets `backoff = REDIAL_MS` inside the per-event handler; `packages/plugins/assessment/core/src/server/index.ts:4255-4258` does begin every stream with `Stream.succeed({kind:'sync'})`, and `:4250-4252` adds a 25s heartbeat. If a connection opens, delivers sync, then errors, the doubling at `:76` restarts from 3000.

WHY IT DOES…

## 被反驳(不修)

### [auth-identity] Suspending a user type or a tenant pauses sessions instead of revoking them; restoring the tenant resurrects every pre-suspension token

- 位置:packages/plugins/base/auth/src/server/session.ts
- 反驳:The auditor's line-level reading of session.ts is literally correct but its two failure levers are both unreachable, and one of them is the fix the auditor mistook for the bug. No Effect combinator is load-bearing here — both branches are plain control flow (`yield* clear(); return yield* new AuthRequired()`, session.ts:208-211), so nothing in repos/effect changes the outcome; the question is purely which writes can put a live session into the `!usable` state.

WHAT IS TRUE. session.ts:71-74 computes `usable` as `u.enabled and t.enabled and n.enabled and (n.expiresAt is null or n.expiresAt > now())`; the expired branch deletes (session.ts:131-135, 201-205) and the `!usable` branch does not (session.ts:138-141, 206-211). `deleteFrom('Session')` exists at exactly three sites: session.ts:80, sign-in.ts:183 (logout), users.ts:348-351 (called only from users.ts:661). The code comment at sessi…

- 反驳:The mechanism is described accurately — `resolve()` and the Authenticated middleware clear the cookie without deleting the row on the `!usable` branch (packages/plugins/base/auth/src/server/session.ts:138-141 and :208-211, versus the expired branch calling `deleteSession` at :132 and :202). But neither of the two states that make `usable` false (session.ts:71-74) is reachable by any actor through this codebase.

(b) TYPE BRANCH — closed by upstream checks in both directions, and the auditor concedes half of it.

- Disabling a type that anyone holds is refused: `countUsersOfType(...) > 0 → UserTypeInUse` (packages/plugins/base/auth/src/server/user-types.ts:426-433). The comment there names exactly this defect as the reason the refusal exists: "It used to be allowed and did two wrong things at once: it revoked sign-in for every holder without ending a single session, so re-enabling handed t…
- 反驳:FRESHNESS: the code is unchanged and still reads as described — `packages/plugins/base/auth/src/server/session.ts:138` and `:208` do `if (!session.usable) { yield* clear(); ... }` with no `deleteSession`, and the three session-deleting writes are exactly `session.ts:80`, `sign-in.ts:183`, `users.ts:350`. `git log -3 -- packages/plugins/base/auth/src/server/session.ts` shows the last touch was c0ceffec (2026-08-07, kysely scope-handle refactor); none of the recent commits went near it. So nothing here is stale — but the claim is refuted on prior art, on all three of its parts.

(1) THE TYPE BRANCH IS AN ALREADY-RECORDED RULING, AND THE CLAIM HAS IT BACKWARDS. `packages/plugins/base/auth/src/server/user-types.ts:426-433` states the exact hazard and its resolution verbatim: "Disabling a type people still hold is refused outright. It used to be allowed and did two wrong things at once: it re…
